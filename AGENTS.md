# AGENTS.md

This file stores durable collaboration rules for this repository.
When user-assistant decisions affect workflow or project scope, update this file.
Keep this rule itself in the file.

## Working Rules

1. Use `conda` environment `handvla` as the default runtime for install and checks.
2. Do not recreate `.venv` unless explicitly requested.
3. Put temporary experiment code, debug scripts, and logs in `codex/`.
4. Before final upload/push, clean up temporary files in `codex/` and keep only what is intentionally versioned.
5. Keep this file updated continuously with important decisions from user-assistant conversation.
6. Record this policy itself as durable context whenever AGENTS.md is updated.

## Current Project Baseline (2026-02-19)

1. Repository goal is to build from previous `keti_dual_arm_VLA` assets into a clean `handvla` project.
2. First runnable target is MuJoCo Allegro hand-only simulation.
3. `run_allegro_hand.py` launches the hand model with right control panel sliders.
4. Hand-only scene should include visible background/floor/lighting by default.
5. Default initial pose should be wrist-lower, hand-up orientation.
6. Background/lighting style should stay aligned with `keti_dual_arm_VLA` base scene unless user asks otherwise.
7. TCP markers should be defined near each fingertip pad, shifted slightly palm-ward for grasp experiments.
8. Viewer should start with a close frontal hand camera, not a distant wide shot.
9. Finger IK feasibility should be tested with per-finger random reachable targets and reported as success rate/error statistics.
10. Temporary logs stay under `codex/logs/`, but core experiment scripts should live in project root or stable modules.
11. `run_allegro_hand.py` should support 1920x1080 recording to `codex/logs/` by default when requested.
12. `finger_ik_experiment.py` should show viewer by default unless `--no-viewer` is passed.
13. IK experiment viewer should use the same default camera as `run_allegro_hand.py` and show target TCP markers clearly.
14. Default visible update speed for viewer-driven experiment scripts should be around 60Hz (`--viewer-step`), with optional manual delay override.
15. Finger IK quick runs should default to 3 trials per finger.
16. Only active finger target marker should be visible during IK trials to avoid visual confusion.
17. `finger_ik_experiment.py` should support 1920x1080 recording options similar to `run_allegro_hand.py`.
18. IK feasibility code and mustard grasp/data-collection code must stay in separate scripts.
19. `finger_ik_experiment.py` is IK-only (no mustard/object mode).
20. Mustard grasp pretraining scope is mustard-only (`006_mustard_bottle`) in `collect_mustard_grasp.py`.
21. Mustard fixed-air spawn pose uses palm-relative offset `[0.10, 0.00, 0.02]` with horizontal+clockwise quaternion `[0.5, 0.5, 0.5, -0.5]`.
22. Mustard object fixation uses freejoint pose clamp (kinematic lock) each simulation step.
23. Grasp success labeling uses geom contact between Allegro fingertip/distal geoms and mustard geoms with thumb-contact requirement plus contact/finger/force/stability thresholds.
24. Mustard grasp closing should use a thumb-opposition pre-shape phase (`open -> pre-shape -> close`) so the thumb rotates inward before full flexion.
25. Dataset collection for training should use `collect_mustard_grasp_dataset.py` and save only successful episodes until target count is reached.
26. Default mustard dataset logging rates are control 100Hz and capture 20Hz (via `capture_every=5`).
27. Mustard raw dataset path is `dataset/mustard_grasp/raw/episode_*.npz` with summary at `dataset/mustard_grasp/collection_summary.json`.
28. OXE conversion should use `convert_mustard_raw_to_oxe.py` to export RLDS/TFDS from raw episodes.
29. Octo fine-tuning should use `finetune_mustard_octo.py` with `mustard_grasp_oxe` (`image_primary` + `state` -> 12-DoF TCP action head: 4 fingertips x xyz).
30. Octo training runtime currently requires `conda` env `octoketi` (while `handvla` remains default for simulation/data tools).
31. Fine-tuning outputs should be stored under `models/mustard_octo_overfit/run_*` with checkpoints, `final_model`, and loss trace.
32. `finetune_mustard_octo.py` should use direct TFDS episode loading (not `make_single_dataset`) for compatibility with current `octoketi` TFDS stack.
33. Current Octo fine-tune script supports `window_size=1` (single-frame policy training).
34. Octo loss compatibility requires `action` and `action_pad_mask` batch tensors shaped as `[B, window(1), action_horizon, action_dim]`.
35. Finetuned model rollout validation should use `rollout_mustard_octo.py` in the same fixed mustard scene with contact-based success metrics.
36. Default stable rollout settings for current overfit model are `max_policy_steps=100`, `control_repeat=5`, `action_smoothing=0.35`.
37. `rollout_mustard_octo.py` should run in `conda` env `handvla` after Octo-compatible dependencies are installed.
38. `rollout_mustard_octo.py` auto-resolves Octo source path from `/home/minchan/Downloads/dual_arm_VLA/octo`, or from project-root `.octo_src_path` override.
39. `collect_mustard_grasp_dataset.py` default `--action-interface` is `tcp12`; existing legacy datasets may still contain `joint16`.
40. `rollout_mustard_octo.py` default `--action-interface` is `tcp12`; use `--action-interface joint16` only for legacy 16-DoF models.
41. Legacy 16-DoF artifacts should use `full_joint` naming (`dataset/mustard_grasp_full_joint`, `dataset/mustard_grasp_oxe_full_joint`, `models/mustard_octo_overfit_full_joint`).
42. Keep presentation-ready comparison videos under `codex/logs/` with explicit interface in filename (e.g., `full_joint_*.mp4`).
43. Rollout recording should keep the default frontal hand camera even when running with `--no-viewer` (headless).
44. Use `rollout_mustard_octo_tcp12.py` for tcp12-specific rollout debugging (step-level `tcp_delta`/`q_delta` logging).
45. TCP12 dataset semantics are command next-delta: `action_t = FK(q_cmd_{t+1}) - FK(q_cmd_t)` (final step repeats previous delta); default frame is `palm_local` (legacy option: `world`).
46. TCP12 rollout interpretation default is delta mode (`tcp_target = tcp_now + pred_delta_world`), where `palm_local` predictions are rotated to world using current palm orientation; absolute mode remains legacy compatibility only.
47. Oracle replay diagnostic (2026-02-26): `joint16` replay reproduces grasp success (100/100), while `tcp12` (`delta` and `absolute`) replay gives 0/100 under thumb-required success criteria.
48. Under current geom-contact success rules, `tcp12` position-only replay consistently misses thumb contact (`best_fingers` tends to `ff,mf,rf`), indicating interface loss (thumb posture/opposition) rather than pure model-learning failure.
49. Validation note: if thumb-contact requirement is disabled, oracle replay success for `tcp12` becomes 100/100; therefore future 12-DoF interface work must encode thumb opposition intent/posture (or revise success/target definition) before fair comparison with full-joint control.
50. Policy-direction diagnostic (2026-02-26): current tcp12-delta model outputs for `ff/mf/rf` are weaker than demonstration scale and often point away from object center, while thumb output dominates; this matches observed thumb-only contact in rollout videos.
51. Keep `codex/logs/tcp12_direction_diagnostics_20260226.json` as reference when discussing why tcp12 VLA behavior differs from per-finger IK feasibility tests.
52. Next primary low-dimensional interface direction is `joint-synergy kD` (joint-space compression) rather than naive `tcp12 xyz-only`; detailed execution plan is tracked in `docs/joint-synergy-kD.md`.
53. Maintain a separate presentation-oriented experiment narrative document at `docs/experiment-flow-for-presentation.md` so slide preparation can follow the verified experimental timeline and conclusions.
54. Joint-synergy tooling is implemented with `build_joint_synergy_basis.py`, `convert_full_joint_raw_to_synergy_raw.py`, and `rollout_mustard_octo_synergy.py`.
55. Current full_joint mustard dataset is effectively low-rank; PCA basis selection run (k=4,6,8,10) produced oracle success 100% for all tested k and selected `k=4` as the smallest valid default (`dataset/synergy_basis/full_joint_pca_k4.npz`).
56. Current synergy experiment artifacts: raw `dataset/mustard_grasp_synergy_k4`, OXE `dataset/mustard_grasp_oxe_synergy_k4`, model `models/mustard_octo_overfit_synergy_k4/run_260227_115921`, eval `codex/logs/synergy_k4_eval_summary_20260227.json`.
57. Initial synergy rollout check with the above `k=4` model achieved multi-finger + thumb contact and 100% success on a 3-episode smoke evaluation.
58. `collect_mustard_grasp_dataset.py` now supports optional diversity controls (spawn jitter, yaw jitter, pose noise, phase-step jitter) and should keep default-zero behavior for legacy deterministic collection.
59. Diverse benchmark dataset uses `dataset/mustard_grasp_full_joint_diverse` with diversity config: xy 0.02m, z 0.01m, yaw 20deg, preshape noise 0.05rad, close noise 0.08rad, step jitter (4,5,15,8), seed 42.
60. Diverse synergy basis run (`dataset/synergy_basis_diverse/full_joint_pca_summary.json`) selected `k=4` with cumulative variance ~0.992 and oracle success 1.0.
61. Diverse fair-training artifacts: full-joint model `models/mustard_octo_overfit_full_joint_diverse/run_260228_011500`, synergy model `models/mustard_octo_overfit_synergy_k4_diverse/run_260228_012422`.
62. Diverse 10-episode rollout evaluation reached 100% success for both interfaces; synergy-k4 showed higher best-contact-finger count (4.0 mean) than full-joint baseline (3.0 mean) in current fixed-scene benchmark.
63. Presentation-ready diverse comparison outputs are `codex/logs/json/diverse_joint_synergy_comparison_20260228.json`, `codex/logs/diverse_joint_synergy_comparison_20260228.md`, plus videos `codex/logs/videos/full_joint_diverse_rollout_demo.mp4` and `codex/logs/synergy_k4_diverse_rollout_demo.mp4`.
64. Viewer simulation loops should not use `mj_step` 1-per-frame for interactive scripts; use batched stepping via `nstep ~= (1/viewer_step)/timestep` (default viewer-step 60Hz) to avoid slow/laggy perceived control.
65. `scripts/sim/run_franka_allegro_mustard.py` keeps right-side UI off by default for performance; enable only with `--show-right-ui`.
64. `rollout_mustard_octo.py` supports `--show-fk-tcp-markers` to visualize FK TCP world positions as blue mocap spheres for full-joint-to-TCP extraction validation and recording.
65. `collect_mustard_grasp_dataset.py` stores auxiliary command traces in raw episodes: `action_joint16_cmd`, `action_tcp12_cmd_world_abs`, `action_tcp12_cmd_world_next_delta`, and `action_tcp12_cmd_palm_local_next_delta` for replay/debug parity checks.
66. Current tcp12 interface-fix stage keeps action dimension at 12 (no extra intent dims yet); focus is command semantics/frame consistency first.
67. Historical command-based tcp12 artifacts (obs->cmd delta stage, 2026-03-03): raw `dataset/mustard_grasp_tcp12_cmd`, OXE `dataset/mustard_grasp_oxe_tcp12_cmd`, model `models/mustard_octo_overfit_tcp12_cmd/run_260303_153616`.
68. TCP12 semantics comparison eval (2026-03-03, 10 episodes each): legacy-world and command-palm_local (obs->cmd delta stage) both scored 0% success under thumb-required rule; command model improved contact occurrence but remained thumb-only. See `codex/logs/tcp12_legacy_world_eval_summary_20260303.json`, `codex/logs/tcp12_cmd_palm_local_eval_summary_20260303.json`, and `codex/logs/json/tcp12_semantics_comparison_20260303.json` with demo videos `codex/logs/tcp12_legacy_world_rollout_demo_20260303.mp4`, `codex/logs/tcp12_cmd_palm_local_rollout_demo_20260303.mp4`.
69. Current cmd-next tcp12 artifacts (2026-03-03): raw `dataset/mustard_grasp_tcp12_cmd_next`, OXE `dataset/mustard_grasp_oxe_tcp12_cmd_next`, model `models/mustard_octo_overfit_tcp12_cmd_next/run_260303_171027`.
70. Cmd-next tcp12 10-episode rollout (2026-03-03, thumb-required) with `models/mustard_octo_overfit_tcp12_cmd_next/run_260303_171027/final_model` still produced 0% success and 0 contact-fingers across episodes; step logs show early motion followed by near-zero updates (`q_delta -> 0`). Demo video: `codex/logs/videos/tcp12_cmd_next_palm_local_rollout_demo_20260303.mp4`.
71. Expanded diverse synergy basis sweep (2026-03-04) tested `k=2,3,4,6` at `dataset/synergy_basis_diverse_k236/full_joint_pca_summary.json`; all ks achieved oracle replay success 1.0, and `k=2` already retained high variance (~0.9899 cumulative).
72. Diverse k-ablation artifacts (2026-03-04): raw `dataset/mustard_grasp_synergy_k{2,3,4,6}_diverse_k236`, OXE `dataset/mustard_grasp_oxe_synergy_k{2,3,4,6}_diverse_k236`, models `models/mustard_octo_overfit_synergy_k{2,3,4,6}_diverse_k236/run_260304_*`, eval summaries `codex/logs/synergy_k{2,3,4,6}_diverse_k236_eval_20260304.json`.
73. Diverse 5000-step rollout eval for `k=2,3,4,6` (10 episodes each, thumb-required) all reached 100% success with 4 contact fingers; comparison table is tracked in `codex/logs/synergy_k_ablation_20260304.{json,md}`.
74. User-selected follow-up baseline (2026-03-04) is `k=4`; dedicated retrain/eval artifacts are `models/mustard_octo_overfit_synergy_k4_selected/run_260304_140028`, `codex/logs/synergy_k4_selected_eval_20260304.json`, and `codex/logs/synergy_k4_selected_summary_20260304.{json,md}`.
75. Paper direction is fixed to `action-interface co-design` for unified arm-hand VLA (not just end-to-end demonstration); ICCAS draft execution plan is tracked in `docs/iccas-action-interface-plan.md`.
76. Before upload, keep only intentionally versioned outputs and prioritize pruning heavy experiment byproducts under `dataset/`, `models/`, and temporary `codex/tmp_*` directories.
77. A repeatable pre-push cleanup script is available at `tools/cleanup_before_push.sh`; default keep set preserves only current core comparison artifacts (`full_joint_diverse` vs `synergy_k4_selected`) and synergy basis files.
78. `.gitignore` now ignores `dataset/` and `models/` by default to prevent accidental large artifact uploads; keep release assets separately if explicitly needed.
79. Log/media pruning policy before upload: keep one representative rollout video per algorithm family in `codex/logs/` (`full_joint`, `tcp12`, `synergy-k4`, plus optional IK feasibility), and remove redundant benchmark/debug clips.
80. Canonical executable entrypoints are now organized under `scripts/` by role: `scripts/sim`, `scripts/data`, `scripts/train`, `scripts/eval`, `scripts/research`.
81. Research/design documents are organized under `docs/` (`docs/iccas-action-interface-plan.md`, `docs/joint-synergy-kD.md`, `docs/experiment-flow-for-presentation.md`).
82. Log/dataset/model cleanup helpers are organized under `tools/` (`tools/cleanup_before_push.sh`, `tools/prune_logs_representative.sh`) and should be run from repository root (scripts auto-resolve root).
83. A single-arm tabletop scene is added for next-stage experiments: `scripts/sim/run_franka_allegro_mustard.py` using `env/franka_allegro_mjcf.py` (`Franka Panda nohand + Allegro + mustard`).
84. Franka assets are vendored under `env/assets/franka_emika_panda` (source: MuJoCo Menagerie), and mustard object remains `env/assets/ycb/006_mustard_bottle`.
85. Franka tabletop scene uses dedicated `env/assets/franka_scene.xml` (floor at z=0, reduced shadow quality, timestep 0.002) to avoid floating appearance and improve viewer responsiveness.
86. Executable scripts now self-bootstrap repository root path, so commands in README run without `PYTHONPATH=.`.
87. Franka tabletop placement is anchored on the table: Franka base frame at `[0.30, -0.10, 0.74]` and mustard spawn at `[0.62, 0.06, 0.82]` in `env/franka_allegro_mjcf.py`.
88. Shadows are disabled for performance in both hand/franka scenes (`castshadow=false`, minimal `shadowsize`), including Franka's internal top light in `panda_nohand.xml`.
89. `scripts/sim/run_franka_allegro_mustard.py` supports arm IK mode via `--arm-mode ik-approach`, driving Franka 7-DoF toward `mustard + offset` while keeping Allegro hand posture fixed.
90. Franka pick-and-lift dataset collection uses `scripts/data/collect_pickandlift_rlds.py` with phase policy `settle -> approach(IK) -> pre-shape -> close -> lift`.
91. Pick-and-lift success is defined jointly as arm reach (`approach_min_err <= threshold`), geom-contact grasp stability (fingertip/distal vs mustard with thumb requirement by default), and object lift (`z` increase >= `lift_success_delta`) with stable-step checks.
92. Verified baseline run on 2026-03-04: `dataset/franka_pickandlift` reached 3/3 saved success episodes in 3 attempts using headless low-capture settings (`capture_hz=2`, `320x240`).
93. Pick-and-lift mustard spawn default is upright (`spawn_quat=[1,0,0,0]`) at `spawn_pos=[0.62, 0.06, 0.82]` in `collect_pickandlift_rlds.py`.
94. To prevent pre-grasp collapse of upright bottle, `collect_pickandlift_rlds.py` defaults to `--lock-object-until-close` (pose clamp during settle/approach/preshape only).
95. Current upright pick-and-lift default arm targets are `approach_offset=[-0.08, 0.00, 0.06]` and `push_offset=[-0.04, -0.03, 0.03]`.
96. Lift success is intentionally stricter: default `lift_height=0.20`, `lift_success_delta=0.08`, and required hold `lift_hold_seconds=1.5` (continuous contact+lift condition during hold).
97. Upright strict-criteria smoke checks on 2026-03-05 succeeded (`dataset/franka_pickandlift_tmp_uprightcheck3`: 3/3 in 3 attempts).
98. Pick-and-lift arm offsets should be interpreted in object frame by default (`--arm-offset-frame object`) to stay aligned with object axes and reduce unnatural approach direction.
99. Pick-and-lift hand closing defaults to `--hand-trajectory keti_human` with `preshape_grasp_ratio=0.45` for smoother, human-like wrap grasp.
100. Pick-and-lift success defaults now enforce multi-finger quality (`min_contact_fingers=3` + thumb required), preventing two-finger transient successes.
101. Verified updated human-like defaults on 2026-03-05: `dataset/franka_pickandlift` reached 3/3 with best fingers consistently `ff,mf,rf,th` and `object_dz_max≈0.177m`.
102. Added dedicated pre-grasp validation script `scripts/sim/run_franka_pregrasp_ik.py` using Franka 6D IK (position+orientation) with object-frame target pose and fixed hand pre-shape for "grasp-just-before" pose checks.
103. Verified 6D pre-grasp capture on 2026-03-05 with default target offset `[-0.078,-0.015,0.065]` in object frame: result `reached=True`, `pos_err≈0.0048m`, `rot_err≈0.58deg`, outputs `codex/logs/images/franka_pregrasp_axes_mapping_20260305.png` and `codex/logs/videos/franka_pregrasp_axes_mapping_20260305.mp4`.
104. `scripts/data/collect_pickandlift_rlds.py` now uses object-6D-aligned arm targets by default (position offsets + quaternion offsets in object frame) and 6D IK (position+orientation) instead of position-only IK.
105. Pick-and-lift raw episodes now include arm pose supervision traces: `arm_cmd_pose_wxyz`, `arm_obs_pose_wxyz`, and `arm_pose_error` (`[pos_m, rot_deg]`) for command-vs-observation alignment validation.
106. Current object-6D pick-and-lift defaults are tuned for stable collection: `approach_steps=700`, `preshape_steps=150`, `close_steps=280`, `ik_rot_weight=0.20`, `arm_approach_offset=[-0.09,-0.015,0.04]`, `arm_push_offset=[-0.078,-0.015,0.01]`.
107. For the above 6D target configuration, `collect_pickandlift_rlds.py` default hand trajectory is `thumb_o_wrap` (wider thumb opposition with delayed curl); `thumb_opposition`, `power_linear`, and `keti_human` remain optional via `--hand-trajectory`.
108. Verified object-6D default collection on 2026-03-05: `dataset/franka_pickandlift_object6d_run0305` reached `saved_success_episodes=3` in `attempts=3` with thumb-required multi-finger contact (`ff,mf,rf,th`) and strict lift-hold criteria.
109. For alignment-only validation (no grasp/lift), use `scripts/sim/run_franka_pregrasp_ik.py`; this script should stop at precise arm-hand pre-grasp alignment against object pose.
110. `env/franka_allegro_mjcf.py` now supports `add_frame_axes` and `run_franka_pregrasp_ik.py` enables it by default (`--show-axes`) to visualize XYZ frames on both palm and mustard for capture-based verification.
111. Default palm-vs-object axis mapping for pre-grasp alignment is now set by object-frame quaternion `[0.70710678, 0.70710678, 0, 0]` (hand red≈object red, hand green≈object blue, and hand blue≈-object green for right-handed consistency).
112. Pre-grasp alignment default uses fully open hand (`hand_preshape_ratio=0.0`) so approach is executed with the hand spread before grasp phases.
113. Verified lowered-hand pick-and-lift defaults on 2026-03-05: `dataset/franka_pickandlift_object6d_low2b_20260305` reached `saved_success_episodes=3` in `attempts=3` with offsets `approach_z=0.04`, `push_z=0.01` and multi-finger+thumb geom-contact grasp detection.
114. Pick-and-lift success logic now uses geom-based grasp acquisition (`grasp_acquired`) plus strict no-drop lift hold (`no_drop_during_hold` over full hold window); hold-time contact continuity is logged (`hold_contact_ok`) but not required for final success.
115. Pick-and-lift hand default is now `thumb_o_wrap` (open -> wide thumb-opposition preshape -> close) to encourage thumb+three-finger circular wrap and keep thumb contact into lift.
116. Pick-and-lift close/lift arm target applies `close_z_clearance=0.015m` above push target to reduce fingertip-table snag during finger flexion.
117. Pick-and-lift control loop now steps MuJoCo with `control_nstep` derived from `control_hz` (`nstep=5` at 100Hz with dt=0.002), fixing previous under-settled IK behavior.
118. Verified tuned run on 2026-03-05: `dataset/franka_pickandlift_object6d_low2e_thumb_20260305` reached `saved_success_episodes=3` in `attempts=3` with stable approach (`reach_err≈0.0028m`, `rot_err≈0.31deg`) and no-drop hold.
119. Large-scale collection completed on 2026-03-05: `dataset/franka_pickandlift_object6d_low2e_thumb_200_fast5hz_20260305` contains 200 successful episodes (merged `raw/episode_00000..00199.npz`) generated via 4 parallel shards (`50x4`) at `capture_hz=5`, `control_hz=100`.
120. Pick-and-lift hand-only low-dim interface uses PCA synergy basis from the 16 hand joints sliced out of 23D actions (`arm7 + hand16`): `dataset/pickandlift_synergy_basis/pickandlift_hand_pca_k4.npz` (`k=4`).
121. Converted hand-synergy dataset artifacts are `dataset/franka_pickandlift_hand_synergy_k4_200_fast5hz_20260305/raw` and OXE export `dataset/franka_pickandlift_hand_synergy_k4_oxe_20260305_v2` (`mustard_grasp_oxe`, `state_dim=60`, `action_dim=4`, 200 episodes).
122. Overfit training artifact for pick-and-lift hand-synergy-k4 is `models/pickandlift_hand_octo_overfit_k4_20260305/run_260305_184714` (checkpoints `1000..5000`, `final_model`).
123. Added `scripts/eval/rollout_pickandlift_hand_octo.py` for deployment-style evaluation (`arm IK + hand VLA`), decoding k-D latent with basis `mu,B`; supports headless recording and `--policy-repeat` for faster CPU inference loops.
124. Current pick-and-lift hand-k4 rollout reference outputs (2026-03-05): demo video `codex/logs/pickandlift_hand4_rollout_demo_r8_20260305.mp4`, summaries `codex/logs/pickandlift_hand4_eval_demo_r8_20260305.json` and `codex/logs/pickandlift_hand4_eval_3ep_r8_20260305.json` (3-episode success 0%, reached+grasped but lift-hold not satisfied).
125. Pick-and-lift hand-policy rollout must render policy observations with the same fixed camera used during collection (`set_default_franka_allegro_camera`); using MuJoCo's default free camera causes severe image-distribution shift and early fist-closing behavior.
126. Camera-shift validation (2026-03-06): with the wrong free camera, the first-step hand prediction decoded to near-close (`||q_hand||≈4.53`) and rollout showed immediate fist-closing; with the corrected collection camera, the same initial observation predicted near-open (`||q_hand||≈0.29`, close to GT `0.263`). Reference assets: `codex/logs/pickandlift_hand4_rollout_demo_r8_fixedcam_20260306.mp4` and frame comparisons `codex/logs/pickandlift_hand4_rollout_demo_r8_20260305_frame0.png`, `codex/logs/pickandlift_hand4_rollout_demo_r8_fixedcam_20260306_frame0.png`.
127. Thumb trajectory comparison on 2026-03-06 showed `thumb_o_wrap` improves scripted grasp quality over `thumb_opposition`: `best_contacts 7 -> 9`, `object_dz_max 0.1732 -> 0.1882`, and thumb contact persists into lift instead of ending during close. Reference reports: `codex/logs/json/pickandlift_thumb_trajectory_comparison_20260306.json`.
128. Validation recollection for the new thumb trajectory is `dataset/franka_pickandlift_object6d_low2e_owrap_20_fast5hz_20260306` (20/20 success, `capture_hz=5`, `action_dim=23`, `state_dim=60`, fixed scene; intentionally small because the trajectory is deterministic and used for rapid overfit diagnosis).
129. The corresponding hand-synergy artifacts are `dataset/pickandlift_synergy_basis_owrap20_20260306/pickandlift_hand_pca_k4.npz`, raw latent dataset `dataset/franka_pickandlift_hand_synergy_k4_owrap20_20260306`, OXE export `dataset/franka_pickandlift_hand_synergy_k4_owrap20_oxe_20260306`, and overfit model `models/pickandlift_hand_octo_overfit_k4_owrap20_20260306/run_260306_122827`.
130. The above `thumb_o_wrap` hand-k4 model fits training frames well under teacher forcing (`mae≈0.0208`, `rmse≈0.0711` on `episode_00000`), and the rollout initial observation exactly matches the training first frame (`image_l1_mean=0`, `state_l1_mean=0`). Reference diagnostics: `codex/logs/pickandlift_hand4_owrap20_trainframe_fit_20260306.json` and `codex/logs/pickandlift_hand4_owrap20_rollout_obs_compare_20260306.json`.
131. `octoketi` uses CUDA JAX (`backend=gpu`, devices `cuda:0,1`) for Octo training, but `handvla` currently uses CPU-only JAX (`backend=cpu`) because its `jaxlib` is not CUDA-enabled; therefore simulation/data tools run in `handvla`, while Octo rollout/training speedups require either GPU-capable `handvla` or a consolidated environment.
132. A rollout bug found on 2026-03-06 in `scripts/eval/rollout_pickandlift_hand_octo.py` was resetting `q_hand_cmd` to `open_pose` on every settle-step. This caused immediate deviation from the expert trajectory (`first_state_divergence` at decision 1) and the first catastrophic action mismatch at decision 7 while still in `approach`. Reference diagnostic: `codex/logs/pickandlift_hand4_owrap20_rollout_divergence_20260306.json` and video `codex/logs/pickandlift_hand4_owrap20_rollout_divergence_20260306.mp4`.
133. After fixing the settle-phase reset bug, the same `thumb_o_wrap` hand-k4 model recovered thumb-inclusive grasp in rollout (`best_contacts=12`, `best_fingers=ff,mf,rf,th`, `grasp=1`) but still failed the strict lift criterion (`object_dz_max≈0.0667 < 0.08`, `lift=0`). Reference summary: `codex/logs/pickandlift_hand4_owrap20_eval_1ep_postfix_20260306.json`.
134. Updated interpretation: the previous “all-open/no-contact” failure was primarily a rollout implementation bug, not pure policy incapacity. The remaining issue is a weaker closed-loop lift/hold behavior after grasp, so next experiments should target temporal/phase conditioning or lift-focused corrective data rather than re-litigating the basic hand latent representation.
135. `handvla` was upgraded on 2026-03-06 from CPU-only JAX to CUDA JAX by installing `jaxlib 0.4.20+cuda11.cudnn86` plus the matching NVIDIA CUDA/cuDNN user-space packages (`nvidia-cublas/cu11`, `cudnn-cu11`, `cusolver/cu11`, `cusparse/cu11`, `nccl/cu11`, etc.); `jax.default_backend()` now reports `gpu` with devices `cuda:0,1`.
136. Patched non-record rollout reference for the `thumb_o_wrap` hand-k4 model is `codex/logs/pickandlift_hand4_owrap20_eval_3ep_postfix_20260306.json` with 3 episodes, 66.7% success, and all episodes achieving thumb-inclusive grasp (`ff,mf,rf,th`); the remaining failures are strict hold/no-drop lift failures rather than grasp failures.
137. Qualitative 1080p demo recording is `codex/logs/pickandlift_hand4_owrap20_rollout_demo_postfix_20260306.mp4`; paired summary `codex/logs/pickandlift_hand4_owrap20_eval_3ep_record_postfix_20260306.json` should be treated as a recording-time qualitative run because its strict no-drop outcome differed from the non-record reference despite similar grasp/lift-height behavior.
138. `scripts/eval/rollout_pickandlift_hand_octo.py` now supports `--start-episode`, which skips earlier episodes while preserving RNG progression; use this when a later rollout episode needs to be reproduced or isolated without changing the stochastic sequence.
139. Presentation-oriented hand-VLA artifact index is `codex/logs/pickandlift_hand4_presentation_assets_20260306.md`, with the best-attempt clip at `codex/logs/pickandlift_hand4_owrap20_best_attempt_clip_20260306.mp4` and the full qualitative demo at `codex/logs/pickandlift_hand4_owrap20_rollout_demo_postfix_20260306.mp4`.
140. Pick-and-lift hand-k4 oscillation diagnosis on 2026-03-06 showed two layers of failure: (a) deployment semantics mismatch worsens oscillation when `policy_repeat` does not match the dataset's 5Hz capture rate (`repeat=20` at `control_hz=100`) or when extra smoothing is applied to absolute hand commands; (b) even with matched semantics (`repeat=20`, `action_smoothing=1.0`), the single-frame policy can jump from `approach` to `lift-like` latent around decision 7 under small closed-loop state drift. Reference: `codex/logs/json/pickandlift_hand_oscillation_diagnosis_20260306.json`.
141. Ground-truth teacher-forcing check for the same overfit model stays on the correct early-phase branch for the first 24 decisions (`settle/approach`, action MAE ~0.002-0.007), so the oscillation is not caused by corrupted labels or an inability to fit the demonstration frames themselves. Reference: `codex/logs/tmp_gt_phase_check.json`.
142. Detailed early-rollout drift analysis (`r20_a1.0`) showed arm state and palm pose remain almost exact (`mean_arm_q_mae_gt≈1.8e-4`, `mean_palm_pos_l2_gt≈4.2e-5`) while the model becomes highly sensitive to modest hand-state/velocity drift near the phase boundary (`decision 7`: `hand_q_mae_gt≈0.06`, `hand_dq_mae_gt≈0.082`, then `pred_phase=lift`). This supports the interpretation that the core remaining issue is phase aliasing / lack of temporal context in single-frame absolute-action BC, not arm IK error. Reference: `codex/logs/json/pickandlift_hand_oscillation_r20a1_detail_20260306.json`.
143. `scripts/eval/rollout_pickandlift_hand_octo.py` defaults were updated on 2026-03-06 to match current pick-and-lift dataset semantics: `--policy-repeat 20` and `--action-smoothing 1.0` (absolute command replay at 5Hz over 100Hz control). Deviating from these values should be treated as an ablation, not the baseline.
144. Sanity check after the default rollout update succeeded on 2026-03-06: `codex/logs/pickandlift_hand4_default_baseline_20260306.json` shows 1/1 success with `reach=1`, `grasp=1`, `lift=1`, `best_contacts=12`, `best_fingers=ff,mf,rf,th`, and `object_dz_max≈0.1713`.
145. `scripts/train/finetune_mustard_octo.py` now supports `window_size > 1` using Octo-style observation history chunking: each training sample contains `[window_size]` image/proprio history with `timestep_pad_mask`, and matching `[window_size, action_horizon, action_dim]` action chunks plus `action_pad_mask`.
146. `scripts/eval/rollout_pickandlift_hand_octo.py` now auto-detects the loaded model's `window_size` from `example_batch` and maintains a matching policy history buffer at inference, padding the beginning of each episode with repeated first observations and a correct `timestep_pad_mask`.
147. Octo reference note for this repo: `window_size` means observation history length, not future lookahead. Local Octo sources show pretraining config/eval examples using history (`octo_pretrain_config.py` uses `window_size=2`; `examples/04_eval_finetuned_on_robot.py` wraps the env with `HistoryWrapper(window_size=2)`).
148. First pick-and-lift hand-k4 history-conditioned training artifact is `models/pickandlift_hand_octo_overfit_k4_owrap20_w2_20260306/run_260306_145247/checkpoint_001000` (trained with `window_size=2`). A 2-step smoke run succeeded structurally at `codex/models/pickandlift_hand_octo_window2_smoke/run_260306_145111`.
149. Initial `window_size=2` evaluation at checkpoint 1000 did not yet improve closed-loop behavior: `codex/logs/pickandlift_hand4_owrap20_w2_ckpt1000_eval3_20260306.json` shows 0/3 success and 0 grasp, indicating the history pipeline is implemented correctly but this first checkpoint is still underfit / insufficiently trained.
150. Continuing `window_size=2` training from the initial checkpoint is supported by reusing `--pretrained-path` with a local checkpoint directory; a resume smoke run succeeded at `codex/models/pickandlift_hand_octo_window2_resume_smoke/run_260306_150951`.
151. Main continuation run for history-conditioned overfitting is `models/pickandlift_hand_octo_overfit_k4_owrap20_w2_continue_20260306/run_260306_151102`, trained for 4000 additional steps from the `window_size=2` checkpoint-1000 seed model.
152. The continuation run loss trace (`models/pickandlift_hand_octo_overfit_k4_owrap20_w2_continue_20260306/run_260306_151102/loss_trace.json`) reached minimum loss `~0.0453` at step `3419`; checkpoint losses were roughly step1000 `0.217`, step2000 `0.285`, step3000 `0.155`, step4000/final `0.970`, so the final saved model is not the best deployment checkpoint.
153. `window_size=2` continuation improved closed-loop performance substantially: `codex/logs/json/pickandlift_hand4_owrap20_w2_continue_ckpt1000_eval3_20260306.json` reached 33.3% success, and `codex/logs/json/pickandlift_hand4_owrap20_w2_continue_ckpt3000_eval3_20260306.json` reached 100% success (3/3) with thumb-inclusive grasp (`ff,mf,rf,th`).
154. The continuation `final_model` at step 4000 regressed in rollout despite still grasping: `codex/logs/json/pickandlift_hand4_owrap20_w2_continue_final_eval3_20260306.json` shows 0/3 success with `grasp=1` but `lift=0`, so for current history-conditioned deployment use `checkpoint_003000`, not `final_model`.
155. For convenience, the best-performing history-conditioned checkpoint was copied to `models/pickandlift_hand_octo_overfit_k4_owrap20_w2_continue_20260306/run_260306_151102/best_model`, with metadata in `best_model_info.json`; this alias currently points to `checkpoint_003000`.
156. Recorded 1080p deployment demo for the current best history-conditioned model at `codex/logs/videos/pickandlift_hand4_owrap20_w2_best_rollout_demo_20260306.mp4`, with matching summary `codex/logs/json/pickandlift_hand4_owrap20_w2_best_rollout_demo_20260306.json`; this run achieved 1/1 success with thumb-inclusive grasp and `object_dz_max≈0.1808`.
157. On 2026-03-06 `codex/logs` was pruned and reorganized into `codex/logs/json`, `codex/logs/images`, and `codex/logs/videos`; root-level log files and ad-hoc frame folders were deleted to keep only representative artifacts.
158. Current curated log keep-set is minimal: videos `codex/logs/videos/{full_joint_diverse_rollout_demo.mp4,tcp12_cmd_next_palm_local_rollout_demo_20260303.mp4,synergy_k4_selected_rollout_demo_20260304.mp4,finger_ik_260219_164246.mp4,franka_pregrasp_axes_mapping_20260305.mp4,pickandlift_hand4_owrap20_w2_best_rollout_demo_20260306.mp4}`, images `codex/logs/images/{franka_pregrasp_axes_mapping_20260305.png,pregrasp_thumb_opposition_055_20260306_crop.png,pregrasp_thumb_owrap_055_20260306_crop.png}`, and representative JSON summaries under `codex/logs/json/`.
159. Historical notes above may mention root-level `codex/logs/...` paths from before the prune; when in doubt, use the curated subfolder versions and prefer the latest `window_size=2` pick-and-lift artifacts.
160. End-to-end pick-and-lift arm+hand VLA now uses `11D = arm7 absolute joints + hand_synergy_k4 latent`, converted from the existing `23D = arm7 + hand16` raw demonstrations via `scripts/research/convert_pickandlift_raw_to_arm_hand_synergy_raw.py`.
161. The current arm+hand low-dimensional raw dataset is `dataset/franka_pickandlift_arm_hand_synergy_k4_owrap20_20260306` and it overwrites `language_instruction` to the short command `grasp the mustard bottle` for Octo fine-tuning.
162. Even for custom pick-and-lift exports, the TFDS builder name remains `mustard_grasp_oxe`; use the custom dataset path via `--data-dir`, but keep `--dataset-name mustard_grasp_oxe` unless the converter itself is renamed.
163. End-to-end arm+hand Octo fine-tuning uses `scripts/train/finetune_pickandlift_arm_hand_octo.py`, which defaults to `window_size=2`, validation split, validation-based early stopping, and `best_model`-only saving (`best_model_info.json`, `eval_trace.json`, `loss_trace.json`, `train_summary.json`).
164. End-to-end deployment-style evaluation uses `scripts/eval/rollout_pickandlift_arm_hand_octo.py`; unlike the hand-only rollout, this script does not use arm IK and instead applies policy outputs directly as Franka joint targets plus decoded hand joint targets.
165. The first verified arm+hand smoke artifacts on 2026-03-06 are: OXE export `dataset/franka_pickandlift_arm_hand_synergy_k4_owrap20_oxe_20260306`, smoke train run `models/pickandlift_arm_hand_octo_overfit_k4_owrap20_20260306_smoke/run_260306_162517`, and smoke rollout summary `codex/logs/json/pickandlift_arm_hand_octo_smoke_20260306.json`.
166. Smoke training for the above arm+hand pipeline completed successfully with `train_episodes=18`, `val_episodes=2`, `action_dim=11`, `window_size=2`, and best validation loss improving to `5.3569` by step 20; rollout runtime is structurally valid, but this first short run does not yet succeed in grasp/lift.
167. The first non-smoke end-to-end arm+hand overfit run is `models/pickandlift_arm_hand_octo_overfit_k4_owrap20_20260306/run_260306_163327`; during training the best validation checkpoint reached `step=2050`, `val_loss≈0.4119`, and was snapshotted to `best_model_snapshot_step2050` for evaluation.
168. Current end-to-end arm+hand rollout result with that snapshot is `codex/logs/json/pickandlift_arm_hand_octo_eval3_step2050_20260306.json`: 0/3 success, 0 reach, 0 grasp, 0 lift, and 0 object motion. This is much worse than the hand-only `arm IK + hand VLA` baseline and should be treated as the current failure case, not a deployable model.
169. Teacher-forcing diagnostic for the same step-2050 arm+hand snapshot is `codex/logs/json/pickandlift_arm_hand_teacher_forcing_step2050_20260306.json`; on the first training frame it predicts the 11D action fairly well (`mae≈0.0275`, `arm_mae≈0.0199`, `hand_mae≈0.0408`), so the current failure is not “cannot fit the first frame” but a stronger closed-loop rollout collapse when arm and hand are both policy-controlled.
170. Current interpretation for the end-to-end arm+hand 11D experiment: history-conditioned BC can fit single-step supervision on the fixed scene, but direct closed-loop control of both Franka arm and Allegro hand is significantly less stable than the hand-only setup. Next debugging should focus on rollout drift/phase collapse rather than further raw action-dim changes.
171. Representative 1080p failure video for the above end-to-end arm+hand snapshot is `codex/logs/videos/pickandlift_arm_hand_octo_fail_demo_step2050_20260306.mp4`, with paired summary `codex/logs/json/pickandlift_arm_hand_octo_fail_demo_step2050_20260306.json`; use this directly against the hand-only success video for presentation/comparison.
172. Oracle replay diagnostic on 2026-03-06 proved that the rollout/control code path itself is correct for end-to-end arm+hand: both raw `joint23` replay and converted `arm11` replay succeed on the same episode (`codex/logs/json/pickandlift_arm_hand_oracle_replay_20260306.json`), with `approach_min_err≈0.00265m`, thumb-inclusive contact, and `object_dz_max≈0.186m`.
173. The live rollout initial observation for the end-to-end arm+hand model exactly matches the training first frame (`image_l1_mean=0`, `state_l1_mean=0`), so the failure is not caused by camera mismatch or an initial-state construction bug.
174. One-step sensitivity diagnostic (`codex/logs/json/pickandlift_arm_hand_one_step_sensitivity_20260306.json`) shows why the end-to-end arm+hand rollout still collapses: the model's first predicted action is close to GT (`action_mae≈0.0275`), but because the arm command is a 5Hz absolute target held for 20 control steps, that small action error already amplifies into a much larger next-state drift (`next palm position error ≈ 2.43cm` vs `0.39cm` for GT replay). This indicates the main bottleneck is transition sensitivity / compounding error under low-rate absolute arm commands, not a broken control-application path.
175. Important correction on 2026-03-06: the current end-to-end arm+hand experiment used `arm_joint7` as the policy action interface, but this is a design mistake relative to the intended Octo/OXE-style setup. The next primary arm-hand interface should use arm TCP / end-effector pose action (derived from `arm_cmd_pose_wxyz` and `arm_obs_pose_wxyz`) plus hand low-dimensional action, with IK remaining as the low-level arm controller.
176. The existing `arm_joint7 + hand_synergy4` pipeline should be retained only as a failed baseline / negative result. The main follow-up path is to rebuild the end-to-end dataset and rollout around arm TCP action semantics instead of direct Franka joint targets.
177. Corrected end-to-end arm+hand interface on 2026-03-06 is `10D = arm_tcp6_absolute_world_xyz_rpy + hand_synergy_k4`, built from raw `arm_cmd_pose_wxyz` plus hand PCA latent; this is now the main arm-hand VLA path.
178. Corrected arm-hand conversion/training/rollout scripts are `scripts/research/convert_pickandlift_raw_to_arm_tcp_hand_synergy_raw.py`, `scripts/train/finetune_pickandlift_arm_tcp_hand_octo.py`, and `scripts/eval/rollout_pickandlift_arm_tcp_hand_octo.py`.
179. Corrected arm-hand rollout semantics: policy arm output is an absolute TCP target `[x,y,z,roll,pitch,yaw]` in world frame, held for `policy_repeat=20` control steps, and tracked through `_step_arm_ik`; the policy never writes Franka arm joints directly in the corrected path.
180. Oracle replay gate for the corrected interface passed on 2026-03-06: `codex/diagnose_pickandlift_arm_tcp_hand_oracle_replay.py` produced success for both `joint23` and corrected `tcp10` replay on the same episode, with reference log `codex/logs/json/pickandlift_arm_tcp_hand_oracle_replay_20260306.json`.
181. Corrected raw dataset artifact is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_object6d_low2e_owrap_20_fast5hz_20260306`, and corrected OXE export is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_object6d_low2e_owrap_20_fast5hz_oxe_20260306`.
182. Corrected arm-hand smoke train artifact is `codex/models/pickandlift_arm_tcp_hand_octo_smoke_20260306/run_260306_210338`; it validates `action_dim=10`, `window_size=2`, early-stop best-model saving, and `mustard_grasp_oxe` TFDS loading for the corrected interface.
183. Corrected arm-hand smoke rollout artifact is `codex/logs/json/pickandlift_arm_tcp_hand_octo_smoke_eval_20260306.json`; runtime is structurally valid in `handvla`, but this 2-step smoke model is not a performance result and should be treated only as pipeline verification.
184. First corrected arm-TCP+hand overfit run is `models/pickandlift_arm_tcp_hand_octo_overfit_k4_20260306/run_260306_211545`; the best checkpoint reached `step=3100` with `val_loss≈0.2564`, recorded in `best_model_info.json`.
185. That corrected run was manually stopped after the best checkpoint plateaued for several minutes; the stop reason is stored in `models/pickandlift_arm_tcp_hand_octo_overfit_k4_20260306/run_260306_211545/run_status.json`.
186. Corrected arm-TCP+hand 3-episode rollout at `step=3100` is `codex/logs/json/pickandlift_arm_tcp_hand_octo_eval3_step3100_20260306.json`: `0/3 success`, but `reach=1` on all episodes with `approach_min_err≈3.34mm` and `approach_min_rot_err≈0.68deg`; the remaining failure is `grasp=0`, not arm approach.
187. Representative corrected arm-TCP+hand failure clip is `codex/logs/videos/pickandlift_arm_tcp_hand_octo_step3100_fail_demo_20260306.mp4`, with paired summary `codex/logs/json/pickandlift_arm_tcp_hand_octo_step3100_fail_demo_20260306.json`; use it to show that the corrected interface fixes arm approach semantics but the end-to-end hand branch still fails to close/grasp.
188. Corrected arm-TCP+hand teacher-forcing diagnostic at `step=3100` is `codex/logs/json/pickandlift_arm_tcp_hand_fit_step3100_20260306.json`; on ground-truth observations the model reproduces both close and lift hand commands well (`hand_lat_mae_all≈0.0104`, `hand_q_mae_all≈0.00735`), including close/lift thumb and non-thumb flexion. Therefore the corrected hand failure is not a label or decoding problem.
189. Corrected arm-TCP+hand rollout diagnostic at `step=3100` is `codex/logs/json/pickandlift_arm_tcp_hand_rollout_diag_step3100_20260306.json`; the first large state divergence appears at decision `t=6` (approach start), but the first strong hand-action divergence appears later at `t=27`, still during approach. After that, rollout stays in a thumb-opposition / preshape-like branch.
190. In corrected rollout, the hand failure mode is specifically `thumb-only preshape`: during `close/lift/hold`, thumb joints stay partially opposed (`thumb_pred` roughly `[1.27, 0.07, 0.30, 0.11]`), while non-thumb flexion remains near zero (`non_thumb_flex_pred≈0`) instead of the demonstrated close/lift values (`~0.68` in close and `~1.35` in lift/hold). This matches the observed behavior that the thumb opens toward the object but the other fingers do not curl in.
191. The current k=4 hand PCA basis cleanly separates thumb-dominant and non-thumb-flexion modes: basis columns PC1/PC2 are thumb-heavy, while PC0/PC3 carry most ff/mf/rf flexion weights. The corrected rollout behavior suggests the policy is activating thumb-dominant modes but failing to switch on the flexion-dominant modes under closed-loop drift.
192. Current non-rule-based recovery strategy for corrected `tcp10` is to add a separate corrective dataset, not to replace the full dataset. The corrective dataset should start from noisy `late-approach`, `preshape`, or `early-close` anchor states and preserve the same raw observation/action semantics as `collect_pickandlift_rlds.py`.
193. Corrective recollection is implemented in `scripts/data/collect_pickandlift_corrective_rlds.py`. It replays the scripted expert to an anchor, perturbs Franka arm joints, Allegro hand joints, and mustard pose, then records only the successful recovery segment through grasp/lift.
194. Corrective raw episodes remain schema-compatible with the existing pick-and-lift converters (`images/state/action/phase/contact/arm_cmd_pose_wxyz/arm_obs_pose_wxyz/arm_pose_error`) and add only extra metadata keys: `corrective_anchor_phase`, `corrective_anchor_step`, `corrective_anchor_ratio`, and `corrective_noise_json`.
195. Recommended training curriculum for the corrected arm-TCP+hand path is: full dataset first, then additional fine-tuning on the corrective anchor dataset. Do not collapse these into a single “all starts near object” dataset by default.
196. First corrective shard collected on 2026-03-08 is `dataset/franka_pickandlift_corrective_anchor_owrap20_20260308` with 20 successful episodes saved in 29 attempts (`capture_hz=5`, `640x480`, anchors sampled from `approach,preshape,close`).
197. The corresponding corrected arm-TCP+hand raw-latent export is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_corrective_anchor_owrap20_20260308`, created with the existing pick-and-lift k=4 basis `dataset/pickandlift_synergy_basis_owrap20_20260306/pickandlift_hand_pca_k4.npz`.
198. The corresponding OXE export for corrective fine-tuning is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_corrective_anchor_owrap20_oxe_20260308` (`mustard_grasp_oxe`, `action_dim=10`, 20 episodes).
199. Corrective-only fine-tuning from the corrected base best model was tested on 2026-03-08 with run `models/pickandlift_arm_tcp_hand_octo_corrective_ft_k4_20260308/run_260308_134909`. It early-stopped at step `1250`, with best validation at step `250` (`val_loss≈2.6492`), but rollout regressed severely: `codex/logs/json/pickandlift_arm_tcp_hand_octo_corrective_ft_eval3_20260308.json` shows `0/3 success`, `reach=0`, `grasp=0`, `lift=0`, `approach_min_err≈0.120m`, `approach_min_rot_err≈27.6deg`. Interpretation: corrective-only causes catastrophic forgetting of the initial reach stage.
200. A mixed latent raw union dataset was created on 2026-03-08 at `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_mixed_full20_corrective20_20260308` by combining the original full 20-episode corrected dataset with the 20-episode corrective shard; the corresponding OXE export is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_mixed_full20_corrective20_oxe_20260308`.
201. Mixed full+corrective fine-tuning from the corrected base best model was tested in `models/pickandlift_arm_tcp_hand_octo_mixed_ft_k4_20260308/run_260308_140533`. During training the best validation snapshot reached at least step `1750` with `val_loss≈0.3641`, and the mixed curriculum preserved arm reach in rollout (`approach_min_err≈1.55mm`, `rot_err≈0.23deg`) unlike corrective-only.
202. However, mixed curriculum still did not solve the hand-closure bottleneck. Snapshot evaluations `codex/logs/json/pickandlift_arm_tcp_hand_octo_mixed_ft_live_eval3_20260308.json` and `codex/logs/json/pickandlift_arm_tcp_hand_octo_mixed_ft_live_eval1_step1750_20260308.json` show `reach=1` but `grasp=0`, `lift=0`, `best_contacts=0`, with `mean_hand_latent_norm≈1.49-1.51`; the policy remains on the early/open branch instead of switching to the close branch.
203. A later `window_size=4` mixed run (`models/pickandlift_arm_tcp_hand_octo_mixed_w4_b4_20260308/run_260308_160821`) exposed severe model-selection mismatch: an early live snapshot around step `300` achieved `reach=1, grasp=1` on a 1-episode check (`codex/logs/json/pickandlift_arm_tcp_hand_octo_mixed_w4_b4_eval1_step300_20260308.json`), but the eventual validation-best snapshot at step `1000` collapsed to `0/3` with `reach=0, grasp=0, lift=0` (`codex/logs/json/pickandlift_arm_tcp_hand_octo_mixed_w4_b4_eval3_step1000_20260308.json`). For this end-to-end path, validation loss alone is not a reliable deployment selector.
204. To debug that mismatch, a checkpoint sweep run was added at `models/pickandlift_arm_tcp_hand_octo_mixed_w4_sweep_20260308/run_260308_162453` with checkpoints every 50 steps. Rollout checks on steps `250/300/500` (`codex/logs/json/pickandlift_arm_tcp_hand_octo_mixed_w4_sweep_eval1_step250_20260308.json`, `...step300...`, `...step500...`) all failed with `reach=0`, confirming high optimization variance and reinforcing the need for rollout-gated checkpoint selection.
205. Local Octo/OXE references in `/home/minchan/Downloads/dual_arm_VLA/octo` assume delta-style manipulation actions in several utilities, but for the current scripted Franka pick-and-lift dataset the arm absolute-TCP replay remains the only oracle-validated arm interface. Naive arm delta compression must not replace the current absolute arm interface unless it passes oracle replay first.
206. `scripts/research/convert_pickandlift_raw_to_arm_tcp_hand_synergy_raw.py` and `scripts/eval/rollout_pickandlift_arm_tcp_hand_octo.py` now support `--arm-action-type {absolute_world,delta_local}`. The `delta_local` path is experimental and currently not validated for this dataset.
207. A delta-local mixed dataset was built at `dataset/franka_pickandlift_arm_tcp_delta_local_hand_synergy_k4_mixed_full20_corrective20_20260308` with OXE export `dataset/franka_pickandlift_arm_tcp_delta_local_hand_synergy_k4_mixed_full20_corrective20_oxe_20260308`, but oracle replay on `codex/diagnose_pickandlift_arm_tcp_hand_oracle_replay.py` still failed for both current-step and next-step local deltas (`codex/logs/json/pickandlift_arm_tcp_hand_oracle_replay_delta_local_20260308.json`, `codex/logs/json/pickandlift_arm_tcp_hand_oracle_replay_delta_local_next_20260308.json`). Interpretation: the current 5Hz pick-and-lift export does not yet admit a faithful compressed arm-delta replay under the present hold/replay semantics.
208. A phase-conditioned state path is now supported through `--append-phase-features` in both `scripts/research/convert_pickandlift_raw_to_arm_tcp_hand_synergy_raw.py` and `scripts/eval/rollout_pickandlift_arm_tcp_hand_octo.py`. It appends 8 dims to the proprio state (`6`-way phase one-hot + phase progress + global progress), raising pick-and-lift state dim from `60` to `68`.
209. The phase-conditioned absolute-arm mixed dataset path is `dataset/franka_pickandlift_arm_tcp_abs_phase_hand_synergy_k4_mixed_full20_corrective20_20260308`, with OXE export `dataset/franka_pickandlift_arm_tcp_abs_phase_hand_synergy_k4_mixed_full20_corrective20_oxe_20260308`.
210. Phase-conditioned end-to-end training on 2026-03-08 did not restore rollout stability in current tests. Both the unfreeze-all run `models/pickandlift_arm_tcp_hand_octo_abs_phase_w4_20260308/run_260308_165438` (checked at steps `200` and `350`) and the default-frozen run `models/pickandlift_arm_tcp_hand_octo_abs_phase_w4_frozen_20260308/run_260308_170629` (checked at steps `50` and `200`) produced `0/1` rollout with `reach=0, grasp=0, lift=0` under `--append-phase-features`. Phase cues alone are therefore insufficient to solve the current end-to-end instability.
211. Local Octo reference check on 2026-03-08 confirmed that original Octo validation uses the same held-out action-head loss as training (`model.heads["action"].loss(...)`) after the same batch preprocessing, but stock `octo/scripts/finetune.py` only logs that validation loss and separate rollout metrics; it does not early-stop or select deployment checkpoints by validation loss alone.
212. For this repo's end-to-end arm+hand experiments, validation loss must be treated as optimization telemetry rather than a deployment selector. A concrete mismatch already appeared in `models/pickandlift_arm_tcp_hand_octo_mixed_w4_b4_20260308/run_260308_160821`: the live snapshot around step `300` reached and grasped, while the lower-validation-loss snapshot at step `1000` collapsed to `reach=0`.
213. The strong regression in `models/pickandlift_arm_tcp_hand_octo_abs_phase_w4_frozen_20260308/run_260308_170629` is characterized by near-zero arm motion rather than a distinct new grasp failure: compared with the earlier mixed `window_size=4` live snapshot (`mean_arm_delta≈0.066`, `reach=1`, `grasp=1`), the frozen phase run outputs tiny arm changes (`mean_arm_delta≈0.0045` at step `50`, `≈0.0012` at step `200`) and stays near the initial pose (`approach_min_err≈0.47m`, `approach_min_rot_err≈57deg`).
214. The most plausible cause of that regression is the combination of (a) a new proprio input shape (`60 -> 68` via `--append-phase-features`), (b) initializing from a non-phase checkpoint, and (c) limited adaptation in the default-frozen run (`--unfreeze-all` off, only `200` steps). Under that setup the model can reduce average held-out action loss by predicting conservative near-mean actions without preserving closed-loop reachability.
215. `scripts/eval/sweep_pickandlift_arm_tcp_hand_checkpoints.py` was added on 2026-03-08 to automate rollout-gated checkpoint selection for end-to-end arm+hand runs. It evaluates saved checkpoints with the existing rollout script and ranks them by success/lift/grasp/reach instead of validation loss.
216. A successful end-to-end 10D deployment was found on 2026-03-08 by continuing from the partial-success snapshot `models/pickandlift_arm_tcp_hand_octo_mixed_w4_b4_20260308/run_260308_160821/best_model_snapshot_live_step300`. The continuation run is `models/pickandlift_arm_tcp_hand_octo_continue_from_step300_20260308/run_260308_180929`, and checkpoint `checkpoint_000250` is the current best rollout model.
217. Important deployment note: the above successful 10D end-to-end checkpoint requires `--policy-repeat 10` at rollout. With the dataset-matched default `policy-repeat 20`, the same checkpoint reaches and grasps but repeatedly fails the final lift-hold/no-drop condition despite exceeding lift height (`object_dz_max≈0.0868`). With `--policy-repeat 10`, the same checkpoint achieves stable lift success.
218. Verified successful artifacts for the current best end-to-end 10D model are `codex/logs/json/pickandlift_arm_tcp_hand_octo_continue_step250_repeat10_eval1_20260308.json` (`1/1 success`), `codex/logs/json/pickandlift_arm_tcp_hand_octo_continue_step250_repeat10_eval3_20260308.json` (`3/3 success`), and `codex/logs/videos/pickandlift_arm_tcp_hand_octo_continue_step250_repeat10_demo_20260308.mp4`.
219. A lift-focused corrective dataset was collected at `dataset/franka_pickandlift_corrective_liftclose_owrap20_20260308` with close-only anchors (`close-anchor-range 0.60..0.95`) to emphasize post-grasp lift recovery. The corresponding latent export is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_corrective_liftclose_owrap20_20260308`, and the mixed OXE dataset is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_mixed_full20_lift40_oxe_20260308`.
220. That lift-focused continuation run `models/pickandlift_arm_tcp_hand_octo_continue_step250_liftmix_20260308/run_260308_184107` did not outperform the successful `checkpoint_000250` deployment path in the first rollout checks; early checkpoints `50/100/150/200` all failed to reach/grasp, and checkpoint `250` only recovered reach without restoring stable lift.
221. Repository cleanup decision on 2026-03-09: keep all `.md` documents, but prune `dataset/` and `models/` down to the final successful 10D end-to-end artifacts only. The kept dataset artifacts are `dataset/pickandlift_synergy_basis_owrap20_20260306`, `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_mixed_full20_corrective20_20260308`, and `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_mixed_full20_corrective20_oxe_20260308`. The kept model artifact is `models/pickandlift_arm_tcp_hand_octo_continue_from_step300_20260308`.
