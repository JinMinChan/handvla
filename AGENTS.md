# AGENTS.md

This file stores durable collaboration rules for this repository.
When user-assistant decisions affect workflow or project scope, update this file.
Keep this rule itself in the file.

## Working Rules

1. Use `conda` environment `handvla` as the default runtime for install and checks.
2. Do not recreate `.venv` unless explicitly requested.
3. Put temporary experiment code, debug scripts, and logs in `codex/`.
4. Before final upload/push, clean up temporary files in `codex/` and keep only what is intentionally versioned.
5. Keep this file updated continuously with important decisions from user-assistant conversation.
6. Record this policy itself as durable context whenever AGENTS.md is updated.
7. Keep a repo-local `handoff/` folder for day-by-day continuity notes.
8. From 2026-03-23 onward, maintain a daily markdown note named `handoffMMDD.md` under `handoff/` (example: `handoff0323.md`) and keep appending the day's progress, current state, key artifacts, and next actions there for later prompts.
9. Record this handoff-note policy itself as durable context whenever AGENTS.md is updated.

## Current Project Baseline (2026-02-19)

1. Repository goal is to build from previous `keti_dual_arm_VLA` assets into a clean `handvla` project.
2. First runnable target is MuJoCo Allegro hand-only simulation.
3. `run_allegro_hand.py` launches the hand model with right control panel sliders.
4. Hand-only scene should include visible background/floor/lighting by default.
5. Default initial pose should be wrist-lower, hand-up orientation.
6. Background/lighting style should stay aligned with `keti_dual_arm_VLA` base scene unless user asks otherwise.
7. TCP markers should be defined near each fingertip pad, shifted slightly palm-ward for grasp experiments.
8. Viewer should start with a close frontal hand camera, not a distant wide shot.
9. Finger IK feasibility should be tested with per-finger random reachable targets and reported as success rate/error statistics.
10. Temporary logs stay under `codex/logs/`, but core experiment scripts should live in project root or stable modules.
11. `run_allegro_hand.py` should support 1920x1080 recording to `codex/logs/` by default when requested.
12. `finger_ik_experiment.py` should show viewer by default unless `--no-viewer` is passed.
13. IK experiment viewer should use the same default camera as `run_allegro_hand.py` and show target TCP markers clearly.
14. Default visible update speed for viewer-driven experiment scripts should be around 60Hz (`--viewer-step`), with optional manual delay override.
15. Finger IK quick runs should default to 3 trials per finger.
16. Only active finger target marker should be visible during IK trials to avoid visual confusion.
17. `finger_ik_experiment.py` should support 1920x1080 recording options similar to `run_allegro_hand.py`.
18. IK feasibility code and mustard grasp/data-collection code must stay in separate scripts.
19. `finger_ik_experiment.py` is IK-only (no mustard/object mode).
20. Mustard grasp pretraining scope is mustard-only (`006_mustard_bottle`) in `collect_mustard_grasp.py`.
21. Mustard fixed-air spawn pose uses palm-relative offset `[0.10, 0.00, 0.02]` with horizontal+clockwise quaternion `[0.5, 0.5, 0.5, -0.5]`.
22. Mustard object fixation uses freejoint pose clamp (kinematic lock) each simulation step.
23. Grasp success labeling uses geom contact between Allegro fingertip/distal geoms and mustard geoms with thumb-contact requirement plus contact/finger/force/stability thresholds.
24. Mustard grasp closing should use a thumb-opposition pre-shape phase (`open -> pre-shape -> close`) so the thumb rotates inward before full flexion.
25. Dataset collection for training should use `collect_mustard_grasp_dataset.py` and save only successful episodes until target count is reached.
26. Default mustard dataset logging rates are control 100Hz and capture 20Hz (via `capture_every=5`).
27. Mustard raw dataset path is `dataset/mustard_grasp/raw/episode_*.npz` with summary at `dataset/mustard_grasp/collection_summary.json`.
28. OXE conversion should use `convert_mustard_raw_to_oxe.py` to export RLDS/TFDS from raw episodes.
29. Octo fine-tuning should use `finetune_mustard_octo.py` with `mustard_grasp_oxe` (`image_primary` + `state` -> 12-DoF TCP action head: 4 fingertips x xyz).
30. Octo training runtime currently requires `conda` env `octoketi` (while `handvla` remains default for simulation/data tools).
31. Fine-tuning outputs should be stored under `models/mustard_octo_overfit/run_*` with checkpoints, `final_model`, and loss trace.
32. `finetune_mustard_octo.py` should use direct TFDS episode loading (not `make_single_dataset`) for compatibility with current `octoketi` TFDS stack.
33. Current Octo fine-tune script supports `window_size=1` (single-frame policy training).
34. Octo loss compatibility requires `action` and `action_pad_mask` batch tensors shaped as `[B, window(1), action_horizon, action_dim]`.
35. Finetuned model rollout validation should use `rollout_mustard_octo.py` in the same fixed mustard scene with contact-based success metrics.
36. Default stable rollout settings for current overfit model are `max_policy_steps=100`, `control_repeat=5`, `action_smoothing=0.35`.
37. `rollout_mustard_octo.py` should run in `conda` env `handvla` after Octo-compatible dependencies are installed.
38. `rollout_mustard_octo.py` auto-resolves Octo source path from `/home/minchan/Downloads/dual_arm_VLA/octo`, or from project-root `.octo_src_path` override.
39. `collect_mustard_grasp_dataset.py` default `--action-interface` is `tcp12`; existing legacy datasets may still contain `joint16`.
40. `rollout_mustard_octo.py` default `--action-interface` is `tcp12`; use `--action-interface joint16` only for legacy 16-DoF models.
41. Legacy 16-DoF artifacts should use `full_joint` naming (`dataset/mustard_grasp_full_joint`, `dataset/mustard_grasp_oxe_full_joint`, `models/mustard_octo_overfit_full_joint`).
42. Keep presentation-ready comparison videos under `codex/logs/` with explicit interface in filename (e.g., `full_joint_*.mp4`).
43. Rollout recording should keep the default frontal hand camera even when running with `--no-viewer` (headless).
44. Use `rollout_mustard_octo_tcp12.py` for tcp12-specific rollout debugging (step-level `tcp_delta`/`q_delta` logging).
45. TCP12 dataset semantics are command next-delta: `action_t = FK(q_cmd_{t+1}) - FK(q_cmd_t)` (final step repeats previous delta); default frame is `palm_local` (legacy option: `world`).
46. TCP12 rollout interpretation default is delta mode (`tcp_target = tcp_now + pred_delta_world`), where `palm_local` predictions are rotated to world using current palm orientation; absolute mode remains legacy compatibility only.
47. Oracle replay diagnostic (2026-02-26): `joint16` replay reproduces grasp success (100/100), while `tcp12` (`delta` and `absolute`) replay gives 0/100 under thumb-required success criteria.
48. Under current geom-contact success rules, `tcp12` position-only replay consistently misses thumb contact (`best_fingers` tends to `ff,mf,rf`), indicating interface loss (thumb posture/opposition) rather than pure model-learning failure.
49. Validation note: if thumb-contact requirement is disabled, oracle replay success for `tcp12` becomes 100/100; therefore future 12-DoF interface work must encode thumb opposition intent/posture (or revise success/target definition) before fair comparison with full-joint control.
50. Policy-direction diagnostic (2026-02-26): current tcp12-delta model outputs for `ff/mf/rf` are weaker than demonstration scale and often point away from object center, while thumb output dominates; this matches observed thumb-only contact in rollout videos.
51. Keep `codex/logs/tcp12_direction_diagnostics_20260226.json` as reference when discussing why tcp12 VLA behavior differs from per-finger IK feasibility tests.
52. Next primary low-dimensional interface direction is `joint-synergy kD` (joint-space compression) rather than naive `tcp12 xyz-only`; detailed execution plan is tracked in `docs/joint-synergy-kD.md`.
53. Maintain a separate presentation-oriented experiment narrative document at `docs/experiment-flow-for-presentation.md` so slide preparation can follow the verified experimental timeline and conclusions.
54. Joint-synergy tooling is implemented with `build_joint_synergy_basis.py`, `convert_full_joint_raw_to_synergy_raw.py`, and `rollout_mustard_octo_synergy.py`.
55. Current full_joint mustard dataset is effectively low-rank; PCA basis selection run (k=4,6,8,10) produced oracle success 100% for all tested k and selected `k=4` as the smallest valid default (`dataset/synergy_basis/full_joint_pca_k4.npz`).
56. Current synergy experiment artifacts: raw `dataset/mustard_grasp_synergy_k4`, OXE `dataset/mustard_grasp_oxe_synergy_k4`, model `models/mustard_octo_overfit_synergy_k4/run_260227_115921`, eval `codex/logs/synergy_k4_eval_summary_20260227.json`.
57. Initial synergy rollout check with the above `k=4` model achieved multi-finger + thumb contact and 100% success on a 3-episode smoke evaluation.
58. `collect_mustard_grasp_dataset.py` now supports optional diversity controls (spawn jitter, yaw jitter, pose noise, phase-step jitter) and should keep default-zero behavior for legacy deterministic collection.
59. Diverse benchmark dataset uses `dataset/mustard_grasp_full_joint_diverse` with diversity config: xy 0.02m, z 0.01m, yaw 20deg, preshape noise 0.05rad, close noise 0.08rad, step jitter (4,5,15,8), seed 42.
60. Diverse synergy basis run (`dataset/synergy_basis_diverse/full_joint_pca_summary.json`) selected `k=4` with cumulative variance ~0.992 and oracle success 1.0.
61. Diverse fair-training artifacts: full-joint model `models/mustard_octo_overfit_full_joint_diverse/run_260228_011500`, synergy model `models/mustard_octo_overfit_synergy_k4_diverse/run_260228_012422`.
62. Diverse 10-episode rollout evaluation reached 100% success for both interfaces; synergy-k4 showed higher best-contact-finger count (4.0 mean) than full-joint baseline (3.0 mean) in current fixed-scene benchmark.
63. Presentation-ready diverse comparison outputs are `codex/logs/json/diverse_joint_synergy_comparison_20260228.json`, `codex/logs/diverse_joint_synergy_comparison_20260228.md`, plus videos `codex/logs/videos/full_joint_diverse_rollout_demo.mp4` and `codex/logs/synergy_k4_diverse_rollout_demo.mp4`.
64. Viewer simulation loops should not use `mj_step` 1-per-frame for interactive scripts; use batched stepping via `nstep ~= (1/viewer_step)/timestep` (default viewer-step 60Hz) to avoid slow/laggy perceived control.
65. `scripts/sim/run_franka_allegro_mustard.py` keeps right-side UI off by default for performance; enable only with `--show-right-ui`.
64. `rollout_mustard_octo.py` supports `--show-fk-tcp-markers` to visualize FK TCP world positions as blue mocap spheres for full-joint-to-TCP extraction validation and recording.
65. `collect_mustard_grasp_dataset.py` stores auxiliary command traces in raw episodes: `action_joint16_cmd`, `action_tcp12_cmd_world_abs`, `action_tcp12_cmd_world_next_delta`, and `action_tcp12_cmd_palm_local_next_delta` for replay/debug parity checks.
66. Current tcp12 interface-fix stage keeps action dimension at 12 (no extra intent dims yet); focus is command semantics/frame consistency first.
67. Historical command-based tcp12 artifacts (obs->cmd delta stage, 2026-03-03): raw `dataset/mustard_grasp_tcp12_cmd`, OXE `dataset/mustard_grasp_oxe_tcp12_cmd`, model `models/mustard_octo_overfit_tcp12_cmd/run_260303_153616`.
68. TCP12 semantics comparison eval (2026-03-03, 10 episodes each): legacy-world and command-palm_local (obs->cmd delta stage) both scored 0% success under thumb-required rule; command model improved contact occurrence but remained thumb-only. See `codex/logs/tcp12_legacy_world_eval_summary_20260303.json`, `codex/logs/tcp12_cmd_palm_local_eval_summary_20260303.json`, and `codex/logs/json/tcp12_semantics_comparison_20260303.json` with demo videos `codex/logs/tcp12_legacy_world_rollout_demo_20260303.mp4`, `codex/logs/tcp12_cmd_palm_local_rollout_demo_20260303.mp4`.
69. Current cmd-next tcp12 artifacts (2026-03-03): raw `dataset/mustard_grasp_tcp12_cmd_next`, OXE `dataset/mustard_grasp_oxe_tcp12_cmd_next`, model `models/mustard_octo_overfit_tcp12_cmd_next/run_260303_171027`.
70. Cmd-next tcp12 10-episode rollout (2026-03-03, thumb-required) with `models/mustard_octo_overfit_tcp12_cmd_next/run_260303_171027/final_model` still produced 0% success and 0 contact-fingers across episodes; step logs show early motion followed by near-zero updates (`q_delta -> 0`). Demo video: `codex/logs/videos/tcp12_cmd_next_palm_local_rollout_demo_20260303.mp4`.
71. Expanded diverse synergy basis sweep (2026-03-04) tested `k=2,3,4,6` at `dataset/synergy_basis_diverse_k236/full_joint_pca_summary.json`; all ks achieved oracle replay success 1.0, and `k=2` already retained high variance (~0.9899 cumulative).
72. Diverse k-ablation artifacts (2026-03-04): raw `dataset/mustard_grasp_synergy_k{2,3,4,6}_diverse_k236`, OXE `dataset/mustard_grasp_oxe_synergy_k{2,3,4,6}_diverse_k236`, models `models/mustard_octo_overfit_synergy_k{2,3,4,6}_diverse_k236/run_260304_*`, eval summaries `codex/logs/synergy_k{2,3,4,6}_diverse_k236_eval_20260304.json`.
73. Diverse 5000-step rollout eval for `k=2,3,4,6` (10 episodes each, thumb-required) all reached 100% success with 4 contact fingers; comparison table is tracked in `codex/logs/synergy_k_ablation_20260304.{json,md}`.
74. User-selected follow-up baseline (2026-03-04) is `k=4`; dedicated retrain/eval artifacts are `models/mustard_octo_overfit_synergy_k4_selected/run_260304_140028`, `codex/logs/synergy_k4_selected_eval_20260304.json`, and `codex/logs/synergy_k4_selected_summary_20260304.{json,md}`.
75. Paper direction is fixed to `action-interface co-design` for unified arm-hand VLA (not just end-to-end demonstration); ICCAS draft execution plan is tracked in `docs/iccas-action-interface-plan.md`.
76. Before upload, keep only intentionally versioned outputs and prioritize pruning heavy experiment byproducts under `dataset/`, `models/`, and temporary `codex/tmp_*` directories.
77. A repeatable pre-push cleanup script is available at `tools/cleanup_before_push.sh`; default keep set preserves only current core comparison artifacts (`full_joint_diverse` vs `synergy_k4_selected`) and synergy basis files.
78. `.gitignore` now ignores `dataset/` and `models/` by default to prevent accidental large artifact uploads; keep release assets separately if explicitly needed.
79. Log/media pruning policy before upload: keep one representative rollout video per algorithm family in `codex/logs/` (`full_joint`, `tcp12`, `synergy-k4`, plus optional IK feasibility), and remove redundant benchmark/debug clips.
80. Canonical executable entrypoints are now organized under `scripts/` by role: `scripts/sim`, `scripts/data`, `scripts/train`, `scripts/eval`, `scripts/research`.
81. Research/design documents are organized under `docs/` (`docs/iccas-action-interface-plan.md`, `docs/joint-synergy-kD.md`, `docs/experiment-flow-for-presentation.md`).
82. Log/dataset/model cleanup helpers are organized under `tools/` (`tools/cleanup_before_push.sh`, `tools/prune_logs_representative.sh`) and should be run from repository root (scripts auto-resolve root).
83. A single-arm tabletop scene is added for next-stage experiments: `scripts/sim/run_franka_allegro_mustard.py` using `env/franka_allegro_mjcf.py` (`Franka Panda nohand + Allegro + mustard`).
84. Franka assets are vendored under `env/assets/franka_emika_panda` (source: MuJoCo Menagerie), and mustard object remains `env/assets/ycb/006_mustard_bottle`.
85. Franka tabletop scene uses dedicated `env/assets/franka_scene.xml` (floor at z=0, reduced shadow quality, timestep 0.002) to avoid floating appearance and improve viewer responsiveness.
86. Executable scripts now self-bootstrap repository root path, so commands in README run without `PYTHONPATH=.`.
87. Franka tabletop placement is anchored on the table: Franka base frame at `[0.30, -0.10, 0.74]` and mustard spawn at `[0.62, 0.06, 0.82]` in `env/franka_allegro_mjcf.py`.
88. Shadows are disabled for performance in both hand/franka scenes (`castshadow=false`, minimal `shadowsize`), including Franka's internal top light in `panda_nohand.xml`.
89. `scripts/sim/run_franka_allegro_mustard.py` supports arm IK mode via `--arm-mode ik-approach`, driving Franka 7-DoF toward `mustard + offset` while keeping Allegro hand posture fixed.
90. Franka pick-and-lift dataset collection uses `scripts/data/collect_pickandlift_rlds.py` with phase policy `settle -> approach(IK) -> pre-shape -> close -> lift`.
91. Pick-and-lift success is defined jointly as arm reach (`approach_min_err <= threshold`), geom-contact grasp stability (fingertip/distal vs mustard with thumb requirement by default), and object lift (`z` increase >= `lift_success_delta`) with stable-step checks.
92. Verified baseline run on 2026-03-04: `dataset/franka_pickandlift` reached 3/3 saved success episodes in 3 attempts using headless low-capture settings (`capture_hz=2`, `320x240`).
93. Pick-and-lift mustard spawn default is upright (`spawn_quat=[1,0,0,0]`) at `spawn_pos=[0.62, 0.06, 0.82]` in `collect_pickandlift_rlds.py`.
94. To prevent pre-grasp collapse of upright bottle, `collect_pickandlift_rlds.py` defaults to `--lock-object-until-close` (pose clamp during settle/approach/preshape only).
95. Current upright pick-and-lift default arm targets are `approach_offset=[-0.08, 0.00, 0.06]` and `push_offset=[-0.04, -0.03, 0.03]`.
96. Lift success is intentionally stricter: default `lift_height=0.20`, `lift_success_delta=0.08`, and required hold `lift_hold_seconds=1.5` (continuous contact+lift condition during hold).
97. Upright strict-criteria smoke checks on 2026-03-05 succeeded (`dataset/franka_pickandlift_tmp_uprightcheck3`: 3/3 in 3 attempts).
98. Pick-and-lift arm offsets should be interpreted in object frame by default (`--arm-offset-frame object`) to stay aligned with object axes and reduce unnatural approach direction.
99. Pick-and-lift hand closing defaults to `--hand-trajectory keti_human` with `preshape_grasp_ratio=0.45` for smoother, human-like wrap grasp.
100. Pick-and-lift success defaults now enforce multi-finger quality (`min_contact_fingers=3` + thumb required), preventing two-finger transient successes.
101. Verified updated human-like defaults on 2026-03-05: `dataset/franka_pickandlift` reached 3/3 with best fingers consistently `ff,mf,rf,th` and `object_dz_max≈0.177m`.
102. Added dedicated pre-grasp validation script `scripts/sim/run_franka_pregrasp_ik.py` using Franka 6D IK (position+orientation) with object-frame target pose and fixed hand pre-shape for "grasp-just-before" pose checks.
103. Verified 6D pre-grasp capture on 2026-03-05 with default target offset `[-0.078,-0.015,0.065]` in object frame: result `reached=True`, `pos_err≈0.0048m`, `rot_err≈0.58deg`, outputs `codex/logs/images/franka_pregrasp_axes_mapping_20260305.png` and `codex/logs/videos/franka_pregrasp_axes_mapping_20260305.mp4`.
104. `scripts/data/collect_pickandlift_rlds.py` now uses object-6D-aligned arm targets by default (position offsets + quaternion offsets in object frame) and 6D IK (position+orientation) instead of position-only IK.
105. Pick-and-lift raw episodes now include arm pose supervision traces: `arm_cmd_pose_wxyz`, `arm_obs_pose_wxyz`, and `arm_pose_error` (`[pos_m, rot_deg]`) for command-vs-observation alignment validation.
106. Current object-6D pick-and-lift defaults are tuned for stable collection: `approach_steps=700`, `preshape_steps=150`, `close_steps=280`, `ik_rot_weight=0.20`, `arm_approach_offset=[-0.09,-0.015,0.04]`, `arm_push_offset=[-0.078,-0.015,0.01]`.
107. For the above 6D target configuration, `collect_pickandlift_rlds.py` default hand trajectory is `thumb_o_wrap` (wider thumb opposition with delayed curl); `thumb_opposition`, `power_linear`, and `keti_human` remain optional via `--hand-trajectory`.
108. Verified object-6D default collection on 2026-03-05: `dataset/franka_pickandlift_object6d_run0305` reached `saved_success_episodes=3` in `attempts=3` with thumb-required multi-finger contact (`ff,mf,rf,th`) and strict lift-hold criteria.
109. For alignment-only validation (no grasp/lift), use `scripts/sim/run_franka_pregrasp_ik.py`; this script should stop at precise arm-hand pre-grasp alignment against object pose.
110. `env/franka_allegro_mjcf.py` now supports `add_frame_axes` and `run_franka_pregrasp_ik.py` enables it by default (`--show-axes`) to visualize XYZ frames on both palm and mustard for capture-based verification.
111. Default palm-vs-object axis mapping for pre-grasp alignment is now set by object-frame quaternion `[0.70710678, 0.70710678, 0, 0]` (hand red≈object red, hand green≈object blue, and hand blue≈-object green for right-handed consistency).
112. Pre-grasp alignment default uses fully open hand (`hand_preshape_ratio=0.0`) so approach is executed with the hand spread before grasp phases.
113. Verified lowered-hand pick-and-lift defaults on 2026-03-05: `dataset/franka_pickandlift_object6d_low2b_20260305` reached `saved_success_episodes=3` in `attempts=3` with offsets `approach_z=0.04`, `push_z=0.01` and multi-finger+thumb geom-contact grasp detection.
114. Pick-and-lift success logic now uses geom-based grasp acquisition (`grasp_acquired`) plus strict no-drop lift hold (`no_drop_during_hold` over full hold window); hold-time contact continuity is logged (`hold_contact_ok`) but not required for final success.
115. Pick-and-lift hand default is now `thumb_o_wrap` (open -> wide thumb-opposition preshape -> close) to encourage thumb+three-finger circular wrap and keep thumb contact into lift.
116. Pick-and-lift close/lift arm target applies `close_z_clearance=0.015m` above push target to reduce fingertip-table snag during finger flexion.
117. Pick-and-lift control loop now steps MuJoCo with `control_nstep` derived from `control_hz` (`nstep=5` at 100Hz with dt=0.002), fixing previous under-settled IK behavior.
118. Verified tuned run on 2026-03-05: `dataset/franka_pickandlift_object6d_low2e_thumb_20260305` reached `saved_success_episodes=3` in `attempts=3` with stable approach (`reach_err≈0.0028m`, `rot_err≈0.31deg`) and no-drop hold.
119. Large-scale collection completed on 2026-03-05: `dataset/franka_pickandlift_object6d_low2e_thumb_200_fast5hz_20260305` contains 200 successful episodes (merged `raw/episode_00000..00199.npz`) generated via 4 parallel shards (`50x4`) at `capture_hz=5`, `control_hz=100`.
120. Pick-and-lift hand-only low-dim interface uses PCA synergy basis from the 16 hand joints sliced out of 23D actions (`arm7 + hand16`): `dataset/pickandlift_synergy_basis/pickandlift_hand_pca_k4.npz` (`k=4`).
121. Converted hand-synergy dataset artifacts are `dataset/franka_pickandlift_hand_synergy_k4_200_fast5hz_20260305/raw` and OXE export `dataset/franka_pickandlift_hand_synergy_k4_oxe_20260305_v2` (`mustard_grasp_oxe`, `state_dim=60`, `action_dim=4`, 200 episodes).
122. Overfit training artifact for pick-and-lift hand-synergy-k4 is `models/pickandlift_hand_octo_overfit_k4_20260305/run_260305_184714` (checkpoints `1000..5000`, `final_model`).
123. Added `scripts/eval/rollout_pickandlift_hand_octo.py` for deployment-style evaluation (`arm IK + hand VLA`), decoding k-D latent with basis `mu,B`; supports headless recording and `--policy-repeat` for faster CPU inference loops.
124. Current pick-and-lift hand-k4 rollout reference outputs (2026-03-05): demo video `codex/logs/pickandlift_hand4_rollout_demo_r8_20260305.mp4`, summaries `codex/logs/pickandlift_hand4_eval_demo_r8_20260305.json` and `codex/logs/pickandlift_hand4_eval_3ep_r8_20260305.json` (3-episode success 0%, reached+grasped but lift-hold not satisfied).
125. Pick-and-lift hand-policy rollout must render policy observations with the same fixed camera used during collection (`set_default_franka_allegro_camera`); using MuJoCo's default free camera causes severe image-distribution shift and early fist-closing behavior.
126. Camera-shift validation (2026-03-06): with the wrong free camera, the first-step hand prediction decoded to near-close (`||q_hand||≈4.53`) and rollout showed immediate fist-closing; with the corrected collection camera, the same initial observation predicted near-open (`||q_hand||≈0.29`, close to GT `0.263`). Reference assets: `codex/logs/pickandlift_hand4_rollout_demo_r8_fixedcam_20260306.mp4` and frame comparisons `codex/logs/pickandlift_hand4_rollout_demo_r8_20260305_frame0.png`, `codex/logs/pickandlift_hand4_rollout_demo_r8_fixedcam_20260306_frame0.png`.
127. Thumb trajectory comparison on 2026-03-06 showed `thumb_o_wrap` improves scripted grasp quality over `thumb_opposition`: `best_contacts 7 -> 9`, `object_dz_max 0.1732 -> 0.1882`, and thumb contact persists into lift instead of ending during close. Reference reports: `codex/logs/json/pickandlift_thumb_trajectory_comparison_20260306.json`.
128. Validation recollection for the new thumb trajectory is `dataset/franka_pickandlift_object6d_low2e_owrap_20_fast5hz_20260306` (20/20 success, `capture_hz=5`, `action_dim=23`, `state_dim=60`, fixed scene; intentionally small because the trajectory is deterministic and used for rapid overfit diagnosis).
129. The corresponding hand-synergy artifacts are `dataset/pickandlift_synergy_basis_owrap20_20260306/pickandlift_hand_pca_k4.npz`, raw latent dataset `dataset/franka_pickandlift_hand_synergy_k4_owrap20_20260306`, OXE export `dataset/franka_pickandlift_hand_synergy_k4_owrap20_oxe_20260306`, and overfit model `models/pickandlift_hand_octo_overfit_k4_owrap20_20260306/run_260306_122827`.
130. The above `thumb_o_wrap` hand-k4 model fits training frames well under teacher forcing (`mae≈0.0208`, `rmse≈0.0711` on `episode_00000`), and the rollout initial observation exactly matches the training first frame (`image_l1_mean=0`, `state_l1_mean=0`). Reference diagnostics: `codex/logs/pickandlift_hand4_owrap20_trainframe_fit_20260306.json` and `codex/logs/pickandlift_hand4_owrap20_rollout_obs_compare_20260306.json`.
131. `octoketi` uses CUDA JAX (`backend=gpu`, devices `cuda:0,1`) for Octo training, but `handvla` currently uses CPU-only JAX (`backend=cpu`) because its `jaxlib` is not CUDA-enabled; therefore simulation/data tools run in `handvla`, while Octo rollout/training speedups require either GPU-capable `handvla` or a consolidated environment.
132. A rollout bug found on 2026-03-06 in `scripts/eval/rollout_pickandlift_hand_octo.py` was resetting `q_hand_cmd` to `open_pose` on every settle-step. This caused immediate deviation from the expert trajectory (`first_state_divergence` at decision 1) and the first catastrophic action mismatch at decision 7 while still in `approach`. Reference diagnostic: `codex/logs/pickandlift_hand4_owrap20_rollout_divergence_20260306.json` and video `codex/logs/pickandlift_hand4_owrap20_rollout_divergence_20260306.mp4`.
133. After fixing the settle-phase reset bug, the same `thumb_o_wrap` hand-k4 model recovered thumb-inclusive grasp in rollout (`best_contacts=12`, `best_fingers=ff,mf,rf,th`, `grasp=1`) but still failed the strict lift criterion (`object_dz_max≈0.0667 < 0.08`, `lift=0`). Reference summary: `codex/logs/pickandlift_hand4_owrap20_eval_1ep_postfix_20260306.json`.
134. Updated interpretation: the previous “all-open/no-contact” failure was primarily a rollout implementation bug, not pure policy incapacity. The remaining issue is a weaker closed-loop lift/hold behavior after grasp, so next experiments should target temporal/phase conditioning or lift-focused corrective data rather than re-litigating the basic hand latent representation.
135. `handvla` was upgraded on 2026-03-06 from CPU-only JAX to CUDA JAX by installing `jaxlib 0.4.20+cuda11.cudnn86` plus the matching NVIDIA CUDA/cuDNN user-space packages (`nvidia-cublas/cu11`, `cudnn-cu11`, `cusolver/cu11`, `cusparse/cu11`, `nccl/cu11`, etc.); `jax.default_backend()` now reports `gpu` with devices `cuda:0,1`.
136. Patched non-record rollout reference for the `thumb_o_wrap` hand-k4 model is `codex/logs/pickandlift_hand4_owrap20_eval_3ep_postfix_20260306.json` with 3 episodes, 66.7% success, and all episodes achieving thumb-inclusive grasp (`ff,mf,rf,th`); the remaining failures are strict hold/no-drop lift failures rather than grasp failures.
137. Qualitative 1080p demo recording is `codex/logs/pickandlift_hand4_owrap20_rollout_demo_postfix_20260306.mp4`; paired summary `codex/logs/pickandlift_hand4_owrap20_eval_3ep_record_postfix_20260306.json` should be treated as a recording-time qualitative run because its strict no-drop outcome differed from the non-record reference despite similar grasp/lift-height behavior.
138. `scripts/eval/rollout_pickandlift_hand_octo.py` now supports `--start-episode`, which skips earlier episodes while preserving RNG progression; use this when a later rollout episode needs to be reproduced or isolated without changing the stochastic sequence.
139. Presentation-oriented hand-VLA artifact index is `codex/logs/pickandlift_hand4_presentation_assets_20260306.md`, with the best-attempt clip at `codex/logs/pickandlift_hand4_owrap20_best_attempt_clip_20260306.mp4` and the full qualitative demo at `codex/logs/pickandlift_hand4_owrap20_rollout_demo_postfix_20260306.mp4`.
140. Pick-and-lift hand-k4 oscillation diagnosis on 2026-03-06 showed two layers of failure: (a) deployment semantics mismatch worsens oscillation when `policy_repeat` does not match the dataset's 5Hz capture rate (`repeat=20` at `control_hz=100`) or when extra smoothing is applied to absolute hand commands; (b) even with matched semantics (`repeat=20`, `action_smoothing=1.0`), the single-frame policy can jump from `approach` to `lift-like` latent around decision 7 under small closed-loop state drift. Reference: `codex/logs/json/pickandlift_hand_oscillation_diagnosis_20260306.json`.
141. Ground-truth teacher-forcing check for the same overfit model stays on the correct early-phase branch for the first 24 decisions (`settle/approach`, action MAE ~0.002-0.007), so the oscillation is not caused by corrupted labels or an inability to fit the demonstration frames themselves. Reference: `codex/logs/tmp_gt_phase_check.json`.
142. Detailed early-rollout drift analysis (`r20_a1.0`) showed arm state and palm pose remain almost exact (`mean_arm_q_mae_gt≈1.8e-4`, `mean_palm_pos_l2_gt≈4.2e-5`) while the model becomes highly sensitive to modest hand-state/velocity drift near the phase boundary (`decision 7`: `hand_q_mae_gt≈0.06`, `hand_dq_mae_gt≈0.082`, then `pred_phase=lift`). This supports the interpretation that the core remaining issue is phase aliasing / lack of temporal context in single-frame absolute-action BC, not arm IK error. Reference: `codex/logs/json/pickandlift_hand_oscillation_r20a1_detail_20260306.json`.
143. `scripts/eval/rollout_pickandlift_hand_octo.py` defaults were updated on 2026-03-06 to match current pick-and-lift dataset semantics: `--policy-repeat 20` and `--action-smoothing 1.0` (absolute command replay at 5Hz over 100Hz control). Deviating from these values should be treated as an ablation, not the baseline.
144. Sanity check after the default rollout update succeeded on 2026-03-06: `codex/logs/pickandlift_hand4_default_baseline_20260306.json` shows 1/1 success with `reach=1`, `grasp=1`, `lift=1`, `best_contacts=12`, `best_fingers=ff,mf,rf,th`, and `object_dz_max≈0.1713`.
145. `scripts/train/finetune_mustard_octo.py` now supports `window_size > 1` using Octo-style observation history chunking: each training sample contains `[window_size]` image/proprio history with `timestep_pad_mask`, and matching `[window_size, action_horizon, action_dim]` action chunks plus `action_pad_mask`.
146. `scripts/eval/rollout_pickandlift_hand_octo.py` now auto-detects the loaded model's `window_size` from `example_batch` and maintains a matching policy history buffer at inference, padding the beginning of each episode with repeated first observations and a correct `timestep_pad_mask`.
147. Octo reference note for this repo: `window_size` means observation history length, not future lookahead. Local Octo sources show pretraining config/eval examples using history (`octo_pretrain_config.py` uses `window_size=2`; `examples/04_eval_finetuned_on_robot.py` wraps the env with `HistoryWrapper(window_size=2)`).
148. First pick-and-lift hand-k4 history-conditioned training artifact is `models/pickandlift_hand_octo_overfit_k4_owrap20_w2_20260306/run_260306_145247/checkpoint_001000` (trained with `window_size=2`). A 2-step smoke run succeeded structurally at `codex/models/pickandlift_hand_octo_window2_smoke/run_260306_145111`.
149. Initial `window_size=2` evaluation at checkpoint 1000 did not yet improve closed-loop behavior: `codex/logs/pickandlift_hand4_owrap20_w2_ckpt1000_eval3_20260306.json` shows 0/3 success and 0 grasp, indicating the history pipeline is implemented correctly but this first checkpoint is still underfit / insufficiently trained.
150. Added an interactive official-wrap rollout entrypoint at `scripts/eval/run_mustard_intent_octo_interactive.py` (2026-03-25). It keeps a live MuJoCo viewer open while running official-style Octo inference and supports terminal controls: `r` reset, `p` pause/resume, `s` single-step, `q` quit. Default paths target the current wrap500 `best_train_model` and shared k4 basis for immediate local debugging.
151. Follow-up fix for `scripts/eval/run_mustard_intent_octo_interactive.py` (2026-03-25): startup now performs an explicit warmup `sample_actions` call before the live loop so the initial JAX compile does not look like a dead viewer, and per-step logging now uses `base_env.get_episode_summary()` instead of the wrapped `info` object. Verified that the script can complete one full episode without crashing under `xvfb-run`.
150. Wrap official chunk-correction ablation on 2026-03-23 (`codex/eval_wrap_chunk_correction_ablation.py`, results `codex/logs/json/mustard_wrap_chunk_correction_ablation_w4_20260323.json`) showed that simple RTC-style shifted chunk blending (`beta=0.5/0.8`) did not improve official wrap rollout: all variants remained 0/3 success with 0 contacts, and stronger blending slightly worsened approach error. This indicates the current wrap failure is not a mere chunk-boundary discontinuity; the predicted late-phase chunks themselves remain on the open-hand branch.
151. A focused wrap close-trigger analysis was added on 2026-03-23 (`codex/analyze_wrap_close_trigger.py`, report `docs/wrap-close-trigger-report-20260323.md`, artifacts `codex/logs/json/wrap_close_trigger_analysis_w4_20260323.json` and `codex/logs/json/mustard_official_image_patches_w4_20260323.json`). It showed that teacher close onset begins at decision 20 (traj timestep 40) before contact exists, while rollout remains near the open-hand attractor through the same window. The object-relative drift is modest (~1.5–2.7 cm), but local hand-object patch drift grows exactly across the close-trigger window. This strengthens the interpretation that the official wrap failure is a late preshape-to-close phase-transition failure rather than a simple reach, exporter, or chunk-boundary problem.
150. Official mustard Octo training path (2026-03-19) now uses only official RLDS export plus `octo_data/mustard.py`; the previous legacy exporter path is no longer the baseline.
151. Official wrap-only randomized dataset baseline (2026-03-19) is `dataset/mustard_intent_wrap_shortsettle_disturb_randxy0p02_yaw10_20260319`, with `k=4` replay-clean artifacts `dataset/mustard_intent_wrap_shortsettle_disturb_randxy0p02_yaw10_k4_20260319` and official OXE export `dataset/mustard_intent_wrap_shortsettle_disturb_randxy0p02_yaw10_k4_20260319_oxe`.
152. For the above official Octo wrap baseline on the current 2-GPU machine, `batch_size=32` OOMs at the first train step while `batch_size=30` is the largest verified safe batch size; keep the default at 30 unless hardware or model shape changes.
153. JAX persistent compilation cache initialization was removed from `scripts/train/finetune_mustard_octo.py` on 2026-03-19 because it caused repeated startup failures (`Instruction name is not unique: constant_...`) during official Octo runs.
154. Official randomized-wrap patch diagnosis on 2026-03-20 (`codex/inspect_mustard_official_image_patches.py`, `codex/logs/json/mustard_official_image_patches_rand_20260320.json`) showed that the dominant teacher-vs-rollout image drift is a stable local patch around image coordinates `x≈120..216`, `y≈72..168` (moving upward after decision 29), i.e. the hand-object interaction region rather than global camera/background mismatch.
150. Official wrap-only Octo diagnosis on 2026-03-19 (`codex/diagnose_mustard_official_modality_gap.py`, outputs `codex/logs/json/mustard_official_modality_gap_20260319.json` and `_3dec_20260319.json`) showed the late close-phase failure is not caused by `goal image`, `language`, or `timestep/task_completed/pad_mask` mismatch: swapping teacher-vs-rollout phase fields leaves predictions unchanged, and `language_goal`, `language_only`, and `goal_only` all stay on the same open-hand branch under rollout observations.
151. The same 2026-03-19 modality-gap diagnosis showed rollout-side observation drift is the active cause of the branch collapse: replacing rollout observations with teacher image history partially recovers the close branch much more than replacing rollout proprio with teacher proprio, so current official rollout failure should be treated as primarily image-distribution drift with additional proprio drift, not a missing goal/task-conditioning feature.
152. Late-phase teacher-vs-rollout image inspection on 2026-03-19 (`codex/inspect_mustard_official_image_gap.py`, outputs `codex/logs/json/mustard_official_image_gap_20260319.json` and `codex/logs/images/mustard_official_image_gap_20260319/teacher_rollout_diff_contact_sheet.png`) showed the image gap is localized to the central-right hand/object region while corner/background difference is effectively zero; therefore the remaining official-rollout image drift is a local hand-object state divergence, not a global camera/crop mismatch.
153. Wrap randomization safety sweep on 2026-03-19 (`codex/jitter_sweep_wrap_20260319/results.tsv`) found scripted wrap collection remained stable through `spawn_jitter_xy=0.02m` and `spawn_yaw_jitter_deg=12`; for the main recollection we chose the slightly more conservative `0.02m / 10deg` window.
154. The resulting randomized wrap dataset is `dataset/mustard_intent_wrap_shortsettle_disturb_randxy0p02_yaw10_20260319` with `50/61` saved successes under short-settle (`40`) and corrective arm disturbance (`0.01, 0.01, 0.005` in `approach,preshape`).
155. The corresponding wrap-only k4 artifacts are `dataset/mustard_intent_wrap_shortsettle_disturb_randxy0p02_yaw10_k4_basis_20260319/mustard_intent_hand_pca_k4.npz`, `dataset/mustard_intent_wrap_shortsettle_disturb_randxy0p02_yaw10_k4_20260319`, and `dataset/mustard_intent_wrap_shortsettle_disturb_randxy0p02_yaw10_k4_20260319_oxe`; oracle replay for this randomized dataset remained `1.0` at `k=4`, so the added spawn diversity did not break replay semantics.
150. Official mustard wrap Octo baseline debugging on 2026-03-17 found a trainer bug in `scripts/train/finetune_mustard_octo.py`: pretrained Octo image tokenizers were filtered by tokenizer name (`primary`, `wrist`) instead of required obs/task keys (`image_primary`, `image_wrist`), so the first `models/mustard_octo_official_wrap_k4/run_260317_163740` run effectively trained a proprio+language-only model and should not be treated as a valid official image/goal-conditioned baseline.
151. Mustard official-wrap diagnostics to keep: `codex/logs/json/mustard_official_teacher_vs_rollout_20260317.json` and `codex/logs/json/mustard_official_teacher_vs_rollout_45_20260317.json`; they show teacher-forcing transitions the hand latent into the close phase, while rollout stays stuck in the early open-hand latent.
152. `scripts/train/finetune_mustard_octo.py` was upgraded on 2026-03-17 for throughput: it now uses JAX data-parallel sharding across all visible devices (`Mesh + NamedSharding`), prefetches batched TF data, and saves host-side checkpoints from replicated state. Current throughput-oriented defaults are `batch_size=32`, `shuffle_buffer_size=1024`, `eval_every=500`, and `rollout_every=1000`.
153. Official-wrap rollout debug on 2026-03-19 now injects Octo-style observation fields during inference (`timestep`, `task_completed`, and `observation.pad_mask_dict`) in `scripts/eval/mustard_intent_octo_official.py` and `scripts/eval/mustard_intent_gym_env.py`. This removed a structural gap with training batches but did not materially change the `models/mustard_octo_official_wrap_k4_fast/run_260317_174613` failure mode: best-rollout still reaches the pre-grasp pose yet stays in the early open-hand latent and never makes contact.
150. Continuing `window_size=2` training from the initial checkpoint is supported by reusing `--pretrained-path` with a local checkpoint directory; a resume smoke run succeeded at `codex/models/pickandlift_hand_octo_window2_resume_smoke/run_260306_150951`.
151. Main continuation run for history-conditioned overfitting is `models/pickandlift_hand_octo_overfit_k4_owrap20_w2_continue_20260306/run_260306_151102`, trained for 4000 additional steps from the `window_size=2` checkpoint-1000 seed model.
152. The continuation run loss trace (`models/pickandlift_hand_octo_overfit_k4_owrap20_w2_continue_20260306/run_260306_151102/loss_trace.json`) reached minimum loss `~0.0453` at step `3419`; checkpoint losses were roughly step1000 `0.217`, step2000 `0.285`, step3000 `0.155`, step4000/final `0.970`, so the final saved model is not the best deployment checkpoint.
153. `window_size=2` continuation improved closed-loop performance substantially: `codex/logs/json/pickandlift_hand4_owrap20_w2_continue_ckpt1000_eval3_20260306.json` reached 33.3% success, and `codex/logs/json/pickandlift_hand4_owrap20_w2_continue_ckpt3000_eval3_20260306.json` reached 100% success (3/3) with thumb-inclusive grasp (`ff,mf,rf,th`).
154. The continuation `final_model` at step 4000 regressed in rollout despite still grasping: `codex/logs/json/pickandlift_hand4_owrap20_w2_continue_final_eval3_20260306.json` shows 0/3 success with `grasp=1` but `lift=0`, so for current history-conditioned deployment use `checkpoint_003000`, not `final_model`.
155. For convenience, the best-performing history-conditioned checkpoint was copied to `models/pickandlift_hand_octo_overfit_k4_owrap20_w2_continue_20260306/run_260306_151102/best_model`, with metadata in `best_model_info.json`; this alias currently points to `checkpoint_003000`.
156. Recorded 1080p deployment demo for the current best history-conditioned model at `codex/logs/videos/pickandlift_hand4_owrap20_w2_best_rollout_demo_20260306.mp4`, with matching summary `codex/logs/json/pickandlift_hand4_owrap20_w2_best_rollout_demo_20260306.json`; this run achieved 1/1 success with thumb-inclusive grasp and `object_dz_max≈0.1808`.
157. On 2026-03-06 `codex/logs` was pruned and reorganized into `codex/logs/json`, `codex/logs/images`, and `codex/logs/videos`; root-level log files and ad-hoc frame folders were deleted to keep only representative artifacts.
158. Current curated log keep-set is minimal: videos `codex/logs/videos/{full_joint_diverse_rollout_demo.mp4,tcp12_cmd_next_palm_local_rollout_demo_20260303.mp4,synergy_k4_selected_rollout_demo_20260304.mp4,finger_ik_260219_164246.mp4,franka_pregrasp_axes_mapping_20260305.mp4,pickandlift_hand4_owrap20_w2_best_rollout_demo_20260306.mp4}`, images `codex/logs/images/{franka_pregrasp_axes_mapping_20260305.png,pregrasp_thumb_opposition_055_20260306_crop.png,pregrasp_thumb_owrap_055_20260306_crop.png}`, and representative JSON summaries under `codex/logs/json/`.
159. Historical notes above may mention root-level `codex/logs/...` paths from before the prune; when in doubt, use the curated subfolder versions and prefer the latest `window_size=2` pick-and-lift artifacts.
160. End-to-end pick-and-lift arm+hand VLA now uses `11D = arm7 absolute joints + hand_synergy_k4 latent`, converted from the existing `23D = arm7 + hand16` raw demonstrations via `scripts/research/convert_pickandlift_raw_to_arm_hand_synergy_raw.py`.
161. The current arm+hand low-dimensional raw dataset is `dataset/franka_pickandlift_arm_hand_synergy_k4_owrap20_20260306` and it overwrites `language_instruction` to the short command `grasp the mustard bottle` for Octo fine-tuning.
162. Even for custom pick-and-lift exports, the TFDS builder name remains `mustard_grasp_oxe`; use the custom dataset path via `--data-dir`, but keep `--dataset-name mustard_grasp_oxe` unless the converter itself is renamed.
163. End-to-end arm+hand Octo fine-tuning uses `scripts/train/finetune_pickandlift_arm_hand_octo.py`, which defaults to `window_size=2`, validation split, validation-based early stopping, and `best_model`-only saving (`best_model_info.json`, `eval_trace.json`, `loss_trace.json`, `train_summary.json`).
164. End-to-end deployment-style evaluation uses `scripts/eval/rollout_pickandlift_arm_hand_octo.py`; unlike the hand-only rollout, this script does not use arm IK and instead applies policy outputs directly as Franka joint targets plus decoded hand joint targets.
165. The first verified arm+hand smoke artifacts on 2026-03-06 are: OXE export `dataset/franka_pickandlift_arm_hand_synergy_k4_owrap20_oxe_20260306`, smoke train run `models/pickandlift_arm_hand_octo_overfit_k4_owrap20_20260306_smoke/run_260306_162517`, and smoke rollout summary `codex/logs/json/pickandlift_arm_hand_octo_smoke_20260306.json`.
166. Smoke training for the above arm+hand pipeline completed successfully with `train_episodes=18`, `val_episodes=2`, `action_dim=11`, `window_size=2`, and best validation loss improving to `5.3569` by step 20; rollout runtime is structurally valid, but this first short run does not yet succeed in grasp/lift.
167. The first non-smoke end-to-end arm+hand overfit run is `models/pickandlift_arm_hand_octo_overfit_k4_owrap20_20260306/run_260306_163327`; during training the best validation checkpoint reached `step=2050`, `val_loss≈0.4119`, and was snapshotted to `best_model_snapshot_step2050` for evaluation.
168. Current end-to-end arm+hand rollout result with that snapshot is `codex/logs/json/pickandlift_arm_hand_octo_eval3_step2050_20260306.json`: 0/3 success, 0 reach, 0 grasp, 0 lift, and 0 object motion. This is much worse than the hand-only `arm IK + hand VLA` baseline and should be treated as the current failure case, not a deployable model.
169. Teacher-forcing diagnostic for the same step-2050 arm+hand snapshot is `codex/logs/json/pickandlift_arm_hand_teacher_forcing_step2050_20260306.json`; on the first training frame it predicts the 11D action fairly well (`mae≈0.0275`, `arm_mae≈0.0199`, `hand_mae≈0.0408`), so the current failure is not “cannot fit the first frame” but a stronger closed-loop rollout collapse when arm and hand are both policy-controlled.
170. Current interpretation for the end-to-end arm+hand 11D experiment: history-conditioned BC can fit single-step supervision on the fixed scene, but direct closed-loop control of both Franka arm and Allegro hand is significantly less stable than the hand-only setup. Next debugging should focus on rollout drift/phase collapse rather than further raw action-dim changes.
171. Representative 1080p failure video for the above end-to-end arm+hand snapshot is `codex/logs/videos/pickandlift_arm_hand_octo_fail_demo_step2050_20260306.mp4`, with paired summary `codex/logs/json/pickandlift_arm_hand_octo_fail_demo_step2050_20260306.json`; use this directly against the hand-only success video for presentation/comparison.
172. Oracle replay diagnostic on 2026-03-06 proved that the rollout/control code path itself is correct for end-to-end arm+hand: both raw `joint23` replay and converted `arm11` replay succeed on the same episode (`codex/logs/json/pickandlift_arm_hand_oracle_replay_20260306.json`), with `approach_min_err≈0.00265m`, thumb-inclusive contact, and `object_dz_max≈0.186m`.
173. The live rollout initial observation for the end-to-end arm+hand model exactly matches the training first frame (`image_l1_mean=0`, `state_l1_mean=0`), so the failure is not caused by camera mismatch or an initial-state construction bug.
174. One-step sensitivity diagnostic (`codex/logs/json/pickandlift_arm_hand_one_step_sensitivity_20260306.json`) shows why the end-to-end arm+hand rollout still collapses: the model's first predicted action is close to GT (`action_mae≈0.0275`), but because the arm command is a 5Hz absolute target held for 20 control steps, that small action error already amplifies into a much larger next-state drift (`next palm position error ≈ 2.43cm` vs `0.39cm` for GT replay). This indicates the main bottleneck is transition sensitivity / compounding error under low-rate absolute arm commands, not a broken control-application path.
175. Important correction on 2026-03-06: the current end-to-end arm+hand experiment used `arm_joint7` as the policy action interface, but this is a design mistake relative to the intended Octo/OXE-style setup. The next primary arm-hand interface should use arm TCP / end-effector pose action (derived from `arm_cmd_pose_wxyz` and `arm_obs_pose_wxyz`) plus hand low-dimensional action, with IK remaining as the low-level arm controller.
176. The existing `arm_joint7 + hand_synergy4` pipeline should be retained only as a failed baseline / negative result. The main follow-up path is to rebuild the end-to-end dataset and rollout around arm TCP action semantics instead of direct Franka joint targets.
177. Corrected end-to-end arm+hand interface on 2026-03-06 is `10D = arm_tcp6_absolute_world_xyz_rpy + hand_synergy_k4`, built from raw `arm_cmd_pose_wxyz` plus hand PCA latent; this is now the main arm-hand VLA path.
178. Corrected arm-hand conversion/training/rollout scripts are `scripts/research/convert_pickandlift_raw_to_arm_tcp_hand_synergy_raw.py`, `scripts/train/finetune_pickandlift_arm_tcp_hand_octo.py`, and `scripts/eval/rollout_pickandlift_arm_tcp_hand_octo.py`.
179. Corrected arm-hand rollout semantics: policy arm output is an absolute TCP target `[x,y,z,roll,pitch,yaw]` in world frame, held for `policy_repeat=20` control steps, and tracked through `_step_arm_ik`; the policy never writes Franka arm joints directly in the corrected path.
180. Oracle replay gate for the corrected interface passed on 2026-03-06: `codex/diagnose_pickandlift_arm_tcp_hand_oracle_replay.py` produced success for both `joint23` and corrected `tcp10` replay on the same episode, with reference log `codex/logs/json/pickandlift_arm_tcp_hand_oracle_replay_20260306.json`.
181. Corrected raw dataset artifact is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_object6d_low2e_owrap_20_fast5hz_20260306`, and corrected OXE export is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_object6d_low2e_owrap_20_fast5hz_oxe_20260306`.
182. Corrected arm-hand smoke train artifact is `codex/models/pickandlift_arm_tcp_hand_octo_smoke_20260306/run_260306_210338`; it validates `action_dim=10`, `window_size=2`, early-stop best-model saving, and `mustard_grasp_oxe` TFDS loading for the corrected interface.
183. Corrected arm-hand smoke rollout artifact is `codex/logs/json/pickandlift_arm_tcp_hand_octo_smoke_eval_20260306.json`; runtime is structurally valid in `handvla`, but this 2-step smoke model is not a performance result and should be treated only as pipeline verification.
184. First corrected arm-TCP+hand overfit run is `models/pickandlift_arm_tcp_hand_octo_overfit_k4_20260306/run_260306_211545`; the best checkpoint reached `step=3100` with `val_loss≈0.2564`, recorded in `best_model_info.json`.
185. That corrected run was manually stopped after the best checkpoint plateaued for several minutes; the stop reason is stored in `models/pickandlift_arm_tcp_hand_octo_overfit_k4_20260306/run_260306_211545/run_status.json`.
186. Corrected arm-TCP+hand 3-episode rollout at `step=3100` is `codex/logs/json/pickandlift_arm_tcp_hand_octo_eval3_step3100_20260306.json`: `0/3 success`, but `reach=1` on all episodes with `approach_min_err≈3.34mm` and `approach_min_rot_err≈0.68deg`; the remaining failure is `grasp=0`, not arm approach.
187. Representative corrected arm-TCP+hand failure clip is `codex/logs/videos/pickandlift_arm_tcp_hand_octo_step3100_fail_demo_20260306.mp4`, with paired summary `codex/logs/json/pickandlift_arm_tcp_hand_octo_step3100_fail_demo_20260306.json`; use it to show that the corrected interface fixes arm approach semantics but the end-to-end hand branch still fails to close/grasp.
188. Corrected arm-TCP+hand teacher-forcing diagnostic at `step=3100` is `codex/logs/json/pickandlift_arm_tcp_hand_fit_step3100_20260306.json`; on ground-truth observations the model reproduces both close and lift hand commands well (`hand_lat_mae_all≈0.0104`, `hand_q_mae_all≈0.00735`), including close/lift thumb and non-thumb flexion. Therefore the corrected hand failure is not a label or decoding problem.
189. Corrected arm-TCP+hand rollout diagnostic at `step=3100` is `codex/logs/json/pickandlift_arm_tcp_hand_rollout_diag_step3100_20260306.json`; the first large state divergence appears at decision `t=6` (approach start), but the first strong hand-action divergence appears later at `t=27`, still during approach. After that, rollout stays in a thumb-opposition / preshape-like branch.
190. In corrected rollout, the hand failure mode is specifically `thumb-only preshape`: during `close/lift/hold`, thumb joints stay partially opposed (`thumb_pred` roughly `[1.27, 0.07, 0.30, 0.11]`), while non-thumb flexion remains near zero (`non_thumb_flex_pred≈0`) instead of the demonstrated close/lift values (`~0.68` in close and `~1.35` in lift/hold). This matches the observed behavior that the thumb opens toward the object but the other fingers do not curl in.
191. The current k=4 hand PCA basis cleanly separates thumb-dominant and non-thumb-flexion modes: basis columns PC1/PC2 are thumb-heavy, while PC0/PC3 carry most ff/mf/rf flexion weights. The corrected rollout behavior suggests the policy is activating thumb-dominant modes but failing to switch on the flexion-dominant modes under closed-loop drift.
192. Current non-rule-based recovery strategy for corrected `tcp10` is to add a separate corrective dataset, not to replace the full dataset. The corrective dataset should start from noisy `late-approach`, `preshape`, or `early-close` anchor states and preserve the same raw observation/action semantics as `collect_pickandlift_rlds.py`.
193. Corrective recollection is implemented in `scripts/data/collect_pickandlift_corrective_rlds.py`. It replays the scripted expert to an anchor, perturbs Franka arm joints, Allegro hand joints, and mustard pose, then records only the successful recovery segment through grasp/lift.
194. Corrective raw episodes remain schema-compatible with the existing pick-and-lift converters (`images/state/action/phase/contact/arm_cmd_pose_wxyz/arm_obs_pose_wxyz/arm_pose_error`) and add only extra metadata keys: `corrective_anchor_phase`, `corrective_anchor_step`, `corrective_anchor_ratio`, and `corrective_noise_json`.
195. Recommended training curriculum for the corrected arm-TCP+hand path is: full dataset first, then additional fine-tuning on the corrective anchor dataset. Do not collapse these into a single “all starts near object” dataset by default.
196. First corrective shard collected on 2026-03-08 is `dataset/franka_pickandlift_corrective_anchor_owrap20_20260308` with 20 successful episodes saved in 29 attempts (`capture_hz=5`, `640x480`, anchors sampled from `approach,preshape,close`).
197. The corresponding corrected arm-TCP+hand raw-latent export is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_corrective_anchor_owrap20_20260308`, created with the existing pick-and-lift k=4 basis `dataset/pickandlift_synergy_basis_owrap20_20260306/pickandlift_hand_pca_k4.npz`.
198. The corresponding OXE export for corrective fine-tuning is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_corrective_anchor_owrap20_oxe_20260308` (`mustard_grasp_oxe`, `action_dim=10`, 20 episodes).
199. Corrective-only fine-tuning from the corrected base best model was tested on 2026-03-08 with run `models/pickandlift_arm_tcp_hand_octo_corrective_ft_k4_20260308/run_260308_134909`. It early-stopped at step `1250`, with best validation at step `250` (`val_loss≈2.6492`), but rollout regressed severely: `codex/logs/json/pickandlift_arm_tcp_hand_octo_corrective_ft_eval3_20260308.json` shows `0/3 success`, `reach=0`, `grasp=0`, `lift=0`, `approach_min_err≈0.120m`, `approach_min_rot_err≈27.6deg`. Interpretation: corrective-only causes catastrophic forgetting of the initial reach stage.
200. A mixed latent raw union dataset was created on 2026-03-08 at `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_mixed_full20_corrective20_20260308` by combining the original full 20-episode corrected dataset with the 20-episode corrective shard; the corresponding OXE export is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_mixed_full20_corrective20_oxe_20260308`.
201. Mixed full+corrective fine-tuning from the corrected base best model was tested in `models/pickandlift_arm_tcp_hand_octo_mixed_ft_k4_20260308/run_260308_140533`. During training the best validation snapshot reached at least step `1750` with `val_loss≈0.3641`, and the mixed curriculum preserved arm reach in rollout (`approach_min_err≈1.55mm`, `rot_err≈0.23deg`) unlike corrective-only.
202. However, mixed curriculum still did not solve the hand-closure bottleneck. Snapshot evaluations `codex/logs/json/pickandlift_arm_tcp_hand_octo_mixed_ft_live_eval3_20260308.json` and `codex/logs/json/pickandlift_arm_tcp_hand_octo_mixed_ft_live_eval1_step1750_20260308.json` show `reach=1` but `grasp=0`, `lift=0`, `best_contacts=0`, with `mean_hand_latent_norm≈1.49-1.51`; the policy remains on the early/open branch instead of switching to the close branch.
203. A later `window_size=4` mixed run (`models/pickandlift_arm_tcp_hand_octo_mixed_w4_b4_20260308/run_260308_160821`) exposed severe model-selection mismatch: an early live snapshot around step `300` achieved `reach=1, grasp=1` on a 1-episode check (`codex/logs/json/pickandlift_arm_tcp_hand_octo_mixed_w4_b4_eval1_step300_20260308.json`), but the eventual validation-best snapshot at step `1000` collapsed to `0/3` with `reach=0, grasp=0, lift=0` (`codex/logs/json/pickandlift_arm_tcp_hand_octo_mixed_w4_b4_eval3_step1000_20260308.json`). For this end-to-end path, validation loss alone is not a reliable deployment selector.
204. To debug that mismatch, a checkpoint sweep run was added at `models/pickandlift_arm_tcp_hand_octo_mixed_w4_sweep_20260308/run_260308_162453` with checkpoints every 50 steps. Rollout checks on steps `250/300/500` (`codex/logs/json/pickandlift_arm_tcp_hand_octo_mixed_w4_sweep_eval1_step250_20260308.json`, `...step300...`, `...step500...`) all failed with `reach=0`, confirming high optimization variance and reinforcing the need for rollout-gated checkpoint selection.
205. Local Octo/OXE references in `/home/minchan/Downloads/dual_arm_VLA/octo` assume delta-style manipulation actions in several utilities, but for the current scripted Franka pick-and-lift dataset the arm absolute-TCP replay remains the only oracle-validated arm interface. Naive arm delta compression must not replace the current absolute arm interface unless it passes oracle replay first.
206. `scripts/research/convert_pickandlift_raw_to_arm_tcp_hand_synergy_raw.py` and `scripts/eval/rollout_pickandlift_arm_tcp_hand_octo.py` now support `--arm-action-type {absolute_world,delta_local}`. The `delta_local` path is experimental and currently not validated for this dataset.
207. A delta-local mixed dataset was built at `dataset/franka_pickandlift_arm_tcp_delta_local_hand_synergy_k4_mixed_full20_corrective20_20260308` with OXE export `dataset/franka_pickandlift_arm_tcp_delta_local_hand_synergy_k4_mixed_full20_corrective20_oxe_20260308`, but oracle replay on `codex/diagnose_pickandlift_arm_tcp_hand_oracle_replay.py` still failed for both current-step and next-step local deltas (`codex/logs/json/pickandlift_arm_tcp_hand_oracle_replay_delta_local_20260308.json`, `codex/logs/json/pickandlift_arm_tcp_hand_oracle_replay_delta_local_next_20260308.json`). Interpretation: the current 5Hz pick-and-lift export does not yet admit a faithful compressed arm-delta replay under the present hold/replay semantics.
208. A phase-conditioned state path is now supported through `--append-phase-features` in both `scripts/research/convert_pickandlift_raw_to_arm_tcp_hand_synergy_raw.py` and `scripts/eval/rollout_pickandlift_arm_tcp_hand_octo.py`. It appends 8 dims to the proprio state (`6`-way phase one-hot + phase progress + global progress), raising pick-and-lift state dim from `60` to `68`.
209. The phase-conditioned absolute-arm mixed dataset path is `dataset/franka_pickandlift_arm_tcp_abs_phase_hand_synergy_k4_mixed_full20_corrective20_20260308`, with OXE export `dataset/franka_pickandlift_arm_tcp_abs_phase_hand_synergy_k4_mixed_full20_corrective20_oxe_20260308`.
210. Phase-conditioned end-to-end training on 2026-03-08 did not restore rollout stability in current tests. Both the unfreeze-all run `models/pickandlift_arm_tcp_hand_octo_abs_phase_w4_20260308/run_260308_165438` (checked at steps `200` and `350`) and the default-frozen run `models/pickandlift_arm_tcp_hand_octo_abs_phase_w4_frozen_20260308/run_260308_170629` (checked at steps `50` and `200`) produced `0/1` rollout with `reach=0, grasp=0, lift=0` under `--append-phase-features`. Phase cues alone are therefore insufficient to solve the current end-to-end instability.
211. Local Octo reference check on 2026-03-08 confirmed that original Octo validation uses the same held-out action-head loss as training (`model.heads["action"].loss(...)`) after the same batch preprocessing, but stock `octo/scripts/finetune.py` only logs that validation loss and separate rollout metrics; it does not early-stop or select deployment checkpoints by validation loss alone.
212. For this repo's end-to-end arm+hand experiments, validation loss must be treated as optimization telemetry rather than a deployment selector. A concrete mismatch already appeared in `models/pickandlift_arm_tcp_hand_octo_mixed_w4_b4_20260308/run_260308_160821`: the live snapshot around step `300` reached and grasped, while the lower-validation-loss snapshot at step `1000` collapsed to `reach=0`.
213. The strong regression in `models/pickandlift_arm_tcp_hand_octo_abs_phase_w4_frozen_20260308/run_260308_170629` is characterized by near-zero arm motion rather than a distinct new grasp failure: compared with the earlier mixed `window_size=4` live snapshot (`mean_arm_delta≈0.066`, `reach=1`, `grasp=1`), the frozen phase run outputs tiny arm changes (`mean_arm_delta≈0.0045` at step `50`, `≈0.0012` at step `200`) and stays near the initial pose (`approach_min_err≈0.47m`, `approach_min_rot_err≈57deg`).
214. The most plausible cause of that regression is the combination of (a) a new proprio input shape (`60 -> 68` via `--append-phase-features`), (b) initializing from a non-phase checkpoint, and (c) limited adaptation in the default-frozen run (`--unfreeze-all` off, only `200` steps). Under that setup the model can reduce average held-out action loss by predicting conservative near-mean actions without preserving closed-loop reachability.
215. `scripts/eval/sweep_pickandlift_arm_tcp_hand_checkpoints.py` was added on 2026-03-08 to automate rollout-gated checkpoint selection for end-to-end arm+hand runs. It evaluates saved checkpoints with the existing rollout script and ranks them by success/lift/grasp/reach instead of validation loss.
216. A successful end-to-end 10D deployment was found on 2026-03-08 by continuing from the partial-success snapshot `models/pickandlift_arm_tcp_hand_octo_mixed_w4_b4_20260308/run_260308_160821/best_model_snapshot_live_step300`. The continuation run is `models/pickandlift_arm_tcp_hand_octo_continue_from_step300_20260308/run_260308_180929`, and checkpoint `checkpoint_000250` is the current best rollout model.
217. Important deployment note: the above successful 10D end-to-end checkpoint requires `--policy-repeat 10` at rollout. With the dataset-matched default `policy-repeat 20`, the same checkpoint reaches and grasps but repeatedly fails the final lift-hold/no-drop condition despite exceeding lift height (`object_dz_max≈0.0868`). With `--policy-repeat 10`, the same checkpoint achieves stable lift success.
218. Verified successful artifacts for the current best end-to-end 10D model are `codex/logs/json/pickandlift_arm_tcp_hand_octo_continue_step250_repeat10_eval1_20260308.json` (`1/1 success`), `codex/logs/json/pickandlift_arm_tcp_hand_octo_continue_step250_repeat10_eval3_20260308.json` (`3/3 success`), and `codex/logs/videos/pickandlift_arm_tcp_hand_octo_continue_step250_repeat10_demo_20260308.mp4`.
219. A lift-focused corrective dataset was collected at `dataset/franka_pickandlift_corrective_liftclose_owrap20_20260308` with close-only anchors (`close-anchor-range 0.60..0.95`) to emphasize post-grasp lift recovery. The corresponding latent export is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_corrective_liftclose_owrap20_20260308`, and the mixed OXE dataset is `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_mixed_full20_lift40_oxe_20260308`.
220. That lift-focused continuation run `models/pickandlift_arm_tcp_hand_octo_continue_step250_liftmix_20260308/run_260308_184107` did not outperform the successful `checkpoint_000250` deployment path in the first rollout checks; early checkpoints `50/100/150/200` all failed to reach/grasp, and checkpoint `250` only recovered reach without restoring stable lift.
221. Repository cleanup decision on 2026-03-09: keep all `.md` documents, but prune `dataset/` and `models/` down to the final successful 10D end-to-end artifacts only. The kept dataset artifacts are `dataset/pickandlift_synergy_basis_owrap20_20260306`, `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_mixed_full20_corrective20_20260308`, and `dataset/franka_pickandlift_arm_tcp_hand_synergy_k4_mixed_full20_corrective20_oxe_20260308`. The kept model artifact is `models/pickandlift_arm_tcp_hand_octo_continue_from_step300_20260308`.
222. The next planned research direction is a hand-intent bottleneck above the current low-level hand synergy interface. The working plan is documented in `docs/hand-intent-bottleneck-plan.md`.
223. In this repo, "hand intent" means semantic hand-object interaction mode (for example `wrap`, `pinch`, `push`, `rotate`), not just arm target selection such as bottle body vs neck.
224. The recommended first hand-intent benchmark should stay mustard-only and multi-task, starting with `wrap_and_lift`, `pinch_and_lift`, `push_over`, and `rotate_in_place`.
225. The intended architecture is `image + state + language -> Octo trunk -> arm TCP head + intent head -> hand decoder -> hand synergy k=4 -> joint16`, with no runtime rule-based switching.
226. The concrete mustard-only benchmark specification is tracked in `docs/hand-intent-mustard-benchmark.md`.
227. For hand-intent claims, task differences should be hand-dominant when possible; "body vs neck" region selection alone is not considered sufficient evidence because it can be explained mainly by arm targeting.
228. The first canonical instruction set for the mustard hand-intent benchmark is `wrap and lift the bottle`, `pinch and lift the bottle`, `push the bottle over`, and `rotate the bottle`.
229. Recommended implementation order for the new benchmark is `wrap_and_lift -> push_over -> pinch_and_lift -> rotate_in_place`, with multi-task direct `arm TCP + hand synergy` training coming before the future intent-bottleneck model.
230. `scripts/data/collect_mustard_intent_benchmark.py` is the new scripted collector for the mustard-only hand-intent benchmark. It reuses the Franka+Allegro IK/contact pipeline and stores per-episode `task_name` plus weak `intent_json` metadata.
231. The benchmark collector currently implements four task names: `wrap_and_lift`, `pinch_and_lift`, `push_over`, and `rotate_in_place`.
232. As of 2026-03-09 smoke checks, `wrap_and_lift` and `push_over` can each save a successful episode with the current default scripted settings. `pinch_and_lift` and `rotate_in_place` are scaffolded but still need trajectory/target tuning before bulk data collection.
233. The first-stage mustard hand-intent benchmark should default to deterministic collection (`spawn_jitter_xy=0`, `spawn_yaw_jitter_deg=0`); jitter should be added only after each scripted task succeeds reliably.
234. User-directed task refinement on 2026-03-09: `pinch_and_lift` should represent a strict thumb-index pinch rather than a thumb-index-middle support grasp. Middle and ring fingers should stay tucked and should not be the main moving/contacting fingers.
235. User-directed task refinement on 2026-03-09: `push_over` should be an index-pointing fingertip push from slight stand-off, not a palm/body slam. The desired pose is pointing-like: index extended, other fingers curled.
236. User-directed task refinement on 2026-03-09: `rotate_in_place` should move toward a wrap-grasp-turn-release style task rather than a pure side-contact scrape, because the intended behavior is stable grasped rotation while the bottle stays upright.
237. Additional `push_over` clarification on 2026-03-10: the arm should first reach the object with the hand open, then switch to a pointing pose at stand-off distance, then press using the index finger long-axis direction. In current axis colors, the pushing finger's blue `+Z` axis should press along the mustard object's red `+X` axis.
238. For push-task debugging, axis visualization should include at least the mustard frame, the hand palm frame, and the index finger distal frame (`ff_distal`) so contact direction can be judged visually.
239. Additional `push_over` spacing refinement on 2026-03-10: the scripted mustard spawn for this task should be shifted forward along the object's local red `+X` axis (current default `spawn_offset_local=(0.08, 0.0, 0.0)`) rather than pulling the arm farther back, because preshape stability degrades when the arm starts too far behind the object.
240. Further `push_over` refinement on 2026-03-10: the object forward shift was increased again (current default `spawn_offset_local=(0.12, 0.0, 0.0)`), and the scripted push now uses a two-stage stand-off behavior: approach open, then hold position while the hand rotates from the open-approach quaternion to an aligned pointing quaternion (`interact_rot_quat=(0.5, 0.5, 0.5, 0.5)`), then only after alignment advance toward the push target.
241. Additional `push_over` behavior refinement on 2026-03-10: the object forward shift was increased further (current default `spawn_offset_local=(0.16, 0.0, 0.0)`), the push stroke was shortened (`interact_offset.x=-0.115`, `interaction_steps=120`), and the scripted sequence no longer snaps the hand open after contact. It now ends with a short pointed hold at the final push pose instead of an immediate retract/open.
242. Additional `push_over` spacing refinement on 2026-03-10: the hand stand-off was moved farther back relative to the object (`approach_offset.x=-0.185`, `interact_offset.x=-0.150`). This change removed the early hand-object catching seen in prior push videos and produced index-only contact (`best_fingers=ff`) in `codex/tmp_intent_push_axes_v8/collection_summary.json`.
243. Cleanup decision on 2026-03-10 for the mustard hand-intent benchmark: keep only the current representative `wrap_and_lift` video and the latest designated `push_over` debug artifact (`codex/logs/videos/mustard_intent_push_over_axes_demo_v8_20260310.mp4` with `codex/tmp_intent_push_axes_v8/collection_summary.json`). Older failed intent-benchmark debug videos and temp summaries should be pruned.
244. User-directed `pinch_and_lift` refinement on 2026-03-10: use the same arm approach/interaction path as `wrap_and_lift` and prioritize successful pickup over strict `<=2` contact-finger purity. The current pinch contact rule therefore requires thumb+index participation and force/contact thresholds, but no longer rejects incidental extra finger contact during lift.
245. With the above change, `pinch_and_lift` now produces a successful scripted episode in `codex/logs/videos/mustard_intent_pinch_and_lift_demo_v3_20260310.mp4` and `codex/tmp_intent_pinch_video_v3/collection_summary.json` (`reach=1`, `success=1`), although the current grasp still includes support contact from `mf,rf` and is therefore a successful pickup baseline rather than a clean textbook two-finger pinch.
246. Subsequent strict-pinch debugging on 2026-03-10 changed `env/allegro_hand_trajectories.py` so `pinch_precision` keeps `mf/rf` extended and splayed instead of curled. The resulting video `codex/logs/videos/mustard_intent_pinch_and_lift_demo_v4_20260310.mp4` shows index+thumb-only contact (`best_fingers=ff,th`), but the episode still fails overall (`success=0`). The remaining bottleneck for `pinch_and_lift` is now lift stability/force, not finger-selection semantics.
247. Additional `pinch_and_lift` control refinement on 2026-03-10: the scripted collector no longer starts lifting immediately after close. It now inserts a close-hold stage (`close_hold_steps`, currently `140` for pinch) at the pre-lift pose and only enters the lift phase after stable grasp acquisition is detected. If stable grasp is never acquired, the episode remains a failure without transitioning into lift.
248. Bug fix on 2026-03-10: `pinch_and_lift` close steps previously used a vertically offset pre-lift target (`interact_target + close_z_clearance`), which visually looked like lifting before the grasp was finished. The close and close-hold stages now stay exactly at `interact_target` height, and vertical motion begins only after stable grasp acquisition. Current verification artifact: `codex/logs/videos/mustard_intent_pinch_and_lift_demo_v6_20260310.mp4` with `codex/tmp_intent_pinch_video_v6/collection_summary.json`.
249. Additional `pinch_and_lift` tuning on 2026-03-10 lowered the contact height from the neck toward the bottle body by changing the task offsets to `approach_offset=(-0.090, -0.015, 0.025)` and `interact_offset=(-0.078, -0.015, -0.015)`. Verification artifact: `codex/logs/videos/mustard_intent_pinch_and_lift_demo_v7_20260310.mp4` with `codex/tmp_intent_pinch_video_v7/collection_summary.json`. The episode still fails, but contact remains isolated to `ff,th`, so the remaining bottleneck is likely pinch force/opposition stability rather than extra-finger interference.
250. Frame-by-frame review of `codex/logs/videos/mustard_intent_pinch_and_lift_demo_v7_20260310.mp4` on 2026-03-10 identified two concrete failure modes: around 9s, the closing motion applies a tangential shove that topples the bottle before a stable pinch is formed; around 17s, thumb and index are not aligned as parallel opposing jaws, so the pinch line misses and the lift fails. The next pinch tuning should therefore reduce tangential arm/hand shove during close and explicitly improve thumb-index jaw alignment.
251. Subsequent `pinch_precision` refinements on 2026-03-10 changed the hand trajectory to a staged thumb-first close (`open -> preshape -> thumb_settle -> close`) and made the thumb/index final pose straighter to resemble parallel jaws. The latest verification artifact is `codex/logs/videos/mustard_intent_pinch_and_lift_demo_v9_20260310.mp4` with `codex/tmp_intent_pinch_video_v9/collection_summary.json`; it still fails, but retains `ff,th`-only contact while reducing the earlier immediate shove/lift logic errors.
252. Benchmark direction update on 2026-03-10: because strict `pinch_and_lift` remains brittle for the mustard bottle, a new easier tangential interaction task `hook_and_pull` is preferred as the third benchmark task. It uses a `hook_pull` hand trajectory and a drag-toward-robot success criterion instead of lift.
253. `hook_and_pull` is implemented in `scripts/data/collect_mustard_intent_benchmark.py` with instruction `hook and pull the bottle`, weak intent label `interaction_mode=hook_pull`, and success based on bottle translation toward the robot with low lift. The initial scripted smoke run succeeded: `codex/logs/videos/mustard_intent_hook_and_pull_demo_v1_20260310.mp4` and `codex/tmp_intent_pull_video_v1/collection_summary.json` (`reach=1`, `success=1`, `best_fingers=ff,mf,rf`).
254. After user review on 2026-03-10, `hook_and_pull` semantics were tightened: it should share the same overall hand shape and staging as `push_over`, except the index fingertip is curled to catch the bottle neck before retracting. The intended motion is `open reach -> pointing preshape -> neck engage -> pull back`, not a multi-finger drag.
255. Current `hook_and_pull` debugging artifacts after the semantic correction are `codex/logs/videos/mustard_intent_hook_and_pull_demo_v4_20260310.mp4` through `codex/logs/videos/mustard_intent_hook_and_pull_demo_v7_20260310.mp4`. These runs now keep `best_fingers=ff` only, but still fail the pull success criterion; remaining tuning is on neck engagement depth and retract path rather than hand-mode semantics.
256. User-directed `hook_and_pull` path update on 2026-03-10: the index finger should move above the object, sweep forward, then lower to create a neck-hook geometry before retracting. The current scripted collector implements this `above -> forward -> down -> pull` structure for ongoing debugging.
257. `hook_and_pull` forward-sweep tuning on 2026-03-10: increasing the sweep too far (`v8`) removed contact entirely, so the current working midpoint is a moderate forward sweep (`v9`) that restores `best_fingers=ff` while keeping the `above -> forward -> down -> pull` path.
258. User then requested a much more aggressive `hook_and_pull` forward reach before descending. The current `v10` tuning increases forward sweep, keeps the hand high for longer, then drops sharply; it still keeps `best_fingers=ff` and increases contact count to 2, making it the current closest match to the desired hook semantics even though pull success is not yet achieved.
259. `hook_and_pull` path tuning on 2026-03-10: replacing object-frame forward motion with a straight world-`+x` sweep removes the unwanted left drift. Purely zeroing the lateral alignment offset removed contact (`v11`), so the current compromise is: keep the small static `y=-0.008` alignment offset, but use straight world-`+x/-x` for sweep and retract (`v12`), which restores `best_fingers=ff`.
260. Broad rightward `hook_and_pull` lateral sweep on 2026-03-10 (`codex/sweep_hook_pull_right.py`) tested hook `y` from `-0.008` to `-0.120`. No setting achieved task success; `ff` contact persisted through moderate right shifts, but contact vanished beyond about `y=-0.092`. Conclusion: lateral-right translation alone is insufficient; future tuning must change hook depth and/or retract magnitude, not only `y`.
261. `hook_and_pull` success criterion was revised on 2026-03-10 based on the representative video `codex/logs/videos/mustard_intent_hook_and_pull_demo_y_m0.056_20260310.mp4`. The previous criterion incorrectly required pull-distance and fingertip contact to overlap in the same late pull frames. The new rule marks success if: (a) hook contact is established at least once, (b) `pull_dx_max >= pull_success_dx`, and (c) object lift remains below `pull_max_lift_dz`. With this rule, the representative run at `hook_y=-0.056` is a valid success.
262. Representative successful `hook_and_pull` artifact under the revised criterion is `codex/logs/videos/mustard_intent_hook_and_pull_demo_y_m0.056_success_criterion_20260310.mp4` with summary `codex/tmp_intent_pull_video_y_m0.056_success_criterion_20260310/collection_summary.json`. Its key metrics are `best_fingers=['ff']`, `pull_dx_max≈0.1133 m`, and `object_dz_max≈0.0030 m`.
263. As of 2026-03-11, the three primary mustard hand-intent collection tasks (`wrap_and_lift`, `push_over`, `hook_and_pull`) share a common default mustard spawn pose via `collect_mustard_intent_benchmark.py --spawn-pos (0.78, 0.06, 0.82)` with `spawn_offset_local=(0,0,0)` in each task spec. Task-specific behavior should be expressed through arm-relative offsets and hand trajectories, not by moving the object.
264. The default `hook_and_pull` collection environment now uses the successful lateral alignment `y=-0.056` in its task spec while keeping the common mustard pose.
265. `push_over` success criterion was revised on 2026-03-11 for collection use: success now depends on whether pointing contact was established at least once, the object toppled beyond `push_success_tilt_deg`, and object lift stayed below `push_max_lift_dz`. This avoids falsely labeling visually successful pushes as failures when fingertip contact does not persist in the final toppled frames.
266. Smoke checks with the common mustard pose on 2026-03-11: `wrap_and_lift` saves successfully in `codex/tmp_task_env_wrap_smoke_v2`, `push_over` saves successfully in `codex/tmp_task_env_push_smoke_v3`, and `hook_and_pull` saves successfully in `codex/tmp_task_env_hook_smoke_v2`.
267. Initial mustard hand-intent dataset collection plan on 2026-03-11: use the three primary tasks (`wrap_and_lift`, `push_over`, `hook_and_pull`) with the shared mustard pose, mild diversity (`spawn_jitter_xy=0.005`, `spawn_yaw_jitter_deg=3`), and `capture_hz=10`. Target size is `50 episodes per task` (150 total), which is large enough for first multi-task train/val experiments without overcommitting to low-diversity scripted data.
268. The first shared-pose mustard hand-intent dataset was collected on 2026-03-11 under `dataset/mustard_intent_v1_common_pose_20260311`. It contains `50` successful episodes each for `wrap_and_lift`, `push_over`, and `hook_and_pull` (`150` total), with per-task summaries in each task folder and a combined summary at `dataset/mustard_intent_v1_common_pose_20260311/collection_summary.json`.
269. Mustard hand-intent k-sweep artifacts are now generated by `scripts/research/build_mustard_intent_hand_synergy_basis.py`. The first sweep on `dataset/mustard_intent_v1_common_pose_20260311` evaluated `k=2..8` and saved results to `dataset/mustard_intent_synergy_basis_20260311/mustard_intent_hand_pca_summary.json`.
270. Important result from the 2026-03-11 mustard hand-intent k-sweep: the hand-action manifold is effectively rank-6 (`k>=6` gives near-zero reconstruction error), but oracle task replay at the current captured action rate (`capture_hz=10`) still fails for `push_over` and `hook_and_pull` even with exact reconstruction. Therefore k-compression is not the current bottleneck for those dynamic tasks; action sampling semantics/capture rate must be revisited before using this dataset as the main Octo multi-task benchmark.
271. To address the dynamic-task sampling issue, a second mustard hand-intent dataset was recollected on 2026-03-11 at `capture_hz=50` under `dataset/mustard_intent_v2_capture50_20260311`. To control storage while still increasing temporal density 5x over the 10Hz set, this recollection uses `20` successful episodes per task (`wrap_and_lift`, `push_over`, `hook_and_pull`; `60` total) with the same shared mustard pose and mild diversity (`spawn_jitter_xy=0.005`, `spawn_yaw_jitter_deg=3`). Combined summary: `dataset/mustard_intent_v2_capture50_20260311/collection_summary.json`.
272. Running `scripts/research/build_mustard_intent_hand_synergy_basis.py` on the 50Hz recollection (`dataset/mustard_intent_synergy_basis_capture50_20260311/mustard_intent_hand_pca_summary.json`) did not resolve the dynamic-task failure. Under current replay semantics, `k=2`/`k=3` still maximize overall oracle success only because `wrap_and_lift` survives while `push_over` and `hook_and_pull` remain at `0%` for all `k=2..8`. Even `k>=6` yields near-zero reconstruction error yet still fails task replay, so k-selection is not the limiting issue for the current intent benchmark.
273. On 2026-03-11, `scripts/research/build_mustard_intent_hand_synergy_basis.py` was fixed so dynamic-task oracle replay recomputes `task_success` after the optional `post_settle_steps` window. Before this fix, `push_over` episodes could visibly topple after the recorded action horizon while still being logged as failures because `tilt_deg_max` was updated during post-settle but `task_success` was not recomputed.
274. After the post-settle success fix, `push_over` must be evaluated separately from the broad multi-task summaries when debugging k-selection. On `dataset/mustard_intent_v2_capture50_20260311`, a push-only sweep (`dataset/mustard_intent_synergy_basis_capture50_pushonly_fixed_20260311/mustard_intent_hand_pca_summary.json`) shows `k=2..8` all reconstruct perfectly (`rmse≈0`) and all achieve the same oracle success rate `0.95`; therefore k is not the bottleneck for `push_over`, and the single remaining push failure is episode `push_over/raw/episode_00014.npz`.
275. On 2026-03-11, `codex/record_mustard_intent_synergy_replay.py` was updated to support `--post-settle-steps` and to keep recording through the passive settle window for dynamic tasks. Use this recorder mode when validating `push_over` or `hook_and_pull`; otherwise videos can end before the delayed topple/pull completes and falsely suggest a replay failure.
276. After re-running `hook_and_pull` with the fixed post-settle evaluator on `dataset/mustard_intent_v2_capture50_20260311`, the hook-only sweep (`dataset/mustard_intent_synergy_basis_capture50_hookonly_fixed_20260311/mustard_intent_hand_pca_summary.json`) shows `k=2..8` all reconstruct perfectly (`rmse≈0`) and all achieve oracle success `0.75`, not `0.0`. Therefore the earlier broad conclusion that hook replay always fails was also overstated; for hook, k is again not the bottleneck, and the remaining replay failures are cases with valid hook contact but slightly insufficient backward translation (`pull_dx_max≈0.070–0.077 m`).
277. Longer passive settle materially changes dynamic-task replay interpretation. With `post_settle_steps=2000`, `push_over` on `dataset/mustard_intent_v2_capture50_20260311` reaches `1.00` oracle success for `k=4` (`dataset/mustard_intent_synergy_basis_capture50_pushonly_post2000_k4_20260311/mustard_intent_hand_pca_summary.json`), confirming that the remaining push replay discrepancy at 500 steps was only horizon truncation. For `hook_and_pull`, increasing settle from `500 -> 2000` raises `k=4` oracle success from `0.75 -> 0.90`, but `5000` does not improve further (`dataset/mustard_intent_synergy_basis_capture50_hookonly_post5000_k4_20260311/mustard_intent_hand_pca_summary.json`), so the last two hook failures are not just due to ending the simulation too early.
278. On 2026-03-11, `hook_and_pull` collection motion was updated per user review: after neck engagement, the hand should retract much farther while keeping a straight world-`-x` pull with fixed `y/z` (no diagonal drift during the pull phase). The scripted collector now uses a longer retract distance (`0.235 m`) and longer pull horizon (`final_steps=280`). Smoke check: `codex/tmp_hook_pull_straight_smoke_20260311/collection_summary.json` saved successfully on the first attempt.
279. Follow-up user review on 2026-03-11: the straight `hook_and_pull` should stop earlier to avoid Franka twisting into its own body late in the pull. The current collector shortens the retract endpoint to `0.200 m` and reduces the pull horizon to `final_steps=220`; the reference demo for this version is `codex/logs/videos/mustard_intent_hook_and_pull_spawn_y0p12_demo_v2_20260311.mp4`.
280. A new common-pose 5Hz mustard-intent dataset was collected on 2026-03-11 under `dataset/mustard_intent_v4_capture5_y0p12_20260311` using shared spawn pose `(0.78, 0.12, 0.82)` for all three tasks (`wrap_and_lift`, `push_over`, `hook_and_pull`). It contains `50` successful episodes per task (`150` total); combined summary: `dataset/mustard_intent_v4_capture5_y0p12_20260311/collection_summary.json`.
281. The combined k-sweep for `dataset/mustard_intent_v4_capture5_y0p12_20260311` was run on 2026-03-11 with `post_settle_steps=2000`, saving to `dataset/mustard_intent_synergy_basis_capture5_y0p12_20260311/mustard_intent_hand_pca_summary.json`. Under replay-based model selection, the best overall k is `2` (`overall oracle=0.80`, task rates: wrap `1.0`, push `0.4`, hook `1.0`). Higher-k PCA reconstruction is more faithful (`cum_var≈1.0` by `k>=6`), but replay success collapses for `push_over` (0 for `k>=3`) and also degrades for `wrap_and_lift` (e.g. `0.28` at `k=4`). Therefore, for this shared-pose 5Hz multi-task benchmark, replay-optimal k is `2`, not the largest-reconstruction k.
282. Debugging on 2026-03-11 showed the broad `push_over` replay failures are a mix of two different issues. Example `dataset/mustard_intent_v4_capture5_y0p12_20260311/push_over/raw/episode_00001.npz` is stored as a successful rollout (`metrics_json.task_success=true`, dataset-frames video `codex/logs/videos/push_over_episode_00001_dataset_frames_20260311.mp4`), but exact raw replay through the current oracle stack still fails (`codex/logs/videos/push_over_episode_00001_raw_exact_replay_20260311.mp4`, `tilt_deg_max≈24`). Separately, the `k=2` reconstructed replay of the same episode topples successfully (`tilt_deg_max≈91`) but is still marked failed because `push_contact_seen=false` (`codex/logs/videos/push_over_episode_00001_k2_failed_replay_20260311.mp4`). Therefore current push conclusions must distinguish (a) replay-stack mismatch from raw success and (b) an overly strict contact requirement in toppled replays.
283. On 2026-03-11, `push_over` success logic was tightened semantically and relaxed against sampled-contact brittleness at the same time. Both `scripts/data/collect_mustard_intent_benchmark.py` and `scripts/research/build_mustard_intent_hand_synergy_basis.py` now use `topple_angle >= push_success_tilt_deg`, low lift (`object_dz_max <= push_max_lift_dz`), and a new `release_after_topple` condition defined as `push_release_steps` consecutive post-topple capture frames with zero fingertip-object contact. `push_contact_seen` is still logged, but it is no longer an absolute success requirement.
284. After the new `push_over` release criterion was applied, replay of `dataset/mustard_intent_v4_capture5_y0p12_20260311/push_over/raw/episode_00001.npz` with `k=2` changed from failure to success (`codex/logs/videos/push_over_episode_00001_k2_replay_releasefix_20260311.mp4`, `tilt_deg_max≈91.4`, `release_after_topple=true`). However, exact raw replay of the same 5Hz-sampled episode still fails to reproduce the stored successful dataset rollout (`codex/logs/videos/push_over_episode_00001_raw_exact_replay_releasefix_20260311.mp4`, `tilt_deg_max≈24.1`, `release_after_topple=false`). This confirms the remaining push problem is not only success labeling; 5Hz sampled raw replay still mismatches the original 100Hz scripted rollout.
285. The older shared-pose 5Hz dataset `dataset/mustard_intent_v4_capture5_y0p12_20260311` should be treated as push-contaminated: its `push_over` subset was collected before the `release_after_topple` rule, so some visibly bad finger-trap episodes were saved as successes. Do not use that `push_over` subset as the main training benchmark.
286. A cleaned replacement dataset was created on 2026-03-11 at `dataset/mustard_intent_v5_capture5_y0p12_pushclean_20260311`. It reuses `wrap_and_lift` and `hook_and_pull` from `v4`, but recollects `push_over` from scratch under the new `release_after_topple` criterion. Final counts are `50` episodes each for `wrap_and_lift`, `push_over`, and `hook_and_pull` (`150` total), with combined summary at `dataset/mustard_intent_v5_capture5_y0p12_pushclean_20260311/collection_summary.json`.
287. Running the combined k-sweep on the cleaned dataset `dataset/mustard_intent_v5_capture5_y0p12_pushclean_20260311` with `post_settle_steps=2000` produced `dataset/mustard_intent_synergy_basis_capture5_y0p12_v5clean_20260311/mustard_intent_hand_pca_summary.json`. Under the current replay-based selector, `best_k=2` with `oracle_success_rate=1.0` and per-task success `wrap=1.0`, `push=1.0`, `hook=1.0`. Higher-k reconstructions are more faithful (`k>=6` gives essentially zero reconstruction error) but still collapse in replay (`push=0.0`, `wrap` also degrades), so this `k=2` result should be treated as the current working baseline for training, not as a final representation-theoretic conclusion.
288. Mustard intent multi-task Octo training baseline (2026-03-11) uses the cleaned dataset `dataset/mustard_intent_v5_capture5_y0p12_pushclean_20260311`, converted through `scripts/research/convert_mustard_intent_raw_to_arm_tcp_hand_synergy_raw.py` into `dataset/mustard_intent_arm_tcp_hand_synergy_k2_v5clean_20260311` with action space `arm TCP6 + hand synergy k=2` (`action_dim=8`).
289. The corresponding TFDS/OXE export for the current mustard intent baseline is `dataset/mustard_intent_arm_tcp_hand_synergy_k2_v5clean_oxe_20260311`, built with `scripts/data/convert_mustard_raw_to_oxe.py`. It reuses builder name `mustard_grasp_oxe`, so training should point `--data-dir` at this root while keeping `--dataset-name mustard_grasp_oxe`.
290. `scripts/train/finetune_mustard_octo.py` now auto-resolves Octo source via project-root `.octo_src_path` before importing `octo`, matching the rollout scripts' behavior.
291. The current local Octo source path is stored in `.octo_src_path` and points to `codex/octo-src`. This local clone is for training/runtime only and should be treated as non-core temporary dependency state unless intentionally versioned later.
292. The current mustard intent multi-task Octo overfit run is `models/mustard_intent_octo_overfit_synergy_k2_v5clean_20260311/run_260311_195811`, trained on `arm TCP6 + hand synergy k=2` with `window_size=2`. Loss drops from about `7.47` to `0.44`, with minimum about `0.321` near step `1890`.
293. Pure-Octo closed-loop rollout for the mustard intent benchmark is implemented in `scripts/eval/rollout_mustard_intent_octo.py`. It uses the finetuned Octo model to predict `arm TCP6 + synergy-k`, decodes synergy back to Allegro joints, and applies task-specific success metrics for `wrap_and_lift`, `push_over`, and `hook_and_pull`.
294. Initial pure-Octo smoke evaluation on 2026-03-13 (`codex/logs/json/mustard_intent_octo_eval_smoke_20260313.json`) gives `0/3` success on one episode per task. In all three tasks the arm reaches the target region (`reached=true`), but fingertip-object contact stays absent (`best_contacts=0`), so the current failure mode is hand interaction not being triggered rather than arm targeting failure.
295. Representative pure-Octo rollout videos currently available are `codex/logs/videos/mustard_intent_octo_wrap_and_lift_demo_260313_130309.mp4`, `codex/logs/videos/mustard_intent_octo_push_over_demo_260313_131759.mp4`, and `codex/logs/videos/mustard_intent_octo_hook_and_pull_demo_260313_131919.mp4`. The short task-specific summaries currently confirmed are `codex/logs/json/mustard_intent_octo_push_demo_short_20260313.json` and `codex/logs/json/mustard_intent_octo_hook_demo_short_20260313.json`, both with `success_rate=0.0`.
296. The original `reached` metric in `scripts/eval/rollout_mustard_intent_octo.py` was wrong: it mixed true object-relative approach error with self-predicted target tracking error from the IK step, making `reached=true` possible even when the arm never approached the bottle. On 2026-03-13 this was fixed so approach error is now computed only against the task interaction target resolved from the current object pose.
297. Pure-Octo failure on the mustard intent benchmark is currently dominated by a closed-loop early-phase stall, not by total train-frame misfit. Teacher-forcing on training frames shows moderate action error (`mae≈0.04–0.05` on the first frame; around `0.016–0.03` through the early approach segment of a wrap episode), but a direct rollout diagnostic shows the predicted arm TCP converging to a small neighborhood around the initial pose (`z≈1.28`) instead of making the larger approach transition seen in the demonstrations (`z≈0.86`). This indicates self-fed rollout drift into an initial-pose fixed point before hand contact is ever triggered.
298. On 2026-03-13, mustard-intent Octo was retrained with chunked action prediction (`action_horizon=8`) at `models/mustard_intent_octo_overfit_synergy_k2_h8_v5clean_20260313/run_260313_160441`. The intended deployment setting is receding-horizon execution: predict 8 future actions, execute only the first 2 (`--execute-actions-per-plan 2`), then replan.
299. Chunked rollout substantially improved `wrap_and_lift` relative to the earlier horizon-1 baseline. With `policy-repeat=20`, wrap now reaches the bottle and forms 4-finger+thumb contact (`best_contacts=8`, `best_fingers=['ff','mf','rf','th']`, `approach_min_err≈0.0119`) but still misses the lift threshold (`object_dz_max≈0.061 < 0.08`), so it remains a near-miss rather than a success. Summary: `codex/logs/json/mustard_intent_octo_h8_wrap_eval_20260313.json`; demo video: `codex/logs/videos/mustard_intent_octo_h8_demos_20260313/mustard_intent_octo_wrap_and_lift_demo_260313_161802.mp4`.
300. For the same chunked model and rollout settings, `push_over` and `hook_and_pull` still fail before meaningful hand interaction. Their current one-episode sanity summaries are `codex/logs/json/mustard_intent_octo_h8_push_eval_20260313.json` and `codex/logs/json/mustard_intent_octo_h8_hook_eval_20260313.json`; both show large approach-rotation error (`~50 deg`), zero contact, and zero task effect. A current push demo was recorded at `codex/logs/videos/mustard_intent_octo_h8_demos_20260313/mustard_intent_octo_push_over_demo_260313_162112.mp4`.
301. The h8 run does not currently support a stronger “use best checkpoint instead of final” explanation. Quick rollout checks on earlier checkpoints show they are worse, not better: `checkpoint_000500` gives zero contact for all three tasks, and `checkpoint_001000` already collapses wrap approach again (`approach_min_err≈0.51 m`). Therefore the current failure mode is not primarily caused by evaluating `final_model` instead of some earlier obvious best snapshot.
302. On 2026-03-13, single-task baselines were created from the cleaned mustard-intent benchmark using the same `arm TCP6 + hand synergy k=2` action space and `action_horizon=8`: push-only raw/OXE (`dataset/mustard_intent_arm_tcp_hand_synergy_k2_pushonly_v5clean_20260313`, `dataset/mustard_intent_arm_tcp_hand_synergy_k2_pushonly_v5clean_oxe_20260313`) and hook-only raw/OXE (`dataset/mustard_intent_arm_tcp_hand_synergy_k2_hookonly_v5clean_20260313`, `dataset/mustard_intent_arm_tcp_hand_synergy_k2_hookonly_v5clean_oxe_20260313`).
303. The corresponding single-task Octo runs are `models/mustard_intent_octo_overfit_synergy_k2_pushonly_h8_b4_20260313/run_260313_165335` and `models/mustard_intent_octo_overfit_synergy_k2_hookonly_h8_20260313/run_260313_164518`. Push required a reduced batch size (`batch_size=4`) to avoid OOM, while hook trained successfully with `batch_size=8`.
304. Single-task training did not solve the rollout failure for dynamic tasks. Current one-episode pure-Octo sanity summaries are `codex/logs/json/mustard_intent_octo_pushonly_h8_eval_20260313.json` and `codex/logs/json/mustard_intent_octo_hookonly_h8_eval_20260313.json`; both still show large approach-rotation error (`~52 deg`), zero contact, and zero task effect. Therefore the main bottleneck is not multi-task interference alone; the current k=2 pure-Octo deployment still fails to enter the correct push/hook interaction branch even when trained on a single task.

305. `scripts/train/finetune_mustard_octo.py` was upgraded on 2026-03-13 to support validation-split monitoring, `best_valid_model`, scheduled rollout evaluation, `best_rollout_model`, and early stopping based on both validation and rollout stagnation. Train loss is now log-only and should not be used as the deployment model selector.
306. The new push-only best-selection run for `k=2` is `models/mustard_intent_octo_overfit_synergy_k2_pushonly_h8_bestroll_v3_20260313/run_260313_171629`. It saved `best_valid_model` up to at least step `1000` (`val_loss≈1.217`), but `best_rollout_model` remained step `500` with `success_rate=0.0` on `push_over`.
307. Push-only `k=4` conversion artifacts were created on 2026-03-13: raw `dataset/mustard_intent_arm_tcp_hand_synergy_k4_pushonly_v5clean_20260313` and OXE `dataset/mustard_intent_arm_tcp_hand_synergy_k4_pushonly_v5clean_oxe_20260313`.
308. The new push-only best-selection run for `k=4` is `models/mustard_intent_octo_overfit_synergy_k4_pushonly_h8_bestroll_20260313/run_260313_172911`. By step `500`, it had improved `best_valid_model` to `val_loss≈2.396` but rollout was still `0.0` on `push_over`, with large approach-rotation error and zero contact, so increasing from `k=2` to `k=4` did not immediately fix the push-only pure-Octo failure.309. On 2026-03-13, side-by-side push-only pure-Octo comparison videos were saved at `codex/logs/videos/mustard_intent_push_compare_k2_20260313/mustard_intent_octo_push_over_demo_260313_173903.mp4` and `codex/logs/videos/mustard_intent_push_compare_k4_20260313/mustard_intent_octo_push_over_demo_260313_174233.mp4`. Both fail before meaningful contact; the main visible difference is that `k=2` shows stronger open/close oscillation in the decoded hand while `k=4` stays more neutral.
310. On 2026-03-13, wrap-only `k=4` conversion artifacts were created at `dataset/mustard_intent_arm_tcp_hand_synergy_k4_wraponly_v5clean_20260313` and `dataset/mustard_intent_arm_tcp_hand_synergy_k4_wraponly_v5clean_oxe_20260313`, and best-selection training was started at `models/mustard_intent_octo_overfit_synergy_k4_wraponly_h8_bestroll_20260313/run_260313_180009`.
311. The wrap-only `k=4` run above produced a strong intermediate rollout by step `1500` even though success remained `0.0`: `approach_min_err≈0.00215`, `approach_min_rot_err_deg≈3.63`, `best_contacts=9`, and `best_fingers=[ff,mf,rf,th]`, but lift height stayed too low (`object_dz_max≈0.0183`) to satisfy the current wrap success criterion.
312. On 2026-03-13, `scripts/train/finetune_mustard_octo.py` was changed for overfit experiments to save `best_train_model` (not `best_valid_model`) and `best_rollout_model`. Early stopping now depends only on best-train plateau (`train_check_every`, `train_patience`), rollout never stops training, and `--num-steps <= 0` means run until train plateau.
313. The current wrap-only `k=4` overfit run under this new policy is `models/mustard_intent_octo_overfit_synergy_k4_wraponly_h8_besttrain_20260313/run_260313_182218`. Early checkpoints show `best_train_model` at step 450 (`train_loss≈0.993`) and `best_rollout_model` at step 500 with `success_rate=0.0`; rollout is approaching and forming thumb-only contact (`approach_min_err≈0.019`, `approach_min_rot_err_deg≈2.99`, `best_contacts=2`, `best_fingers=[th]`) but is not yet wrapping/lifting.
314. Oracle replay for the mustard-intent benchmark was previously mismatched to the collector in two ways: (a) sparse 5Hz actions were replayed with zero-order hold instead of the collector's 100Hz linear interpolation between stored samples, and (b) `wrap_and_lift` replay required `hold_contact_ok` while the collector only required the object to stay lifted through the hold phase. On 2026-03-13, `scripts/research/build_mustard_intent_hand_synergy_basis.py` was fixed to interpolate arm/hand commands inside each capture interval and to match the collector's wrap success semantics exactly.
315. After the above replay fixes, `wrap_and_lift` from `dataset/mustard_intent_v5_capture5_y0p12_pushclean_20260311` became fully replay-reproducible: both raw-exact replay (`k=16`, `rmse=0`) and `k=4` replay now achieve `oracle_success_rate=1.0` in `codex/tmp_wrap_replay_check_interp2_20260313/mustard_intent_hand_pca_summary.json`. This establishes that the earlier wrap failures were caused by replay-semantics mismatch rather than lack of representation capacity.
316. `wrap_only + k=4` policy debugging is being tracked in `docs/wrap-k4-debug-notes.md`. Current conclusion: dataset validity and `k=4` replay capacity are no longer the blocker for `wrap`; the remaining issue is policy-side (`wrap_only` Octo has not convincingly overfit yet, and rollout-model selection still needs a better tie-break when success remains zero).
317. Wrap-only k=4 policy debugging on 2026-03-16 isolated an early-phase aliasing issue rather than a data/capacity issue. In run `models/mustard_intent_octo_overfit_synergy_k4_wraponly_h8_besttrain_replayfixed_v2_20260316/run_260316_122218`, the `best_train_model` (step 2400) matches the demonstration's initial near-static settle segment best, while the better closed-loop `best_rollout_model` (step 250) matches a later approach-jump segment better. This indicates that the stored wrap demonstrations contain an ambiguous early segment (~first 6 captured actions near the initial pose), so lower train loss can actually reinforce hover/settle behavior instead of the decisive bottle-approach transition. Reference diagnostic: `codex/logs/json/wrap_k4_step250_vs_besttrain_chunk_diag_20260316.json`.
318. Simple wrap-only dataset prefix trimming is not sufficient to resolve the early-phase aliasing problem. A training-only variant that drops the first 6 captured wrap steps (`dataset/mustard_intent_arm_tcp_hand_synergy_k4_wraponly_v5clean_trim6_20260316`, OXE `dataset/mustard_intent_arm_tcp_hand_synergy_k4_wraponly_v5clean_trim6_oxe_20260316`) failed to improve rollout: step-250 eval from `models/mustard_intent_octo_overfit_synergy_k4_wraponly_h8_trim6_20260316/run_260316_151304/rollout_eval_step_000250.json` showed zero contact. The trimmed first training state/action equals the original demonstration at step 6 exactly, so trimming changes the start-state distribution instead of merely shortening settle time; rollout still begins from the true initial state. Therefore fixing wrap requires recollecting data with a shorter scripted settle (same start-state semantics) or adding explicit phase/progress conditioning, not blind prefix cropping.
319. A bottom-up Octo pipeline audit checklist is maintained at `docs/octo-pipeline-audit-checklist.md`. Use it to distinguish data-integrity, replay-semantics, latent-conversion, OXE-export, training-config, rollout-semantic, and closed-loop robustness failures before drawing conclusions from policy results.
320. On 2026-03-16, `scripts/data/collect_mustard_intent_benchmark.py` was extended with recollection-oriented options for shorter settle and low-frequency corrective arm disturbance: `--settle-steps-override`, `--arm-disturb-xyz`, `--arm-disturb-resample-steps`, and `--arm-disturb-phases`. These keep the true deployment start state while perturbing scripted arm targets only in selected phases.
321. The first wrap-only corrective recollection dataset is `dataset/mustard_intent_wrap_shortsettle_disturb_20260316` (50/50 successes in 65 attempts, `capture_hz=5`, `control_hz=100`, `spawn_pos=[0.78,0.12,0.82]`, `settle_steps=40`, `arm_disturb_xyz=[0.01,0.01,0.005]` during `approach,preshape`). Compared with the earlier wrap split in `dataset/mustard_intent_v5_capture5_y0p12_pushclean_20260311/wrap_and_lift`, the first state/action remains identical across episodes while mean trajectory action distance increases from `0.0108 -> 0.0416`, making it a better candidate for feedback-policy training without changing start-state semantics.
322. Replay sanity on the first corrective wrap recollection revealed one outlier episode (`dataset/mustard_intent_wrap_shortsettle_disturb_20260316/raw/episode_00014.npz`) that fails both raw-exact and `k=4` replay despite being stored as successful. Reference artifacts are `codex/logs/videos/wrap_shortsettle_disturb_episode_00014_dataset_frames_20260316.mp4`, `codex/logs/videos/wrap_shortsettle_disturb_episode_00014_k4_replay_20260316.mp4`, and `codex/logs/json/wrap_shortsettle_disturb_episode_00014_k4_replay_20260316.json`. A cleaned training bundle excluding that episode is `dataset/mustard_intent_wrap_shortsettle_disturb_clean49_bundle_20260316/wrap_and_lift/raw`.
323. The cleaned wrap recollection has verified replay and export parity. Basis summary `dataset/mustard_intent_synergy_basis_wrap_shortsettle_disturb_clean49_20260316/mustard_intent_hand_pca_summary.json` shows `k=4` and raw-exact (`k=16`) both replay at `1.0`. Converted raw dataset is `dataset/mustard_intent_arm_tcp_hand_synergy_k4_wrap_shortsettle_disturb_clean49_20260316`, OXE export is `dataset/mustard_intent_arm_tcp_hand_synergy_k4_wrap_shortsettle_disturb_clean49_oxe_20260316`, and TFDS parity against raw is exact when matching by `source_file`.
324. First clean49 wrap-only Octo retrain is `models/mustard_intent_octo_overfit_synergy_k4_wraponly_shortsettle_disturb_20260316/run_260316_181444`. It uses `window_size=2`, `action_horizon=8`, `action_dim=10`, `learning_rate=5e-5`, `unfreeze_all`, and rollout-based model selection on `wrap_and_lift`. Best rollout remained at step `250` with near-success behavior on 3 eval episodes (`best_contacts 7-9`, `object_dz_max 0.0366-0.1480`) but `success_rate=0.0`; later checkpoints (`500`, `750`) collapsed back to zero-contact behavior. Reference rollout summaries are `.../rollout_eval_step_000250.json`, `.../rollout_eval_step_000500.json`, and `.../rollout_eval_step_000750.json`, with demo video `codex/logs/videos/wrap_shortsettle_disturb_bestroll_demo_20260316/mustard_intent_octo_wrap_and_lift_demo_260316_183206.mp4`.
325. Wrap stutter/backtracking diagnosis on 2026-03-17 is tracked in `codex/logs/json/wrap_stutter_diagnosis_20260317.json`, `wrap_stutter_diagnosis_r20e1_120_20260317.json`, and `wrap_stutter_diagnosis_r20e2_interp_20260317.json`, using helper script `codex/diagnose_mustard_intent_wrap_stutter.py`. The key result is that the clean49 best-rollout checkpoint does cross the lift threshold under fixed-spawn evaluation (`object_dz_max≈0.272`, `crossed_lift_threshold=true`) but still fails because the stable lifted-grasp window only reaches `max_wrap_hold_hits=39`, below the required `150` effective control hits (`1.5s`).
326. The same wrap diagnosis confirmed the user's observed "go back and return" behavior is real late-phase oscillation, not just a rendering illusion. In the tail of the rollout, the policy alternates between two nearby lift targets (roughly a lower target near `[0.814,0.233,1.083]` and a higher target near `[0.820,0.225,1.119]`), while object height oscillates around `0.232 <-> 0.256` m above reference and the hold counter resets. This is the clearest current root cause of wrap near-success failures.
327. Naive rollout-semantics tweaks do not solve the wrap failure. A fairer `execute_actions_per_plan=1` ablation (with doubled policy steps to keep action count comparable) destroys the useful wrap branch entirely (`best_contacts=0`, almost zero lift), while intra-repeat interpolation slightly raises peak lift but still does not stabilize the hold. Remaining wrap failure is therefore best described as late-phase oscillatory action branching rather than raw data corruption, `k=4` capacity failure, or simple threshold mis-setting.
328. On 2026-03-17, late-phase wrap stabilization experiments showed that the oscillation can be neutralized once valid lift+contact is formed. Diagnostic artifacts `codex/logs/json/wrap_stutter_latch_baseline_20260317.json`, `wrap_stutter_latch_dz008_20260317.json`, and `wrap_stutter_latch_dz008_monoz_20260317.json` demonstrate that a `wrap-hold latch` at `dz>=0.08` turns the fixed-spawn wrap case from failure (`max_wrap_hold_hits=20`) into success under both `freeze` and `monotone_z` modes (`max_wrap_hold_hits≈388` and `≈378`, respectively).
329. `scripts/eval/rollout_mustard_intent_octo.py` now supports optional wrap-specific late-phase stabilization with `--wrap-hold-latch-dz` and `--wrap-hold-latch-mode {freeze, monotone_z}`. The `freeze` mode freezes arm/hand commands once lifted wrap contact is established; `monotone_z` instead keeps the hand closed and prevents the commanded lift target `z` from decreasing thereafter.
330. Actual wrap rollout improved measurably after adding the late-phase latch to the real evaluator: jittered 3-episode evaluation `codex/logs/json/wrap_rollout_latch_eval3_20260317.json` reached `1/3` success, and fixed-spawn monotone-z evaluation `codex/logs/json/wrap_rollout_latch_fixedspawn_monoz_eval1_20260317.json` reached `1/1` success. This means the previously diagnosed late-phase oscillation is not only explanatory but also directly actionable in rollout.
331. On 2026-03-17 the workspace was intentionally pruned to reduce artifact sprawl. Under `dataset/`, only the latest directly collected raw dataset `dataset/mustard_intent_wrap_shortsettle_disturb_20260316` is kept. Under `codex/logs/`, only the current wrap-oscillation diagnosis/fix artifacts are kept: `codex/logs/videos/wrap_success_monoz_diag_20260317.mp4` plus the JSONs `wrap_success_monoz_diag_20260317.json`, `wrap_stutter_diagnosis_20260317.json`, `wrap_stutter_strategy_compare_20260317.json`, `wrap_stutter_latch_baseline_20260317.json`, `wrap_stutter_latch_dz008_20260317.json`, `wrap_stutter_latch_dz008_monoz_20260317.json`, `wrap_rollout_latch_eval3_20260317.json`, and `wrap_rollout_latch_fixedspawn_monoz_eval1_20260317.json`.
332. A dedicated comparison between official Octo inference style and the current mustard-intent rollout is documented in `docs/octo-inference-gap-analysis.md`. Current conclusion: the policy backbone is Octo, but the rollout path has drifted into a custom control stack (manual history stacking, manual action queue, internal `policy_repeat`, IK-driven high-rate target holding, and optional wrap-specific latching). This is useful for diagnosis but should not be treated as the final "Octo-only, real-robot-style" inference path.
333. Current mustard-intent OXE export and fine-tuning no longer need to stay language-only. On 2026-03-17, `scripts/data/convert_mustard_raw_to_oxe.py` was extended to export episode-level `goal_image_primary`, and `scripts/train/finetune_mustard_octo.py` was extended with `--task-conditioning {language,goal,language_goal}` so goal-image-conditioned Octo experiments can be run from the same pipeline.
334. Official-style mustard-intent rollout support was added on 2026-03-17 via `scripts/eval/mustard_intent_gym_env.py` and `scripts/eval/rollout_mustard_intent_octo_official.py`. These implement a dataset-rate Gym env and wrapper-based rollout using Octo's `HistoryWrapper`, `RHCWrapper`, and `TemporalEnsembleWrapper`, with optional goal-image task creation.
335. Wrap-only smoke comparisons on 2026-03-17 established that inference semantics matter sharply. With the same `best_rollout_model`, pure official wrapper rollout (`official resize`, history updated per env-step) failed badly, while matching our actual fine-tune preprocessing (`train_resize`) and custom replanning history semantics (`per_plan`) recovered most of the current custom rollout behavior. Reference artifacts: `codex/logs/json/mustard_intent_octo_official_wrap_smoke_20260317.json`, `codex/logs/json/mustard_intent_octo_official_wrap_trainresize_smoke_20260317.json`, `codex/logs/json/mustard_intent_octo_official_wrap_trainresize_perplan_smoke_20260317.json`, `codex/logs/json/mustard_intent_octo_custom_wrap_smoke_20260317.json`, and `codex/logs/octo_official_vs_custom_wrap_compare_20260317.md`.
336. Local Octo OXE config audit on 2026-03-17 found Franka-related dataset entries (`nyu_franka_play_dataset_converted_externally_to_rlds`, `cmu_franka_exploration_dataset_converted_externally_to_rlds`, `furniture_bench_dataset_converted_externally_to_rlds`) but no obvious Franka+Allegro/dexterous-hand entry. Downloaded mirror shards for inspection are kept under `codex/oxe_samples/` (`cmu_franka_exploration_00000.tar`, `nyu_franka_play_00001.tar`).
337. As of 2026-03-17, `scripts/data/convert_mustard_raw_to_oxe.py` no longer keeps legacy export branches. The exporter now uses the official-RLDS-style TFDS builder path as the default and only path: `tfds.rlds.rlds_base.build_info(...)`, `steps: tfds.features.Dataset(...)`, and step-level `language_instruction`. This output is readable by Octo's `dlimp.from_rlds(...)` and `make_dataset_from_rlds(...)`. Audit summary is recorded in `docs/oxe-format-audit.md`.
338. Current official-Octo alignment status on 2026-03-17: raw export is now on the official RLDS path, but `scripts/train/finetune_mustard_octo.py` still contains a custom sample-construction layer (`_make_sample_dataset`) instead of delegating fully to Octo's dataset config / standardize_fn / goal-relabeling path. Treat this trainer-side custom layer as the next primary cleanup target.
339. Current goal-image status on 2026-03-17: `goal_image_primary` is exported and stored in episode metadata, but it is not yet routed through Octo's official goal-conditioned task path. The remaining gap is to promote goal images into `task["image_*"]` through an official-style mustard dataset config / standardization or goal-relabeling transform rather than ad-hoc trainer logic.
340. On 2026-03-17, project-local official-style Octo dataset helpers were added at `octo_data/mustard.py`. `make_mustard_dataset_kwargs(...)` now builds `make_dataset_from_rlds(...)` kwargs for mustard using a standardize_fn (`mustard_dataset_transform`) that exposes `observation.proprio` from the exported `state` field without relying on trainer-side remapping.
341. On 2026-03-17, the official Octo goal-conditioned data path was partially opened for mustard without trainer hacks by using Octo's built-in future-goal relabeling. `make_mustard_goal_trajectory_kwargs(...)` in `octo_data/mustard.py` returns `goal_relabeling_strategy='uniform'`, and smoke verification in `codex/logs/json/mustard_official_dataset_smoke_20260317.json` confirmed that `apply_trajectory_transforms(...)` produces `task.image_primary` and `task.language_instruction` from the official data pipeline. This current goal path uses future observation images, not the exported episode-metadata `goal_image_primary`.
342. Latest verified official-RLDS export artifact on 2026-03-17 is `dataset/mustard_intent_wrap_shortsettle_disturb_20260316_oxe`, built from `dataset/mustard_intent_wrap_shortsettle_disturb_20260316/raw`. Inspection reference is `codex/logs/json/wrap_oxe_dataset_inspect_20260317.json`, and per-episode exported goal-image references are stored under `codex/logs/images/wrap_goal_images_20260317/` with index `codex/logs/json/wrap_goal_image_index_20260317.json`.
343. The latest k4 official-RLDS export for wrap-only training is `dataset/mustard_intent_wrap_shortsettle_disturb_k4_20260317_oxe`, built from `dataset/mustard_intent_wrap_shortsettle_disturb_k4_20260317/raw`. Inspection reference is `codex/logs/json/wrap_k4_oxe_dataset_inspect_20260317.json`, which confirms `action_shape=[10]` (`arm_tcp6 + hand_synergy_k4`), `state_shape=[60]`, and `goal_image_shape=[480,640,3]`.
344. On 2026-03-17, `scripts/train/finetune_mustard_octo.py` was rewritten to follow the official Octo training path instead of the repo's old custom sample-builder path. The current baseline is: load `octo-base-1.5`, build data through `make_single_dataset(...)`, use `octo_data/mustard.py` for dataset kwargs/standardization, use official goal relabeling (`uniform`) inside trajectory transforms, and full-finetune the model on the mustard wrap k4 OXE dataset. Treat this as the canonical Octo training baseline going forward.
345. Smoke verification for the new official Octo training baseline succeeded on 2026-03-17 under `codex/tmp/mustard_octo_official_smoke/run_260317_161406`. A 1-step run completed successfully and saved `best_train_model`, confirming that the official RLDS/OXE mustard dataset can be consumed by Octo's dataset loader and optimizer path without the previous custom trainer-side sample construction.
346. Additional batch-shape verification for the official Octo training baseline is stored at `codex/logs/json/mustard_official_train_batch_summary_20260317.json`. The standardized batch contains `observation.image_primary`, `observation.proprio`, `task.language_instruction`, `task.image_primary` (goal image from official future-goal relabeling), and `action` with shape `[B, window=2, action_horizon=8, action_dim=10]`, which matches the intended wrap-only `arm_tcp6 + hand_synergy_k4` training setup.
347. On 2026-03-17, the official mustard Octo baseline was tightened further: goal conditioning no longer uses future-goal relabeling. Instead, `octo_data/mustard.py` now defines the goal image as the trajectory's final observation frame and attaches it to `task.image_primary` through a dataset-side post-chunk transform, keeping goal-image handling inside the Octo data pipeline rather than trainer logic. Verification artifact: `codex/logs/json/mustard_official_train_batch_summary_goalfinal_20260317.json`.
348. On 2026-03-17, train-side early stopping for `scripts/train/finetune_mustard_octo.py` was relaxed after the first official baseline stopped at step 34. The current baseline waits until at least `1000` steps before considering plateau, and only checks plateau every `50` steps (`train_check_every=50`, `train_patience=20`, `min_steps_before_stop=1000`). A 2-step smoke run with the relaxed baseline succeeded at `codex/tmp/mustard_octo_official_smoke_v4/run_260317_163548`.
349. Official wrap randomization diagnosis on 2026-03-19/20 established that the dominant teacher-vs-rollout image mismatch is a local hand-object interaction patch, not a global camera mismatch. The highest-difference patch stays near image coordinates `x≈120..216`, `y≈72..168` in the 256x256 policy image and moves upward in late lift/hold decisions. Reference artifacts: `codex/logs/json/mustard_official_image_patches_rand_20260320.json` and `codex/logs/images/mustard_official_image_patches_rand_20260320/teacher_rollout_patch_contact_sheet.png`.
350. Safe wrap randomization sweep for corrective recollection on 2026-03-20 verified that `spawn_jitter_xy=0.02m` and `spawn_yaw_jitter_deg=10` remain collectable, and late-phase arm disturbances at `preshape,close` with magnitudes up to `(0.008, 0.008, 0.004)` still produced 5/5 successful pilot episodes. The chosen close-focused corrective disturbance is `arm_disturb_xyz=(0.006, 0.006, 0.003)` applied during `preshape,close`.
351. Two new official-wrap recollection datasets were produced on 2026-03-20. The broad randomized set is `dataset/mustard_intent_wrap_bulk_randxy0p02_yaw10_20260320` with `200/230` successful episodes using `spawn_jitter_xy=0.02`, `spawn_yaw_jitter_deg=10`, `settle_steps=40`, and `arm_disturb_xyz=(0.01,0.01,0.005)` during `approach,preshape`. The close-focused corrective set is `dataset/mustard_intent_wrap_closefocus_randxy0p02_yaw10_20260320` with `100/104` successful episodes using the same spawn randomization and `arm_disturb_xyz=(0.006,0.006,0.003)` during `preshape,close`.
352. Replay-clean training bundles were prepared on 2026-03-20 by dropping exactly one oracle-replay outlier from each new wrap set: bulk excludes `episode_00176` and lives at `dataset/mustard_intent_wrap_bulk_randxy0p02_yaw10_clean199_bundle_20260320`, while close-focus excludes `episode_00060` and lives at `dataset/mustard_intent_wrap_closefocus_randxy0p02_yaw10_clean99_bundle_20260320`. Rebuilt k4 bases `dataset/mustard_intent_wrap_bulk_randxy0p02_yaw10_clean199_k4_basis_20260320` and `dataset/mustard_intent_wrap_closefocus_randxy0p02_yaw10_clean99_k4_basis_20260320` both reached oracle replay `1.0`.
353. Official RLDS/OXE exports for the new replay-clean wrap k4 datasets were completed on 2026-03-20 and are ready for Octo training: `dataset/mustard_intent_wrap_bulk_randxy0p02_yaw10_clean199_k4_20260320_oxe` (`199` episodes, `32` TFRecord shards) and `dataset/mustard_intent_wrap_closefocus_randxy0p02_yaw10_clean99_k4_20260320_oxe` (`99` episodes, `16` TFRecord shards). Inspection references `codex/logs/json/wrap_bulk_k4_oxe_dataset_inspect_20260320.json` and `codex/logs/json/wrap_closefocus_k4_oxe_dataset_inspect_20260320.json` confirm `81` steps per episode, `action_shape=[10]`, `state_shape=[60]`, `image_shape=[480,640,3]`, and episode-level `goal_image_primary`.
354. The bulk and close-focused wrap sets cannot be merged directly at the latent level because their first k4 conversions used different PCA bases. On 2026-03-20 a shared replay-clean merged bundle was therefore created first at the raw-action level: `dataset/mustard_intent_wrap_bulkclose_clean298_bundle_20260320`, then one more replay outlier (`episode_00075` in merged numbering) was removed to produce `dataset/mustard_intent_wrap_bulkclose_clean297_bundle_20260320`. The rebuilt shared basis `dataset/mustard_intent_wrap_bulkclose_clean297_k4_basis_20260320` reached oracle replay `1.0`.
355. The canonical merged official-Octo wrap training input as of 2026-03-20 is `dataset/mustard_intent_wrap_bulkclose_clean297_k4_20260320_oxe`, built from `dataset/mustard_intent_wrap_bulkclose_clean297_k4_20260320/raw` with the shared basis above. Inspection reference `codex/logs/json/wrap_bulkclose_k4_oxe_dataset_inspect_20260320.json` confirms `297` episodes, `81` steps, `action_shape=[10]`, `state_shape=[60]`, and episode-level `goal_image_primary`. Official Octo training on this merged dataset was started under `models/mustard_octo_official_wrap_k4_bulkclose_20260320`.
356. A multi-GPU validation bug was fixed on 2026-03-20 in `scripts/train/finetune_mustard_octo.py`: when `batch_size=30` on 2 GPUs, the validation iterator previously emitted a last partial batch of size `15`, which caused JAX sharding failure at step 500 (`dimension size 15 not divisible by 2`). Train/example/validation batching now uses `drop_remainder=True` so official 2-GPU runs do not crash on remainder batches.
357. On 2026-03-20, official Octo training gained explicit run-state logging in `scripts/train/finetune_mustard_octo.py`. Every run now writes `run_status.json` in the run directory with `state in {starting,running,finished,failed}`, current `step`, `pid`, timestamps, and latest best-train / best-rollout info. `latest_metrics.json` is also updated during training, and `train_summary.json` is now written on both success and failure paths so progress can be checked without relying on a background terminal session.
358. A wrap-specific DAgger-lite corrective collector was added on 2026-03-20 at `scripts/data/collect_mustard_intent_dagger_wrap.py`. It follows the current official Octo wrap rollout path (`language_goal`, official resize/history, `RHCWrapper`) until the policy reaches a `reached=true, best_contacts=0` failure state, then switches to scripted expert recovery (`recenter -> close -> lift`) and saves only successful corrective segments in the standard raw mustard-intent NPZ format plus extra `dagger_*` handoff metadata (`qpos`, `qvel`, command state, and policy summary).
359. The first DAgger-lite wrap pilot dataset is `dataset/mustard_intent_wrap_daggerlite_pilot5_20260320`. Using `models/mustard_octo_official_wrap_k4_bulkclose_20260320/run_260320_141029/best_rollout_model` and basis `dataset/mustard_intent_wrap_bulkclose_clean297_k4_basis_20260320/mustard_intent_hand_pca_k4.npz`, the collector saved `5/5` corrective episodes from policy handoff step `30`. In all five cases the policy handoff state had `reached=true`, `best_contacts=0`, and `approach_min_err≈0.023-0.030`, while the scripted recovery succeeded with `best_contacts=7-8`, `best_fingers=ff,mf,rf,th`, and `object_dz_max≈0.226-0.242`. The saved corrective raw episodes are variable-length (`40` captured steps, `action_dim=23`) and pass the existing raw->OXE dry-run scan unchanged.
360. A larger wrap DAgger-lite corrective dataset was collected on 2026-03-20 at `dataset/mustard_intent_wrap_daggerlite50_20260320` using the same official-wrap best-rollout policy and shared k4 basis. It saved `50/50` corrective episodes (`handoff_successes=50`), all from genuine `reached=true, best_contacts=0` policy states that were successfully recovered by the scripted expert.
361. To test DAgger-lite data without changing the latent coordinate system, the existing replay-clean base wrap bundle `dataset/mustard_intent_wrap_bulkclose_clean297_bundle_20260320` was merged with the new DAgger raw episodes at the raw-action level into `dataset/mustard_intent_wrap_bulkclose_plusdagger50_bundle_20260320` (`297 + 50 = 347` episodes). This merged raw bundle was converted to k4 using the already-verified shared basis `dataset/mustard_intent_wrap_bulkclose_clean297_k4_basis_20260320/mustard_intent_hand_pca_k4.npz`, producing `dataset/mustard_intent_wrap_bulkclose_plusdagger50_k4_20260320` with `action_dim=10` and essentially zero hand reconstruction error.
362. The corresponding official RLDS/OXE export for the DAgger-augmented wrap dataset completed on 2026-03-20 at `dataset/mustard_intent_wrap_bulkclose_plusdagger50_k4_20260320_oxe` (`32` TFRecord shards). This is the current canonical official-Octo training input for testing whether DAgger-lite corrective data helps the wrap close-branch failure.
363. Official Octo training on the DAgger-augmented wrap dataset was started on 2026-03-20 under `models/mustard_octo_official_wrap_k4_bulkclose_plusdagger50_20260320/run_260320_161719` with `octo-base-1.5`, the shared basis `dataset/mustard_intent_wrap_bulkclose_clean297_k4_basis_20260320/mustard_intent_hand_pca_k4.npz`, and rollout goal episode `dataset/mustard_intent_wrap_bulkclose_plusdagger50_k4_20260320/raw/episode_00000.npz`. The training run uses the new `run_status.json` / `latest_metrics.json` progress reporting and was confirmed to enter the train loop successfully (`[train] start`, first best-train saves emitted).
364. The first DAgger-augmented official wrap training result (2026-03-20) finished at `models/mustard_octo_official_wrap_k4_bulkclose_plusdagger50_20260320/run_260320_161719` after `2450` steps with early stop. It slightly improved the best rollout score (`60.7872`, compared with `60.7665` for the previous bulk+close run and `60.7822` for the randomized run), but rollout behavior remained qualitatively unchanged: `reached=true`, `best_contacts=0`, and `object_dz_max≈0` at both step-1000 and step-2000 evaluations. Follow-up diagnostics (`codex/logs/json/mustard_official_teacher_vs_rollout_dagger50_20260320.json`, `codex/logs/json/mustard_official_modality_gap_dagger50_20260320.json`) showed that the model still predicts close-hand behavior under teacher forcing while official closed-loop rollout remains near the open-hand branch, so simply adding DAgger-lite corrective segments has not yet solved the official close-phase transition failure.
365. A late-phase pause/small-action audit of the merged DAgger-augmented wrap dataset was added at `codex/audit_mustard_wrap_pause_actions.py`, with results stored in `codex/logs/json/mustard_wrap_pause_audit_bulkclose_plusdagger50_20260320.json`. The key finding is that `close` is not dominated by pauses (`quiet_fraction≈0.0447`, quiet runs are almost always single-step), `lift` is consistently moving (`quiet_fraction=0`), but `lift_hold` is highly static (`quiet_fraction≈0.857`, typical quiet run length `6` captured steps). Because the interface uses absolute commands, this audit is based on *successive action deltas* rather than raw action magnitudes.
366. Official rollout chunk-correction was tested on 2026-03-20 using Octo's built-in `TemporalEnsembleWrapper` versus the current `RHCWrapper`, both under the same official conditioning path on the DAgger50 model. Comparison artifacts are `codex/logs/json/mustard_octo_official_wrap_dagger50_temporal_eval3_20260320.json` and `codex/logs/json/mustard_octo_official_wrap_dagger50_rhc_eval3_20260320.json` (plus recorded single-episode temporal run `codex/logs/json/mustard_octo_official_wrap_dagger50_temporal_eval1_record_20260320.json`). Temporal ensembling did not help: both wrappers stayed at `0/3` success with `best_contacts=0`, and temporal was slightly worse on approach error (`0.0243m` vs `0.0206m`) and rotation error (`2.12deg` vs `1.24deg`).
367. A wider-history official Octo baseline was started on 2026-03-20 under `models/mustard_octo_official_wrap_k4_bulkclose_plusdagger50_w4_20260320/run_260320_171924` with `window_size=4`. Memory probing showed `window_size=4, batch_size=20` OOMs during the first train step (attention allocation ~25.8GiB temp per replica), so the current safe launch uses `batch_size=8`. A smoke compile at `codex/tmp/mustard_octo_w4_smoke_b8/run_260320_171607` confirmed the `w=4` path can initialize and enter `[train] start` without immediate OOM.
368. A repo-specific Codex skill was added on 2026-03-20 at `~/.codex/skills/mustard-octo-official`. It is intended for repeated official-mustard workflows in this repository: RLDS/OXE export, k4 dataset prep/merging, official Octo training, official rollout evaluation, DAgger-lite corrective collection, and run-status inspection. The skill was validated with the built-in skill validator and includes a concise `references/workflow.md` command reference plus `agents/openai.yaml` metadata for UI invocation (`$mustard-octo-official`).
369. On 2026-03-23, the workflow gained a repo-local daily handoff log under `handoff/`. Future prompts should keep appending current-state summaries, key results, and next actions to the date-stamped file `handoffMMDD.md` so experiment continuity does not depend on scrolling old chat context.
370. On 2026-03-23, a first-stage external `aux_close` prototype was added for the official wrap failure analysis without changing the main Octo policy interface. New prototype artifacts are `codex/wrap_aux_close_common.py`, `codex/train_wrap_aux_close.py`, `codex/eval_wrap_aux_close_gate.py`, and the report `docs/wrap-aux-close-stage1-report-20260323.md`. The aux model is trained from official-style resized/cropped image history (`window_size=4`, `16x16` grayscale per frame) to predict binary `phase>=3`, with trained weights stored at `codex/models/wrap_aux_close_imghist_w4_20260323/aux_close_model.npz` and summary metrics at `codex/models/wrap_aux_close_imghist_w4_20260323/summary.json`.
371. The first `aux_close` prototype results on 2026-03-23 show that the architecture is viable but not yet sufficient for full success. Offline fit is strong (`val F1≈0.9968`), and on raw teacher data the aux score rises sharply near close onset. However, in official rollout the raw learned threshold (`0.4`) is over-conservative because rollout scores are compressed (`max≈0.103`), so the gate never activates (`codex/logs/json/wrap_aux_close_gate_eval3_20260323.json`). After manual calibration to `threshold=0.08`, moderate hand blending (`blend=0.65`, `commit=3`) recovered nonzero wrap contact (`best_contacts=4`, `best_fingers=['mf','rf','th']`) in `codex/logs/json/wrap_aux_close_gate_eval1_thr008_20260323.json`, while a stronger correction (`blend=0.85`, `commit=5`) regressed to `best_contacts=0` in `codex/logs/json/wrap_aux_close_gate_eval1_thr008_b085_c5_20260323.json`. Treat this as the first evidence that an external close-entry signal plus gated hand correction can break the pure `contact=0` failure mode, with calibration and residual design now the next bottlenecks.
372. On 2026-03-23, the aux-close target was refined from the broad label `phase>=3` to a dedicated `close_entry_window` target in `codex/train_wrap_aux_close.py`. The new mode marks a short window around close entry (`first_phase3 - 5` through `first_phase3 + 2`) and is trained under `--target-mode close_entry_window`. The resulting model `codex/models/wrap_aux_close_entry_imghist_w4_20260323/aux_close_model.npz` has lower but still strong offline fit (`val F1≈0.9464`, threshold `0.3`), and most importantly it triggers materially earlier on teacher data (`timestep 39` instead of `45` for `episode_00000`).
373. The same close-entry aux model also improves rollout-side signal calibration without any manual threshold override. In the pure-signal diagnostic `codex/logs/json/wrap_aux_close_entry_signal_eval1_20260323.json` (run with `aux_blend=0` so behavior is unchanged), the rollout `aux_score_max` rises to `≈0.647` and naturally crosses its learned threshold at steps `14..17`. This is a major improvement over the earlier `phase>=3` aux model, whose rollout score only reached `≈0.103` and never triggered under its learned threshold. Treat this as evidence that the previous bottleneck was partly caused by an overly late target definition; the next bottleneck is now downstream hand-correction design, not signal timing.
374. On 2026-03-23, a hybrid aux-close diagnostic showed that the improved close-entry signal was not the only ingredient needed; the correction prior mattered critically. The hybrid artifact `codex/models/wrap_aux_close_entrysignal_oldprior_hybrid_20260323/aux_close_model.npz` reuses the newer close-entry detector but swaps in the older stronger `phase>=3` close prior. Under the standard official wrap benchmark this hybrid consistently breaks the pure no-contact failure mode: `codex/logs/json/wrap_aux_close_entrysignal_oldprior_blend_eval3_20260323.json` reached `best_contacts=6`, `best_fingers=['ff','mf','rf','th']`, and `object_dz_max≈0.0343` across all 3 episodes, even though strict success remained `0/3`.
375. Additional continuation diagnostics on 2026-03-23 narrowed the remaining failure after the hybrid trigger/prior fix. A traced run at `codex/logs/json/wrap_aux_close_entrysignal_oldprior_trace_20260323.json` showed aux trigger at decisions `14..17`, first real contact only around decision `23`, and strongest wrap/lift interaction around decisions `27..30`, reaching `best_contacts=8` and `object_dz_max≈0.0646`. A simple post-contact hand-hold heuristic added to `codex/eval_wrap_aux_close_gate.py` (`--contact-hold-*` options) did not help: `codex/logs/json/wrap_aux_close_entrysignal_oldprior_hold_eval1_c3d6_b035_20260323.json` improved contact count to `7` but reduced lift to `object_dz_max≈0.0094`, indicating that blindly holding the hand more closed after contact tends to pin or over-constrain the object instead of stabilizing lift.
376. Increasing the rollout budget after the hybrid fix showed that the remaining issue is no longer pure close entry and not simply lack of time. With the same hybrid trigger/prior and no post-contact hold, `codex/logs/json/wrap_aux_close_entrysignal_oldprior_blend_eval1_steps80_20260323.json` stayed at `best_contacts=6`, `object_dz_max≈0.0343`, but `codex/logs/json/wrap_aux_close_entrysignal_oldprior_blend_eval1_steps100_20260323.json` reached `best_contacts=9`, `best_fingers=['ff','mf','rf','th']`, and `object_dz_max≈0.1922` while still failing strict success because `wrap_hold_hits=0`. Treat the current bottleneck as **stable contact-preserving lift/hold after trigger and initial wrap are already working**, not as a pure close-branch activation failure anymore.
377. On 2026-03-24, a presentation-ready qualitative rollout video for the promising `steps100` hybrid setting was recorded at `codex/logs/videos/wrap_aux_close_entrysignal_oldprior_steps100_20260324.mp4`, with paired summary `codex/logs/json/wrap_aux_close_entrysignal_oldprior_blend_eval1_steps100_record_20260324.json`. That recorded run reached `best_contacts=9`, `best_fingers=['ff','mf','rf','th']`, and `object_dz_max≈0.1922`, yet still ended with `wrap_hold_hits=0` and `success=false`, confirming that the system can now wrap and lift substantially without satisfying the strict sustained-hold success criterion.
378. A dedicated continuation-analysis helper was added on 2026-03-24 at `codex/analyze_wrap_hold_failure.py`, with reference output `codex/logs/json/wrap_hold_failure_analysis_steps100_20260324.json`. The traced analysis run showed `hold_required_steps=150`, `first_lifted_decision=null`, `first_hold_eligible_decision=null`, and `max_consecutive_hold_eligible_decisions=0`, while the separate recorded run under the same nominal controller reached high lift (`dz≈0.1922`). Treat the current post-trigger failure as **contact-dynamics-sensitive continuation**: the policy/controller is now close enough that some runs achieve strong wrap+lifts, but the strict hold condition is not robustly maintained, and even nominally identical runs can diverge between moderate lift (`dz≈0.0343`) and strong lift (`dz≈0.1922`).
379. On 2026-03-24, `codex/eval_wrap_aux_close_gate.py` gained a `--save-trace-json` option so the exact hybrid rollout code path can emit per-decision continuation diagnostics (contacts, touched fingers, force, object lift `dz`, `lifted`, `contact_meets`, `hold_eligible`, and `wrap_hold_hits`) without relying on a separate analysis controller loop. The first direct trace run is `codex/logs/json/wrap_aux_close_entrysignal_oldprior_blend_trace_steps100_20260324.json`, paired with summary `codex/logs/json/wrap_aux_close_entrysignal_oldprior_blend_eval1_steps100_trace_20260324.json`.
380. The first exact-code-path continuation trace on 2026-03-24 did not reproduce the earlier high-lift recorded path, which itself is an important result. In that traced run, `contact_meets` became true only intermittently (first at decision `26`, later again at `31`, `37`, and `50`), but `lifted` was never true because `object_dz_max` stayed at `≈0.0343 < 0.08`; therefore `hold_eligible` never became true and `wrap_hold_hits` stayed zero. Combined with the separate recorded run that reached `dz≈0.1922`, the current interpretation is that the same nominal hybrid setting is already in a contact-dynamics-sensitive regime where continuation outcomes diverge substantially between runs.
381. Interpretation note for auxiliary-wrap videos as of 2026-03-24: the current recorder in `codex/eval_wrap_aux_close_gate.py` appends one frame only after each outer `env.step(...)`, while `scripts/eval/mustard_intent_gym_env.py` executes `policy_repeat=20` internal control iterations per policy step (`control_hz=100`). Therefore saved videos for this path are effectively decision-rate (~5 Hz) snapshots and visually exaggerate jerkiness. Do not use these clips alone to judge inner-loop smoothness. However, whole-arm tilt / spin during lift is still a real continuation instability, not just a recording artifact, because the policy updates coarse absolute arm targets at the policy rate and the object remains only partially stabilized during lift.
382. On 2026-03-24, a reusable seed-sweep helper for the current hybrid wrap controller was added at `codex/sweep_wrap_aux_close_success_seed.py`. It launches `codex/eval_wrap_aux_close_gate.py` in a subprocess for each seed, logs compact per-seed status lines (`seed N - 성공/실패 | contacts=... fingers=... dz=... hold=... aux=...`), stores per-seed summaries under `codex/logs/json/wrap_aux_seed_sweep_<timestamp>/seed_XXXX.json`, writes a master summary `codex/logs/json/wrap_aux_seed_sweep_<timestamp>.json`, and stops at the first strict success by default. The baked-in canonical target is the current hybrid aux-close wrap setting (`best_rollout_model` from `run_260320_171924`, shared k4 basis, and `wrap_aux_close_entrysignal_oldprior_hybrid_20260323`).
383. On 2026-03-24, a raw-data sanity montage was created for the current hybrid-wrap training set. `codex/render_wrap_raw_montage.py` renders a 2x5 grid video directly from `dataset/mustard_intent_wrap_bulkclose_plusdagger50_k4_20260320/raw`. The first reference artifact is `codex/logs/videos/wrap_bulkclose_plusdagger50_train10_montage_20260324.mp4` with preview PNG and JSON manifest of selected episode indices. A direct raw metadata scan confirmed `347/347` episodes in that training set have `success=True`, so the current wrap failure should not be explained simply as training on obviously failed or non-lifting demonstrations.
384. On 2026-03-24, `scripts/data/collect_mustard_intent_benchmark.py` changed lift behavior for the lift tasks so that once a grasp is acquired, the collector latches the actually achieved palm pose and raises from that pose using world-z-only translation (`lift_target_mode="latched_palm_z_up"`) instead of lifting from the nominal `interact_target` pose. This is intended to reduce XY/orientation correction during the raise and make wrap/pinch collection more strictly z-up from the realized grasp pose. Smoke validation at `codex/tmp_wrap_zup_latched_smoke` succeeded (`1/1` save, `best_contacts=9`, `object_dz_max≈0.2944`).
385. On 2026-03-24, the first latched-z-up lift change was judged insufficient by visual inspection (the bottle still appeared to roll/tilt noticeably during lift), so `scripts/data/collect_mustard_intent_benchmark.py` was refined again to steer the wrist toward an upright lift orientation during the raise. The collector now uses `lift_target_mode="latched_palm_z_up_upright_interp"`: it latches the achieved palm pose for position, then interpolates wrist orientation from the realized grasp pose toward an upright object-frame lift pose while raising in world z. Validation artifacts are `codex/tmp_wrap_zup_upright_record/collection_summary.json`, `codex/logs/videos/mustard_intent_wrap_and_lift_latched_zup_upright_demo_20260324.mp4`, and `codex/logs/json/wrap_zup_upright_compare_20260324.json`. Compared with the earlier latched-z-up version, this reduced lift-phase object tilt mean from about `82.64 deg` to `53.43 deg` and hold-phase tilt mean from about `80.66 deg` to `16.06 deg` while preserving successful collection (`best_contacts=9`, `object_dz_max≈0.2703`).
386. A same-day upright-lift sweep established a better default orientation schedule for collection. `scripts/data/collect_mustard_intent_benchmark.py` now exposes `--lift-upright-rot-fraction` and `--lift-upright-rot-power` so rotation-uprighting speed can be tuned separately from lift height. A 1-episode grid search over `{(1.0,1.0),(0.35,0.5),(0.25,0.5),(0.20,0.5)}` is recorded at `codex/logs/json/wrap_upright_grid_20260324.json`. All four settings still collected successful episodes with `best_contacts=9` and `object_dz_max≈0.267..0.270`, but the faster-upright schedules materially reduced lift-phase tilt. The chosen new default is `lift_upright_rot_fraction=0.25`, `lift_upright_rot_power=0.5`, which achieved `lift_tilt_mean≈32.39 deg` and `hold_tilt_mean≈16.82 deg`, substantially straighter than the original linear-upright schedule.
387. On 2026-03-24, further debugging showed the remaining “90-degree tip then recover” transient was caused by starting upward motion and upright reorientation at the same time. `scripts/data/collect_mustard_intent_benchmark.py` now adds a post-grasp stabilization stage controlled by `--post-grasp-upright-steps` (default `40`): after `grasp_acquired`, the collector first keeps the palm position fixed and rotates the wrist toward the upright lift pose, then begins the z-up raise from that stabilized orientation. It also removes the extra `close_z_clearance` jump from the post-grasp lift path itself. Validation artifacts are `codex/tmp_wrap_zup_upright_stabilized_smoke/collection_summary.json`, `codex/logs/json/wrap_upright_stabilized_compare_20260324.json`, and `codex/logs/videos/mustard_intent_wrap_and_lift_latched_zup_upright_stabilized_rawdemo_20260324.mp4`. Compared with the previous best `(rot_fraction=0.25, rot_power=0.5)` collector, the stabilized version reduced lift-start max tilt from about `79.17 deg` to `60.60 deg` and lift-phase tilt mean from about `32.39 deg` to `25.01 deg` while preserving successful collection (`best_contacts=9`, `success=1`).
388. A frame-level audit on 2026-03-24 corrected the interpretation of the remaining bottle-tip problem. The user requested direct inspection of the `5s..7s` window, and `codex/tmp_video_inspect_0324_stabilized_5to7/contact_sheet_5s_7s.png` plus `codex/logs/json/wrap_stabilized_5to7_frames_20260324.json` showed that the most severe tilt still occurs in late `close` (`phase=3`), not in the later `lift` phase alone: e.g. `6.2s -> 61.68 deg`, `6.4s -> 83.54 deg`, `6.6s -> 67.57 deg`, only recovering after the lift phase begins (`6.8s -> 50.16 deg`, `7.0s -> 25.43 deg`). Therefore the remaining collector issue should now be treated as a **late-close / grasp-acquisition stability problem**, not just a post-grasp lift-continuation problem.
389. A follow-up debug on 2026-03-24 found a concrete close-phase control bug in `scripts/data/collect_mustard_intent_benchmark.py`: during `wrap_and_lift` / `pinch_and_lift`, the main interaction loop kept running through the rest of `interaction_steps` even after `grasp_acquired=True`, so the scripted close policy continued to squeeze/push the bottle after a stable grasp had already been detected. This was the main cause of the large late-close bottle tip. The collector now breaks the interaction loop immediately on grasp acquisition and skips the separate `close_hold_steps` loop when grasp has already been achieved. Combined with the retract-first post-grasp lift path, this materially improved the exact `5s..7s` user-audited window: `codex/logs/json/wrap_breakclose_5to7_frames_20260324.json` and `codex/tmp_video_inspect_0324_breakclose_5to7/contact_sheet_5s_7s.png` show worst tilt reduced from about `83.54 deg` to `35.38 deg` and mean tilt over that window reduced from about `32.56 deg`/`35.07 deg`-class behavior to `20.58 deg`. Reference raw demo after the fix: `codex/logs/videos/mustard_intent_wrap_and_lift_breakclose_rawdemo_20260324.mp4`. Future collector metrics now label this lift path as `lift_target_mode="latched_palm_retract_then_upright_z_up"`.
390. On 2026-03-24, `scripts/data/collect_mustard_intent_benchmark.py` gained approach early-exit so scripted collection does not spend unnecessary captured frames idling near the same approach target. New knobs are `--approach-early-exit-pos-err` (default `0.01 m`), `--approach-early-exit-rot-err-deg` (default `2.0 deg`), and `--approach-early-exit-stable-steps` (default `3` captured frames). Validation at `codex/tmp_wrap_approachexit_smoke/collection_summary.json` showed the captured `approach` segment for `wrap_and_lift` dropped from `35` frames to `10` frames (`approach_steps_used=191` out of a `700`-step budget) while preserving success, contact quality, and lift height (`success=1`, `best_contacts=7`, `object_dz_max≈0.1929`). This early-exit path is now the preferred default for cleaner recollection data.
391. On 2026-03-24, the next large wrap recollection plan was fixed at `500` episodes split into three buckets for balance rather than a single distribution: `100` clean anchor episodes, `250` bulk randomized episodes, and `150` close-focused episodes. The randomized buckets keep the current validated noise settings: bulk uses `spawn_jitter_xy=0.02`, `spawn_yaw_jitter_deg=10`, `settle_steps_override=40`, `arm_disturb_xyz=(0.01,0.01,0.005)`, `arm_disturb_phases=approach,preshape`; close-focused uses the same spawn jitter plus `arm_disturb_xyz=(0.006,0.006,0.003)`, `arm_disturb_phases=preshape,close`. This 500-plan should always run on top of the current collector fixes (break-on-grasp, retract-then-upright world-z lift, and approach early-exit), not on the older noisier lift collector.
392. The canonical orchestration entrypoint for that 500-episode run is `codex/run_wrap500_collect_and_train_20260324.sh`. It collects the three buckets in parallel under `handvla`, merges them into `dataset/mustard_intent_wrap500_mix_bundle_20260324`, builds a shared k4 hand basis at `dataset/mustard_intent_wrap500_mix_k4_basis_20260324`, converts to `dataset/mustard_intent_wrap500_mix_k4_20260324`, exports official OXE at `dataset/mustard_intent_wrap500_mix_k4_20260324_oxe`, and then launches official Octo training (current default `window_size=4`) under `models/mustard_octo_official_wrap500_mix_k4_20260324`. The long-running canonical tmux session for this run is `wrap500_mix_0324`, and its master progress log is `codex/logs/wrap500_mix_20260324/master.log`.
393. The first real execution of the 500-episode wrap recollection pipeline completed the collection stage successfully on 2026-03-24. Final bucket summaries are: clean `dataset/mustard_intent_wrap500_clean100_zupfix_20260324` (`100/100` in `100` attempts), bulk `dataset/mustard_intent_wrap500_bulk250_zupfix_20260324` (`250/250` in `271` attempts), and close-focused `dataset/mustard_intent_wrap500_close150_zupfix_20260324` (`150/150` in `157` attempts). These summaries confirm the current fixed collector path was used throughout (`lift_target_mode="latched_palm_retract_then_upright_z_up"` plus approach early-exit), so treat this 500-episode set as the new canonical recollection candidate for the next wrap-training round.
394. The chained official training that followed that 500-episode recollection failed immediately from memory pressure, not data quality. The first launched run is `models/mustard_octo_official_wrap500_mix_k4_20260324/run_260324_161348`, and both `run_status.json` and `train_summary.json` record `state="failed"` / `finished=false` at `step=1` with `XlaRuntimeError: RESOURCE_EXHAUSTED` while trying to allocate about `41.8 GB`. The failure happened in the `window_size=4` official Octo train step, so the next action for this dataset should be a smaller-batch relaunch rather than another recollection pass.
395. The immediate memory-mitigation relaunch for the same 500-wrap dataset is `batch_size=8` with `window_size=4` instead of the previous `batch_size=30` launch. The canonical relaunch helper is `codex/run_wrap500_train_b8_w4_20260324.sh`, and the corresponding tmux session is `wrap500_train_b8_0324`. The current relaunch run directory is `models/mustard_octo_official_wrap500_mix_k4_20260324/run_260324_182938`. At the last checked point it had cleared setup, dataset loading, and pretrained model loading and remained in the long initialization/JAX compile stage without immediately reproducing the earlier OOM, so this is the current active training attempt for the 500-episode wrap mix.
396. The `batch_size=8`, `window_size=4` relaunch on the 500-episode wrap mix completed successfully enough to remove the memory bottleneck. Final run: `models/mustard_octo_official_wrap500_mix_k4_20260324/run_260324_182938`. Final summary: `finished=true`, `num_steps_ran=2150`, `stopped_early=true`, `best_train_step=1127`, `best_train_loss≈0.64299`, `best_rollout_step=2000`, `best_rollout_score≈217.75849`, and `success_rate=0.0`. This means the smaller-batch relaunch is the new canonical training result for the 500-wrap dataset: infrastructure is now working, but official rollout behavior is still failing qualitatively despite the larger recollection.
397. Interpretation of the 500-wrap official result (run `260324_182938`) must not rely on `best_rollout_score` alone. The rollout-score helper in `scripts/train/finetune_mustard_octo.py` rewards `best_contacts * 100` and `object_dz * 10000` even when `success=false`, so the apparent jump from the older `w=4` score (`≈60.79`) to the new 500-wrap score (`≈217.76`) is mostly due to incidental contact, not actual task completion. Qualitatively the 500-wrap policy is not better: at `step 2000` it still has `success=0`, and compared with the older `w=4` run it actually regressed in arm approach (`reached=false`, `approach_min_err≈0.0799m` vs the older `reached=true`, `approach_min_err≈0.0198m`) while only adding minor thumb-only contact (`best_contacts=2`, `best_fingers=['th']`, negligible `object_dz`). Offline validation improved (`eval@2000 ≈ 1.383` vs the older `≈1.663`), so the larger recollection helps the supervised objective but not the official closed-loop behavior yet.
398. Goal-image conditioning is still active in the current official wrap training path and should not be blamed for the 500-wrap arm-approach regression. The exporter writes `goal_image_primary`, `octo_data/mustard.py` moves it into `task.image_primary`, `scripts/train/finetune_mustard_octo.py` loads camera views `("primary","goal")`, and the saved model configs for run `260324_182938` show the `primary` image tokenizer with `task_stack_keys=['image_primary']`. For the 500-wrap run, the more likely explanation for the visibly off-front alignment is the combination of (a) heavier randomized/disturbed training mix and (b) checkpoint selection by the contact-heavy auxiliary rollout score, which can prefer thumb-touching/no-reach checkpoints over cleaner reaching checkpoints.
399. For interactive/real-robot-style official Octo rollout, prefer the official wrapper semantics: `HistoryWrapper -> TemporalEnsembleWrapper`, low-frequency outer policy steps (`~0.2s`, e.g. `policy_repeat=20` at `control_hz=100`), and separate high-rate low-level servo. Viewer smoothness should be treated as a display concern, not a change to policy semantics.
400. `scripts/eval/run_mustard_intent_octo_interactive.py` now defaults to Octo-like execution semantics: `TemporalEnsembleWrapper`, wall-clock outer-step pacing of `0.2s`, and optional per-inner-control-step viewer sync. `scripts/eval/mustard_intent_gym_env.py` supports a viewer sync callback so display smoothness can improve without changing policy semantics. `scripts/eval/rollout_mustard_intent_octo_official.py` also now defaults to `wrapper=temporal`. These are inference/runtime changes only and do not require retraining.
401. Heavy artifact cleanup on 2026-03-25 standardized the repository around the current wrap500 pipeline only. Keep the canonical dataset set (`mustard_intent_wrap500_clean100_zupfix_20260324`, `...bulk250...`, `...close150...`, `...mix_bundle...`, `...mix_k4_basis...`, `...mix_k4...`, `...mix_k4_oxe...`) and the canonical model root `models/mustard_octo_official_wrap500_mix_k4_20260324`. Older wrap variants, older official-wrap model roots, old pick-and-lift model roots, temporary `codex/models`, and `codex/tmp*` artifacts were intentionally removed to reduce clutter and disk usage.
402. `scripts/train/finetune_mustard_octo.py` now treats validation as the primary checkpoint-selection signal for official wrap fine-tuning. New runs save `best_val_model` on validation improvement and use validation plateau for early stopping when eval is enabled; train-loss best remains a tracked metric only. Continuing wrap500 training should currently start from `models/mustard_octo_official_wrap500_mix_k4_20260324/run_260324_182938/best_train_model` until a `best_val_model` exists from the new regime.
403. Training-time rollout probes for official wrap fine-tuning are now configurable and should no longer be hardcoded to `rhc`. `scripts/train/finetune_mustard_octo.py` exposes `--rollout-wrapper`, `--rollout-exec-horizon`, `--rollout-history-mode`, `--rollout-conditioning`, `--rollout-resize-mode`, `--rollout-task-builder`, `--rollout-spawn-jitter-xy`, and `--rollout-spawn-yaw-jitter-deg`. For current wrap500 continuation runs, prefer `rollout-wrapper=temporal` with small object jitter so `best_rollout_model` selection matches the intended deployment semantics more closely.
404. The current temporal-aligned continuation launcher is `codex/run_wrap500_continue_temporalrollout_20260325.sh`. It starts from `models/mustard_octo_official_wrap500_mix_k4_continue_20260325/run_260325_151712/best_val_model`, saves under `models/mustard_octo_official_wrap500_mix_k4_temporalcontinue_20260325`, uses `batch_size=8`, `window_size=4`, `num_steps=3000`, and evaluates rollout every `500` steps with `wrapper=temporal`, `episodes=3`, `spawn_jitter_xy=0.02`, and `spawn_yaw_jitter_deg=10`. The corresponding tmux session is `wrap500_continue_temporalrollout_0325`.
405. Temporal-aligned continuation run `models/mustard_octo_official_wrap500_mix_k4_temporalcontinue_20260325/run_260325_161632` finished on 2026-03-25 with improved offline fit (`best_train_loss≈0.3384`, `best_val_loss≈0.7574` at step `1750`) and a higher temporal rollout score (`best_rollout_step=2000`, `score≈1027.13`), but still `success_rate=0.0`. The best temporal rollout probe at step `2000` achieved one near-success out of three jittered episodes: `reached=true`, `best_contacts=8`, `best_fingers=['ff','mf','rf','th']`, and `object_dz_max≈0.1665`, yet `wrap_hold_hits=0`. Treat this as evidence that the current main blocker is post-grasp hold continuity rather than pure reach or close failure.
406. Current wrap500 reference wording should distinguish between strict benchmark success and practical near-success. The current reference model is `models/mustard_octo_official_wrap500_mix_k4_temporalcontinue_20260325/run_260325_161632/best_rollout_model`, which is an official Octo VLA closed-loop rollout model and not an oracle/scripted controller, but it is still `strict_success=false` under the current hold metric. Use `practical-success candidate` rather than `benchmark-success model`. Documentation reference: `docs/wrap500_practical_success_candidate_20260330.md`.
407. Public-repo upload baseline for the current wrap500 official-Octo stage should include the core end-to-end path only: collector (`scripts/data/collect_mustard_intent_benchmark.py`), raw-to-OXE export (`scripts/data/convert_mustard_raw_to_oxe.py`), shared mustard Octo adapter (`octo_data/mustard.py`), k4 basis / latent conversion scripts, official train/eval entrypoints (`scripts/train/finetune_mustard_octo.py`, `scripts/eval/mustard_intent_gym_env.py`, `scripts/eval/rollout_mustard_intent_octo_official.py`, `scripts/eval/run_mustard_intent_octo_interactive.py`), the latest wrap500 result note (`docs/wrap500_practical_success_candidate_20260330.md`), and one representative practical-success-candidate video asset at `docs/assets/wrap500_temporalcontinue_practical_success_candidate_20260330.mp4`. Keep `.octo_src_path`, `handoff/`, datasets, models, and transient logs out of Git.
408. The repository README should now be maintained as a full wrap500 handoff document, not just a short project overview. It must cover: what the repo is, environment split (`handvla` vs `octoketi`), the role of the major scripts, the detailed end-to-end process that produced the current wrap500 practical-success candidate (including data collection mix, collector fixes, k4 conversion, goal-image conditioning, and staged fine-tuning), and runnable command examples for collection, training, evaluation, and video reproduction.
